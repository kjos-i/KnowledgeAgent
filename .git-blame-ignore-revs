# Bulk reformats to ignore in `git blame` (git config blame.ignoreRevsFile
# .git-blame-ignore-revs, or set globally). Each entry is a commit that
# only restyled code without changing behavior.

# Adopt ruff across the repo: format + lint cleanup (2026-07-04)
46ce3f9499373e6c79ff34c63b473210ec6a076b
