"""Top-level tab modules — one per `ft.Tabs` entry.

Each tab module owns one tab's contents. The top-level `ft.Tabs`
in `app.py` composes them; switching tabs is the Flet Tabs control's
responsibility (no manual mode-state to track here).

Slice 1 ships Search fully wired; Library and Evaluation are
empty-state stubs filled in later slices.
"""
