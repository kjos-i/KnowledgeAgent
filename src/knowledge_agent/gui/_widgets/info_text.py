"""Central registry of (i) help-icon text, keyed by a stable ID.

Each entry is an `InfoText` (a title plus up to three tier strings:
standard, beginner, technical). Call sites render one with
`info(app, "<key>")` instead of hand-writing the prose inline — that
keeps the GUI layout code readable and gathers every help string in one
place to review and tone-check as a body.

Keys are namespaced by location, `"<area>.<control>"` (e.g.
`"settings.system_health"`, `"library.corpus.embedder"`), so the key
tells you where the icon lives. A `KeyError` from `info(app, key)` means
a typo'd or unregistered key — it fails loudly in dev rather than
rendering blank.

The widget, the three tiers (standard = blue `(i)`, beginner = green
cap, technical = orange wrench), and their Settings → App toggles live in
`info_icon.py`. This module is data only.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

from knowledge_agent.gui._widgets.info_icon import InfoText, info_point

if TYPE_CHECKING:
    import flet as ft

    from knowledge_agent.gui.app import GuiApp


INFO: dict[str, InfoText] = {
    "settings.system_health": InfoText(
        title="System health",
        standard=(
            "Checks the app can reach the parts it needs to answer a query, as "
            "four checks: the Neo4j knowledge graph (a quick round-trip), the "
            "LanceDB store (it opens it and lists tables), and one each for your "
            "active LLM and embedder API keys. The key checks only confirm a key "
            "is SET, not that it works — and a local provider (like Ollama, or a "
            "local embedder) passes without needing a key. So an all-green board "
            "means the plumbing is reachable, not that a query is guaranteed to "
            "succeed. Only the keys for the providers you've actually selected are "
            "checked; other providers' keys are ignored.\n\n"
            "The four checks run independently — one failing never blocks the "
            "others — and each shows OK in green or FAIL in red, with the details "
            "(and any fix, such as which key to set) shown when you hover it. "
            "Diagnostics runs the board automatically when you open it; Re-run "
            "repeats it, and you'll want to press Re-run after you create a "
            "corpus. In fact the checks only run once a corpus exists — a corpus "
            "supplies the database connection — so if you haven't made one yet, "
            "the panel shows a reminder instead of the checks. Make one in the "
            "Library tab and press Re-run."
        ),
        beginner=(
            "A quick self-test of the app's plumbing. It runs four small checks — "
            "can it reach the Neo4j database, can it open the LanceDB search "
            "store, and is an API key set for each of your two chosen models (the "
            "language model and the embedding model). Each check turns green and "
            "says OK if it's fine, or red and says FAIL if not; hover over one to "
            "see the details and, if it failed, what to do about it (for example, "
            "which key to add). One failing check won't stop the others from "
            "running.\n\n"
            "A couple of things to know: the key checks only look that a key is "
            "filled in, not that it truly works, and models that run on your own "
            "computer don't need a key at all — so all-green means the basics are "
            "in place, not a guarantee every query will work. And until you've "
            "created a corpus (your own searchable collection of documents) the "
            "app has no database to connect to, so instead of the checks it shows "
            "a reminder to make one. Create a corpus in the Library tab, press "
            "Re-run, and the checks appear."
        ),
        technical=(
            "Runs health.system_status(): four checks returned as chips in order "
            "— neo4j (a RETURN 1 round-trip), lancedb (opens the store via "
            "_ensure_conn() then list_tables()), llm_key and embed_key (a presence "
            "check on the active provider's key). Neo4j and LanceDB run "
            "concurrently (asyncio.gather, so wall-time is the slowest of the "
            "two); the key checks run inline after. Each check catches its own "
            "error and reports independently — a failure never blocks the others. "
            "A key check only tests the key is SET, not that it works; a local "
            "provider whose key attr is None auto-passes ('local (no key "
            "required)') — that's Ollama on the LLM side and HuggingFace on the "
            "embedder side. Only settings.llm_provider and "
            "settings.embedding_provider are checked; other providers' keys are "
            "ignored.\n\n"
            "Each chip renders '<name>: OK' (green) or '<name>: FAIL' (red) with "
            "the detail as a hover tooltip — neo4j 'RETURN 1 round-trip OK' or the "
            "exception repr; lancedb 'connected (N table(s))' or repr; keys "
            "'<provider>: set' / '<provider>: local (no key required)' / "
            "'<provider>: MISSING (set <KEY> in .env)'. The remediation hint lives "
            "only in the tooltip. The same system_status() backs the CLI (ka "
            "health via render_report_text).\n\n"
            "The report is gated on get_settings(), which is required-no-default "
            "for neo4j_uri / neo4j_user / neo4j_password / lancedb_path — but in "
            "practice only NEO4J_PASSWORD is missing without a corpus (the "
            "URI/user and a LANCEDB_PATH sentinel are always bridged to env), and "
            "the password is put in env only by an active corpus that has a saved "
            "keyring password. Without it get_settings() raises, system_status() "
            "returns a config_error with no chips, and the panel shows the "
            "matching 'not set — create a corpus in Library' prompt. The panel "
            "auto-runs _refresh_diagnostics() on first show (a '(running...)' "
            "placeholder first); Re-run re-triggers it (disabled while running) "
            "and is the way to refresh after a corpus is created elsewhere, since "
            "the panel doesn't auto-refresh on that event."
        ),
    ),
    "library.create_dataset": InfoText(
        title="Create a new corpus",
        standard=(
            "Registers a new corpus (a searchable collection with its own Neo4j "
            "graph and LanceDB store). Set up a database in Neo4j Desktop first, "
            "then fill in a name (it must be unique — an already-registered name is "
            "refused), its Neo4j connection (the URI + user defaults — "
            "neo4j://127.0.0.1:7687 and user 'neo4j' — fit a standard local Neo4j "
            "Desktop; the password is the one you set for that database), and a "
            "folder for the corpus's files. All five fields are required. Create "
            "tests the connection with a real query (~5s timeout), writes the "
            "corpus, and makes it the active one across the whole app — then you set "
            "layers and add documents in the Ingest tab.\n\n"
            "A radio picks the folder mode: 'Create a new folder named after the "
            "corpus' makes a <name> subfolder under the folder you pick; 'Use the "
            "selected folder as the corpus home' adopts that folder as-is (useful "
            "when it already holds your documents). At creation the folder gets just "
            "corpus.toml — the search index (lancedb/) is created on your first "
            "ingest, and a figures/ folder only appears if you later switch on "
            "figure extraction. Neo4j's own data stays in Neo4j Desktop, and the "
            "password goes to your system keychain — neither lives in the folder."
        ),
        beginner=(
            "A corpus is your own searchable collection of documents. Each one "
            "needs a place to store its files and a Neo4j database (the graph half "
            "of the search), so before this form open Neo4j Desktop and start a "
            "database. That gives you three things to copy in here: the connection "
            "address (URI), the user, and the password. The defaults usually fit a "
            "fresh local Neo4j Desktop, so often you only need to type the "
            "password.\n\n"
            "Give the corpus a short name (each corpus needs a different name), pick "
            "a folder for it, and press Create. The app first checks it can actually "
            "reach Neo4j (this can take a few seconds; if it can't, nothing is "
            "created and it tells you why), then it switches the whole app over to "
            "your new — still empty — corpus. You add documents afterward in the "
            "Ingest tab; nothing is searched yet.\n\n"
            "About the folder: two radio options let you either make a new "
            "sub-folder named after the corpus (the usual choice) or use a folder "
            "you already have. Right after creation that folder holds just one small "
            "settings file (corpus.toml); the search index and any figures are added "
            "later, when you ingest. Your Neo4j database stays in Neo4j Desktop and "
            "your password goes to your system's secure keychain — neither is kept "
            "in the folder."
        ),
        technical=(
            "Create validates the form, then runs a real RETURN 1 against the URI "
            "with the entered credentials (~5s timeout — a wrong password, "
            "unreachable host, or timeout aborts with a specific message and writes "
            "NOTHING: no folder, no keyring entry, no registry row). Validation "
            "before the ping: all of name/URI/user/password/folder are required "
            "(each with its own error); the name must be unique (compared "
            "case-insensitively — 'Papers' collides with 'papers') and free of "
            "characters a folder name can't contain (< > : \" / \\ | ? *, control "
            "chars, or a trailing dot), since it becomes a folder name and keyring "
            "account; and the target folder must not already contain a corpus.toml "
            "or a non-empty lancedb/ (the guard that makes adopt-mode safe). The "
            "Neo4j URI is deliberately NOT checked for uniqueness — corpora are "
            "distinguished by their saved password (keyring neo4j-<name>), and "
            "separate Neo4j Desktop instances commonly share one bolt port, so "
            "several corpora reusing 127.0.0.1:7687 is normal (each authenticates "
            "its own running instance).\n\n"
            "On success it writes a seed corpus.toml via _seed_corpus_config() = a "
            "bare CorpusConfig() (chunks layer on; all other L1–L10 off; xrefs none; "
            "all sub-labels allowed; no ontologies; no entities/extractor; embedder "
            "= Voyage / voyage-multimodal-3 / 1024 dims; chunker = hybrid / "
            "chunk_max_tokens 512 / merge_peers on; plus enable_image_ocr on, "
            "enable_pdf_ocr off, images_scale 1.0, extract_figures off, embed_images "
            "off, min_figure_bytes 2048, optimize_indexes_per_ingest on, and the "
            "L6/L8 extractor model + temperature=0.0 defaults). exclude_none=True is "
            "why only entities/cross_doc/cross_doc_xrefs are omitted from the file. "
            "The write is atomic (corpus.toml.tmp + rename); if the later keyring or "
            "settings.json save fails, the toml is unlinked so a retry isn't "
            "blocked.\n\n"
            "The password is saved to the OS keyring — service APP_NAME "
            "('KnowledgeAgent'), account neo4j-<name> — never on disk (CorpusEntry "
            "has no password field). 'Make it active' is broad: it appends a "
            "CorpusEntry (name, URI, user, lancedb_path, corpus_config_path), "
            "mirrors those onto the top-level GuiConfig, sets active_corpus_name, "
            "persists settings.json, bridges the connection + active-corpus embedder "
            "to env, sets NEO4J_PASSWORD, resets the cached LLM/embedder factories, "
            "rebuilds the top-bar corpus dropdown, and refreshes the Select + Ingest "
            "tabs — so query, health, and ingest all switch to the new corpus. The "
            "form then resets, the status reads 'corpus <name> created + active…', "
            "and a line is posted to chat.\n\n"
            "The corpus home holds only corpus.toml at creation; lancedb/ is created "
            "by LanceDB on first ingest and figures/<doc_id>/ lazily when "
            "extract_figures is on. Create-mode makes a <name> subfolder under the "
            "folder you pick; adopt-mode (a radio on this form) uses the folder "
            "itself. Neo4j's own data stays in Neo4j Desktop, outside the folder. "
            "Layers, ontologies, extractor and thresholds — and the per-run "
            "sub-labels — are set in the Ingest tab, not here."
        ),
    ),
    "library.corpus_folder": InfoText(
        title="Why one folder per corpus",
        standard=(
            "A corpus keeps its own files together in one folder — its config "
            "(corpus.toml) and vector store (lancedb/), plus figures/ for "
            "multimodal ingests when figure extraction is on — so a whole corpus "
            "can be moved or backed up largely as a unit. Keeping your source "
            "documents in this folder too is optional — the app reads them wherever "
            "they are, in place, and never copies them — but doing so makes the "
            "corpus more self-contained and makes Sync and Re-ingest dependable, "
            "since both work by finding your files again at a stable path. Only "
            "corpus.toml (plus lancedb/ once you ingest) is truly required; the "
            "documents are up to you.\n\n"
            "Two things a corpus needs live OUTSIDE the folder: the Neo4j graph (in "
            "Neo4j Desktop) and your API keys + DB password (in the OS keyring). And "
            "the app's own record of the corpus — its name and connection — is in "
            "the app's settings file, not the folder. So a folder copy alone isn't a "
            "complete backup, and a folder you move or copy has to be re-pointed in "
            "the Library tab before the app can use it."
        ),
        beginner=(
            "The idea: a corpus keeps its own files together in one folder, so it's "
            "easy to find, move, or back up. The app puts its own files there — a "
            "small settings file (corpus.toml) and the search index (lancedb/), plus "
            "a figures folder only if you've turned on figure extraction (not just "
            "because your documents contain pictures). A few things stay outside: "
            "the Neo4j database (it lives in Neo4j Desktop), your keys and password "
            "(kept in your system's secure keychain), and the app's own note of "
            "where this corpus is (in the app's settings). Because of those, a copy "
            "of the folder on its own isn't a full backup, and a folder you move "
            "needs re-pointing in the Library tab.\n\n"
            "You can also keep your actual documents in this folder — that's "
            "optional, the app can read them from anywhere — but if you do, two "
            "things work more smoothly: Sync (noticing when you add, change, or "
            "remove documents) and Re-ingest (re-reading a document), because both "
            "need to find your original files again. The part you can't skip is the "
            "corpus's own settings and index files — those ARE the corpus."
        ),
        technical=(
            "The folder holds the app-managed artifacts: corpus.toml (the config "
            "load_corpus_config requires — it must exist, though an empty file is "
            "accepted and yields a default config) and lancedb/ (the vector store, "
            "created on first ingest), plus figures/<doc_id>/ beside lancedb when "
            "extract_figures is on (standalone image documents are referenced in "
            "place and never written here; with extract_figures on but embed_images "
            "off, figure PNGs are saved but not embedded). Situational extras: "
            "eval_output/ (a SQLite eval_ledger.db + timestamped reports, from eval "
            "runs) and .ka_session.json (the GUI); dataset JSON is written here by "
            "the Evaluation dataset-authoring tab, not by running an eval; the "
            "atomic corpus.toml write can briefly leave a corpus.toml.tmp.\n\n"
            "NOT in the folder: the Neo4j graph (in Neo4j Desktop — only URI/user "
            "stored, password in the OS keyring), the corpus registry entry "
            "(name/URI/user/lancedb_path/corpus_config_path in the app's "
            "settings.json under the OS config dir), and the shared "
            "ontology-downloads cache (in the OS cache dir — potentially multiple "
            "GB). So a folder copy is not a full backup or a portable corpus by "
            "itself: the graph must be backed up/rebuilt separately (L5–L10 are "
            "reconstructable from LanceDB via backfills; L1–L4 and the live graph "
            "are not in the folder), and a moved/copied folder must be re-registered "
            "(Library → Relocate).\n\n"
            "Source files are never copied — ingest records each file's path (as "
            "posix) and reads it in place from any folder you point at. A stable, "
            "corpus-owned location matters for Sync (it hashes the SCANNED folder's "
            "files and compares them to stored paths -> NEW / MOVED / EDITED / "
            "ORPHAN, so a file moved WITHIN the scanned folder is MOVED — its stored "
            "path is patched — while a file gone from the scanned folder is ORPHAN, "
            "even if it still exists elsewhere) and per-document Re-ingest (re-reads "
            "the stored path, fails if it's gone). Re-embed and the backfills read "
            "chunk text from LanceDB, so they don't need the source file.\n\n"
            "Co-location is a create/adopt-time convention, not enforced at runtime: "
            "figures are derived from lancedb_path.parent (beside the vector store), "
            "not from corpus.toml, so if the stored lancedb_path pointed elsewhere "
            "they'd follow lancedb. Adopt-mode is the payoff: point 'Use the "
            "selected folder as the corpus home' at a folder that already holds your "
            "documents and the app writes lancedb/ + corpus.toml + figures/ "
            "alongside — a self-contained, portable-with-caveats corpus (the graph "
            "and registry entry still live outside)."
        ),
    ),
    "ingest.corpus_ingestion": InfoText(
        title="Corpus ingestion",
        standard=(
            "This column adds documents to the corpus currently chosen in the "
            'top-right "Selected corpus" dropdown — everything here, and the '
            "settings on the right, applies to that corpus. Switching the "
            "dropdown reloads this whole panel for the other corpus.\n\n"
            'To add documents, point "Ingest folder" at a folder (it includes '
            "every subfolder, supported file types only) or pick a single file. "
            "Ingest folder is additive: it only ingests files not already in the "
            "corpus, identified by their content — so re-selecting a folder skips "
            "unchanged files, and a file counts as new only if its contents "
            "changed. Documents are stored by content, not by folder path, so "
            'there\'s no folder "tree" — each file stands alone. (To reflect '
            "deletions or moves, use Sync, not Ingest folder — Sync can delete "
            "anything not found under the folder you point it at, so choose it "
            "carefully.)\n\n"
            "The settings on the right are this corpus's recipe — labels, which "
            "graph layers are built (L1–L10, below), the entity extractor, "
            "ontologies, thresholds, and the embedder. Edits are a draft (a dot "
            "marks unsaved changes; Discard reverts to the last saved state). The "
            "draft is saved the moment you run any ingest OR bulk operation — "
            "before the confirm dialog — so it sticks even if you then cancel. "
            "Turning a graph layer (L6–L10) OFF and then ingesting removes that "
            "layer's data across the whole corpus; turning one ON only affects "
            "new or re-ingested documents until you backfill it under Bulk "
            "operations."
            "\n\n"
            '"Freeze settings" (top) locks the embedder and the graph layers so '
            "they can't change between ingests — a safety catch; labels, paper "
            "resolution, and chunking stay editable.\n\n"
            "The layers, in order (each independently on/off, each building on "
            "the ones before): L1–L4 paper metadata (authors, venues, citations, "
            "topics); L5 chunks + embeddings (the searchable text); L6 entities "
            "(notable things pulled from chunks); L7 ontology linking "
            "(canonicalise entities against vocabularies like MeSH/GO); L8 "
            "triples (typed relations between entities in a chunk); L9 cross-doc "
            "links (docs sharing entities); L10 cross-doc xref links (docs "
            "sharing ontology concepts)."
        ),
        beginner=(
            "This column is where you add documents to the corpus selected in the "
            "top-right dropdown. Pick a folder (it includes all subfolders) or a "
            "single file. Adding a folder only brings in files that aren't "
            "already there — it recognises files by their contents, not their "
            'location, so nothing is a "folder tree", just a bag of '
            "documents.\n\n"
            "The panel on the right controls how documents are processed for this "
            "corpus. Changing something there is a pending edit (a dot shows "
            "unsaved changes; Discard undoes them). Your edits are saved the "
            "moment you run any ingest or bulk button — even if you then cancel — "
            "so what's shown is what gets used. Turning off a graph layer such "
            "as entities, relationships, or cross-document links and then "
            "ingesting removes that layer's data for the whole corpus, so it can "
            "delete, not just add.\n\n"
            '"Freeze settings" locks the parts that are risky to change (the '
            "embedding model and the graph layers) so you don't alter them by "
            "accident; labels, paper look-up, and chunking stay editable.\n\n"
            "The knowledge base is built in layers, L1 to L10 — from paper "
            "details and searchable text, up through entities, vocabulary links, "
            "relationships, and links between related documents. Each layer can "
            "be on or off per corpus, and each section on the right has its own "
            "(i) explaining it."
        ),
        technical=(
            "Everything here targets `gui_config.active_corpus_name` (top-right "
            "dropdown → `select_corpus` → `switch_active_corpus` → "
            "`refresh_ingest`). The right-column editor and the pending-changes "
            'summary read one shared `CorpusConfigEditor`: "current" = '
            "`_baseline_config` (a snapshot of the corpus's on-disk corpus.toml "
            'at load/save), "pending" = the live `_corpus_config` draft; the '
            "summary is `config_diff(baseline, draft)`.\n\n"
            "Ingest folder = `bulk_ops.add_plan`/`add_execute`: "
            '`_walk_and_hash(folder)` recursively walks `folder.rglob("*")` '
            "filtered to `parse.supported_extensions()`, `doc_id` = SHA-256 of "
            "bytes, files "
            "already indexed are skipped (additive; never enumerates the "
            "corpus). Re-ingest reprocesses all folder files; Sync's "
            "`sync_diff.classify` buckets NEW/UNCHANGED/MOVED/EDITED/ORPHAN and "
            "ORPHAN deletes are corpus-wide. A file edited in place gets a new "
            "`doc_id` → Ingest folder makes a duplicate (Sync's EDITED handles "
            "edits).\n\n"
            "Save-on-click: `try_save_and_get_error` writes the whole draft to "
            "corpus.toml before the confirm (persists on cancel; invalid draft "
            "blocks). Applying = `kg/reconcile.py` at the top of each "
            "`ingest_document` / `bulk_backfill_*` — a corpus-wide wipe of what "
            "an L6–L10 layer/ontology/entity-type the config dropped no longer "
            "covers (DETACH DELETE for L6/L7 nodes, edge DELETE for L8–L10; "
            "fail-hard); there is no reconcile for L1–L5, so those changes are "
            "additive. Freeze = the `CorpusConfig.frozen` flag; the editor "
            "disables the embedder + the L6–L10 section wrappers "
            "(`_apply_frozen_lock`).\n\n"
            "Layer → write module: L1–L4 `openalex_writes`, L5 `chunk_writes`, L6 "
            "`entity_writes`, L7 `ontologies/*`, L8 `triples_writes`, L9 "
            "`cross_doc_writes`, L10 `cross_doc_xrefs_writes`. Cascade L5→L10: "
            "rebuilding chunks or entities rebuilds dependents; L8/L9 read raw "
            "entities, L10 rides on L7 canonicals+xrefs."
        ),
    ),
    "ingest.folder_actions": InfoText(
        title="Ingest folder, Re-ingest & Sync",
        standard=(
            "These three buttons all act on the folder you picked above (including its "
            "subfolders — the scan is recursive), and each usually ends in a summary "
            "you confirm before anything runs. Only supported file types are picked up; "
            "anything else is quietly ignored. They differ in what they do to documents "
            "already in the corpus.\n\n"
            "Ingest folder adds only files whose content isn't already in the corpus; "
            "anything already there is skipped, so it's safe to run repeatedly. It "
            "won't remove documents you deleted from the folder, and a file you edited "
            "in place is added as a second copy beside the old one. One caveat that "
            "applies to all three: each run first saves your pending settings and "
            "reconciles the corpus to them — so if you'd turned a graph layer off, that "
            "layer's data is wiped corpus-wide as part of the run (so at the graph "
            "level it doesn't only add).\n\n"
            "Re-ingest reprocesses every file in the folder from scratch, replacing the "
            "data of documents already present (no duplicates of unchanged files) — and "
            "it also adds any brand-new files it finds. Use it after changing the "
            "corpus's settings, to rebuild existing documents under them, or to fix a "
            "document that parsed badly. Like Ingest folder, it won't delete removed "
            "files or clean up edited-file copies. Its confirm summary warns you if any "
            "documents you hand-edited will have their metadata replaced.\n\n"
            "Sync makes the corpus match the folder exactly: it adds new files, updates "
            "moved files, replaces edited files, and DELETES any corpus document with "
            "no matching file in the folder. It treats the folder as the complete set "
            "for this corpus, so a partial or wrong folder makes it propose deleting "
            "everything else — deletion can't be undone, though the confirmation lists "
            "every document it will remove. The picked labels are applied to the new "
            "(and re-ingested) files across the folder.\n\n"
            "The confirm dialog is worth reading each time — it shows the file count "
            "and total size (your check against picking the wrong folder), how many are "
            "already in the DB, and, for Sync, exactly how many will be added / moved / "
            "replaced / deleted. If there's nothing to do, the run just says so with no "
            "dialog; an invalid setting or a missing provider key stops it with a "
            "message instead. A run reports how many files succeeded and failed — one "
            "bad file doesn't abort the rest.\n\n"
            "Rule of thumb: Ingest folder to add, Re-ingest to rebuild, Sync to mirror "
            "a folder exactly."
        ),
        beginner=(
            "A corpus is your own searchable collection of documents. These three "
            "buttons all work on the folder you picked above — and everything inside "
            "its subfolders too — and usually show a summary to confirm first. Only "
            "file types the app understands are picked up; the rest are skipped. They "
            "differ in how they treat documents you already added.\n\n"
            "Ingest folder adds documents that aren't in your corpus yet and skips the "
            "rest, so running it again is safe and never duplicates unchanged files. It "
            "won't remove a document because you deleted its file, and if you change a "
            "file after adding it, you'll end up with two copies. (One thing that's "
            "true for all three: each run first saves your current settings and brings "
            "the corpus in line with them — so if you'd switched a knowledge-graph "
            "feature off, that feature's data gets cleared across the whole corpus as "
            "part of the run.)\n\n"
            "Re-ingest rebuilds documents from your files. It goes over every file in "
            "the folder again from scratch and replaces what's stored rather than "
            "duplicating it — and it adds any new files too. Reach for it when you "
            "changed a corpus setting and want documents you already added rebuilt with "
            "it, or to give a document that came out wrong a clean run. Its summary "
            "warns you if any documents whose details you edited by hand will be "
            "overwritten.\n\n"
            "Sync makes your corpus match the folder exactly — think of the folder as "
            "the master copy. It adds new files, follows moved ones, replaces edited "
            "ones, and removes from the corpus any document whose file is no longer in "
            "the folder. That removal is permanent, so if you pick a folder missing "
            "some of your documents, Sync will offer to delete them (it shows the full "
            "list first). When unsure, use Ingest folder — it only adds documents.\n\n"
            "Always read the summary: it shows how many files and how big (your check "
            "against picking the wrong folder), how many are already added, and for "
            "Sync how many will be added, moved, replaced, and deleted. If there's "
            "nothing to do it just says so; a bad setting or a missing key stops it "
            "with a message. It tells you how many files succeeded and failed — one bad "
            "file won't stop the others.\n\n"
            "Short version: Ingest folder to add new documents, Re-ingest to rebuild "
            "existing ones, Sync to make the corpus mirror a folder."
        ),
        technical=(
            "All three act on the picked folder RECURSIVELY (rglob), filtered to "
            "parse.supported_extensions() (other files silently ignored), and share "
            "the plan→confirm dialog, a cooperative Cancel (stops at a document "
            "boundary), and a missing-key pre-flight — the EMBEDDER key is always "
            "required (even for a delete/move-only Sync), plus the LLM key when the "
            "corpus extracts with 'llm' or has triples on; local Ollama/HuggingFace "
            "need none. Preconditions: an active corpus and a picked folder. Starting "
            "any action disables all four action buttons + both Browse until it "
            "finishes (no concurrent runs). Each run first saves the config editor to "
            "corpus.toml (an invalid config aborts with a dialog, nothing runs), and "
            "reconcile_*_to_config runs INSIDE each ingested file's "
            "pipeline.ingest_document (per NEW/EDITED file, not once per action, and "
            "NOT for MOVED/ORPHAN-only work) — wiping disabled-layer data corpus-wide "
            "(fail-hard). doc_id is a SHA-256 of the file's bytes (content, not path); "
            "two byte-identical files in one folder dedupe to one doc but both count "
            "processed. All three are per-file fail-soft (a bad file is logged + "
            "counted failed, the run continues; result = N succeeded / M failed). An "
            "empty plan short-circuits with a status line and no dialog.\n\n"
            "Ingest folder → bulk_ops.add_plan / add_execute: walks+hashes the folder, "
            "skips any file whose doc_id already has chunks in LanceDB "
            "(get_chunks_by_doc_id), ingests the rest via pipeline.ingest_document. "
            "Dedup by content hash, not path; never enumerates the index, never deletes "
            "documents, never re-paths. An edited-in-place file (new doc_id) is added "
            "while the old doc_id remains — a duplicate. Confirm summary: new-file "
            'count + total MB + "M already in DB skipped".\n\n'
            "Re-ingest → bulk_ops.ingest_folder_plan / ingest_folder_execute: runs the "
            "full pipeline on EVERY supported file — including brand-new ones (it's "
            "Ingest-folder ∪ forced-overwrite), no orphan handling; ingest_document "
            "deletes each doc across LanceDB + all Neo4j layers then rewrites it "
            "(replace). The Overwrite checkbox = preserve_existing_labels INVERTED "
            "(preserve_existing_labels = not overwrite; default off = preserve on): off "
            "keeps a doc's stored (main, sub) labels, on forces the picked labels — "
            "labels only, content/graph rebuilt either way. Confirm summary: file count "
            '+ MB + "overwrite K already in DB" + "N have manual metadata that will '
            'be replaced".\n\n'
            "Sync → bulk_ops.sync_plan / sync_execute over sync_diff.classify: hashes "
            "the folder AND enumerates the whole corpus index (list_indexed_docs), "
            "classifying NEW / UNCHANGED / MOVED / EDITED / ORPHAN (path matching "
            "case-sensitive). NEW = ingest (gets the picked labels); MOVED = a cheap "
            "metadata-only patch of LanceDB source_path (no re-parse/re-embed/KG "
            "change); EDITED = delete old + ingest new (old labels carried only when "
            "Overwrite is off — else picked labels); ORPHAN = delete via "
            "pipeline.delete_doc (irreversible), scoped to the WHOLE corpus, so a "
            "partial folder orphans everything else. A file both moved AND edited is "
            "classified ORPHAN (old path deleted) + NEW (EDITED requires the same "
            'path). Confirm summary: "X new / Y moved / Z edited / W orphans to '
            'DELETE", and lists the orphan names (orphan_display_names). (Ingesting a '
            "Paper-labelled file also triggers a network OpenAlex L1–L4 lookup — "
            "is_doi_eligible.)"
        ),
    ),
    "ingest.single_file": InfoText(
        title="Ingest single file",
        standard=(
            "Ingest single file adds one document — the file shown in the File field "
            "above (pick it with that Browse button) — into the active corpus, "
            "wherever it lives on disk. It runs the same full pipeline as the folder "
            "buttons for that one file: it reads and splits the file, builds its "
            "search vectors, and fills in whichever knowledge-graph layers the corpus "
            'has on. It asks you to confirm first ("Ingest <name> into the active '
            'corpus?").\n\n'
            "It behaves like a one-file Re-ingest, not like Ingest folder: it ALWAYS "
            "reprocesses the file, so ingesting the same file again replaces its data "
            "(no duplicate) rather than skipping it. Like every ingest, it first saves "
            "your pending settings and reconciles the corpus to them — so if you'd "
            "turned a graph layer off, that layer is wiped corpus-wide as part of this "
            "one-file run. Once it starts it can't be cancelled.\n\n"
            "When to use it: to add or refresh a single document without scanning a "
            "whole folder. When not to use it: for many files at once (Ingest folder); "
            "to remove documents or mirror a folder (Sync); to rebuild everything "
            "after a settings change (Re-ingest). And it doesn't clean up edits — if "
            "you ingest a file you've changed since first adding it, the old version "
            "stays behind as a separate copy (use Sync to truly replace an edited "
            "file).\n\n"
            'One important caveat: the status says "Ingested" as long as no error '
            "was raised — but individual steps fail quietly. If embedding fails, the "
            "document is stored with NO searchable chunks; if a graph-layer write "
            'fails, that layer is skipped — yet you\'ll still see "Ingested." So a '
            "green message isn't a guarantee every part landed. It can also fail up "
            "front: an unsupported file type, a missing provider key, or a "
            'not-yet-installed parser/extractor stops it with "Ingest failed" before '
            "anything is written."
        ),
        beginner=(
            "A corpus is your own searchable collection of documents. This button adds "
            "one document to it — the file in the File field just above (click its "
            "Browse to choose one), wherever it is on your computer. It runs the same "
            "full processing as the folder buttons, for exactly one file. It shows "
            '"Ingest <name> into the active corpus?" and waits for you to '
            "confirm.\n\n"
            "It works like a one-file Re-ingest: adding the very same file again "
            "re-does it and refreshes its data (you won't get two copies). Use it when "
            "you just want to add or redo one document — no need to gather files into "
            "a folder first. (Like any ingest, it first saves your current settings "
            "and brings the corpus in line with them, so switching a graph feature off "
            "then ingesting one file clears that feature across the whole corpus.)\n\n"
            "Skip it when you have lots of files (Ingest folder does the whole folder "
            "at once), or when you want your corpus to match a folder — adding new, "
            "removing deleted — which is Sync's job. If you changed a file after first "
            "adding it and ingest it here, the older version isn't removed and you'd "
            "end up with both; to replace an edited document cleanly, use Sync.\n\n"
            "Two things to know. It only handles file types the app understands (PDFs, "
            "Office docs, text/markdown, images, audio/video, code, and more) — and "
            "some of those need an extra component installed, or an API key, so it can "
            'stop with "Ingest failed" before adding anything. And a "done" message '
            "means it didn't hit an error — but if, say, the search-indexing step "
            "quietly failed, the document could be added without being searchable, so "
            "it's worth a quick check that a new document actually turns up in search."
        ),
        technical=(
            "The button → _on_ingest_file_clicked → _start_action('Ingest single "
            "file') → _run_action → _plan_single_file → _execute_single_file, which "
            "calls pipeline.ingest_document(path, config, main, sub, "
            "preserve_existing_labels=not overwrite) on the single path in the File "
            "field (its own picker, hand-typable + persisted on blur, pre-filled per "
            'corpus from the last single-file ingested; empty → "Pick a file first '
            '(Browse)."). No folder walk/hash: the confirm dialog is "Ingest <name> '
            'into the active corpus?" (title/OK button "Ingest single file"), not a '
            "scanned plan summary, and the run isn't cancellable (Cancel is armed only "
            "for folder executes).\n\n"
            "Shared ingest contract (in _run_action): saves the config editor first "
            "(invalid config → dialog, no run), the missing-key pre-flight runs "
            "(embedder key always; LLM key when extract='llm' or triples on; local "
            "exempt), and ingest_document runs the corpus-wide reconcile_*_to_config — "
            "so ingesting one file with a layer just disabled wipes that layer across "
            "the whole corpus (fail-hard). If the entities layer is on, ingest_document "
            "also validates every configured extractor's package + weights (else "
            'WeightsNotDownloadedError → "Ingest failed"). doc_id = SHA-256 of the '
            "file's BYTES: identity is content, not path/name — renaming re-ingests as "
            "the same doc; two byte-identical files dedupe; ingesting an identical file "
            "re-runs delete-then-write on the same doc_id (replace); an edited file "
            "(new doc_id) is written new while the old doc_id lingers (only Sync "
            "reconciles that).\n\n"
            "What it builds: parse (format dispatch by extension via "
            "supported_extensions() — docling for "
            "PDF/Office/HTML/MD/images/JATS-xml/csv/AsciiDoc/LaTeX/vtt/audio-video, "
            "json_parser for json/jsonl/ndjson, code_parser for "
            "py/ts/js/go/rust/java/c/… which needs the parsers-code extra; audio/video "
            "need parsers-asr + an ffmpeg binary — a supported extension can still "
            'raise in parse() → "Ingest failed") → chunk → embed → LanceDB write, '
            "then KG layers as configured: OpenAlex L1–L4 (Paper-only, "
            "is_doi_eligible), chunks L5, entities L6, ontology L7, triples L8, "
            "cross-doc L9, cross-doc-xrefs L10.\n\n"
            "Failure reporting is loose: _execute_single_file discards the returned "
            'IngestResult and writes "Ingested <name>." whenever ingest_document '
            "doesn't RAISE — it never checks embed_ok / lancedb_ok / kg_*_ok. A caught "
            "embed failure sets embed_ok=False and then SKIPS the LanceDB write (the "
            "doc has zero searchable chunks); a caught LanceDB or any KG-layer write "
            'failure is likewise swallowed — all still report "Ingested." Only a '
            'raised exception yields "Ingest failed." Two GUI-side guards run BEFORE '
            'the pipeline in _plan_single_file: Path.is_file() (fail → status "not a '
            'file: <path>", ingest_document never called), while the extension check '
            'is inside ingest_document (fail → raises → "Ingest failed"). Labels: '
            "main/sub from the Labels section apply to the doc; Overwrite "
            "(preserve_existing_labels = not overwrite; default off = preserve) "
            "governs whether an already-present file keeps its stored labels."
        ),
    ),
    "ingest.progress": InfoText(
        title="Progress",
        standard=(
            "The Progress line is the live status of the current job. Before you "
            "run anything this session it shows 'Empty'; after a run it keeps the "
            "last message — a summary or an error — until your next action, so it "
            "isn't reset back to 'Empty'.\n\n"
            "Every ingest action (Ingest folder / Re-ingest / Sync / single file) "
            "opens with a spinner and '<action>: preparing…' while the ingestion "
            "engine loads (slow only on the first run of a session). If the corpus "
            "is missing a key it needs — the embedder key, or the LLM key when the "
            "corpus extracts entities or triples — the run stops right here with a "
            "pop-up asking you to add it, and the line stays on 'preparing…' behind "
            "the dialog.\n\n"
            "A folder action then shows '<action>: scanning <folder>…' while it "
            "hashes every file to build the plan. After you confirm, a progress bar "
            "replaces the spinner and the line counts documents — '<action>: 3 / 20 "
            "files' — ending in a summary like 'Ingest folder done: 18 succeeded, 2 "
            "failed.' or 'Sync done: 5 new, 3 moved, 1 re-ingested, 2 removed, 0 "
            "failed.'\n\n"
            "Ingest single file skips the scan and just shows 'Ingesting <name>…' "
            "then \"Ingested '<name>'.\" (no bar). Bulk operations use this same line "
            "with no bar and no Cancel: '<op>: working…' then '<op> done: …'. Other "
            "messages you may see: 'nothing to do' when the folder holds nothing to "
            "change; 'No active corpus.'; 'Pick a folder first (Browse).'; a 'could "
            "not load corpus.toml' or 'could not plan' error; and, on a failed run, "
            "'<action> failed: …' (a single file that fails reads 'Ingest failed: "
            "…').\n\n"
            "Cancel: the button is only active while a folder ingest (Ingest folder "
            "/ Re-ingest / Sync) is actually running — it's greyed the rest of the "
            "time, including during the initial scan and for single-file or "
            "bulk-operation runs. Pressing it doesn't stop instantly: the document "
            "being processed right now finishes, then the run stops before the next "
            "one. Anything already ingested stays — Cancel never undoes finished "
            "work — so you're left with a partial ingest and the summary is prefixed "
            "'Cancelled —'. If the current file is large it can take a moment to "
            "stop, even though the button greys out the instant you click it."
        ),
        beginner=(
            "This line tells you what the app is doing right now. When you first "
            "open the app it says 'Empty'; after you run something it shows how that "
            "went and keeps that message until you start the next thing.\n\n"
            "When you run one of the ingest buttons a small spinner appears and the "
            "line describes each step in words — first it gets ready (and, for a "
            "folder, reads through your files), then a bar fills up and the line "
            "counts documents as it goes, like '3 / 20 files', so you can see how "
            "far along it is. When it finishes, the line shows a short summary — how "
            "many documents were added, and how many failed if any. (If the app is "
            "missing a key it needs for the model, it stops right at the start and "
            "pops up a message asking you to add the key.)\n\n"
            "Some messages you might see: 'Ingesting <name>…' then \"Ingested "
            "'<name>'.\" for a single file; 'nothing to do' if there was nothing new "
            "to process; or a short error if something went wrong (like no corpus "
            "selected, or a folder not picked yet). While the spinner is turning "
            "it's still working — let it finish.\n\n"
            "About the Cancel button: it only works while documents are actually "
            "being added from a folder (Ingest folder, Re-ingest, Sync) — otherwise "
            "it's greyed out. When you press it, it doesn't stop everything at once: "
            "it lets the document it's currently working on finish, then stops "
            "before starting the next. Whatever was already added stays added — it "
            "won't undo them — so you end up with some documents in and the rest "
            "not, and the summary starts with 'Cancelled'. If the current document "
            "is big it may take a little while to stop even though the button greys "
            "straight away; that's normal."
        ),
        technical=(
            "Three controls share this section — an indeterminate spinner "
            "(`progress_ring`), the status `Text`, and a `Cancel` button — with a "
            "determinate `progress_bar` below. 'Empty' is only the initial "
            "placeholder set once at control creation; the line is never reset to it "
            "— after a run it holds the last message (summary, error, or a brief "
            "blank while the confirm dialog is open).\n\n"
            "Every INGEST action opens with '<action>: preparing…' (spinner) during "
            "the off-thread backend import (`_run_action`). Before a folder or "
            "single-file ingest, a key preflight (`_missing_ingest_key`) checks the "
            "embedder key (always required) and the LLM key (only when the corpus "
            "extracts with an LLM / has triples on); a miss aborts into a 'missing "
            "key' dialog and clears the spinner but leaves the line still reading "
            "'preparing…'. Bulk operations do NOT show 'preparing…' — they import "
            "synchronously and open straight on '<op>: working…'.\n\n"
            "Folder actions scan BEFORE the confirm dialog: `_run_action` writes "
            "'<action>: scanning <folder-name>…' during `_walk_and_hash` (in "
            "`bulk_ops`), and an empty plan short-circuits to '<action>: nothing to "
            "do in <full-folder-path>.' (basename in the scan line, full path here). "
            "After confirm, `_execute_action` runs: '<action>: working…' with the "
            "bar at 0 for the first document, then `_on_ingest_progress` sets the "
            "bar to done/total and writes '<action>: {done} / {total} files' per "
            "item — for Sync, per item across the new/moved/edited/orphan buckets "
            "(the word stays 'files' even for moved/removed items). The final line "
            "is `_fmt_ingest_result` ('<action> done: N succeeded, M failed.' [+ "
            "'First failure: <name> — <err>']) or `_fmt_sync_result` ('Sync done: X "
            "new, M moved, Y re-ingested, Z removed, W failed.').\n\n"
            "The bar appears only for folder executes. Ingest single file runs with "
            "the spinner only ('Ingesting <name>…' → \"Ingested '<name>'.\" [name "
            "quoted], or 'Ingest failed: <exc>' on error), as do the bulk operations "
            "('<op>: working…' → '<op> done: …' / '<op> failed: …'). Many other "
            "setup/error strings render on this same line: 'No active corpus.', "
            "'could not load corpus.toml: <exc>' (no action prefix), '<action>: "
            "could not load ingestion backend — <exc>', '<action>: could not plan — "
            "<exc>', 'Pick a folder first (Browse).' / 'Pick a file first "
            "(Browse).', 'not a file: <path>', folder/file 'picker error: <exc>', "
            "and (bulk) '<op>: not wired.' / 'Pick an ontology to clear.'\n\n"
            "Cancel is a cooperative flag (`_cancel_requested`), armed only by "
            "`_execute_action` (folder ingests) — single-file and bulk-op executes "
            "never enable it, and it's dead during the pre-confirm scan/hash. It's "
            "polled at the top of each per-item loop in `bulk_ops` (add / "
            "ingest_folder / sync), never inside `ingest_document`, so the in-flight "
            "document's full parse→embed→write completes and the loop then breaks at "
            "that boundary — no mid-document abort, no rollback; prior writes to "
            "both stores stay. Pressing it writes 'Cancelling — will stop after the "
            "current file…', greys the button (one press), and no-ops when idle; the "
            "final line is prefixed 'Cancelled —' with the partial counts. For Sync "
            "the break also skips all remaining NEW/MOVED/EDITED/ORPHAN items, "
            "leaving the corpus partially reconciled (the stop delay only matters "
            "for NEW/EDITED items, which re-ingest; moved/orphan items patch or "
            "delete instantly). Two edges: if pressed while the last item is already "
            "processing, everything completes but the summary still reads 'Cancelled "
            "—'; and if the executor itself raises a top-level error while a cancel "
            "is pending, the line shows '<action> failed: <exc>' rather than the "
            "'Cancelled —' prefix. The flag is reset only at the next folder "
            "execute's start, not at run-end."
        ),
    ),
    "ingest.infrastructure": InfoText(
        title="Ingest infrastructure (read-only)",
        standard=(
            "These four values are process-wide tuning knobs. They are "
            "shared by every corpus, not set per-corpus, which is why they "
            "are shown here read-only. To change one, edit the .env file "
            "(or set the matching environment variable) and restart the "
            "app. They are not editable from the Settings tab. The first "
            "three govern every outbound HTTP request the app makes "
            "(paper-metadata lookups on OpenAlex, ontology-file downloads, "
            "the FIBO GitHub listing, and the Ollama reachability check). "
            "The fourth governs how much ingest work runs at the same "
            "time.\n\n"
            "http_default_timeout (default 30.0): the number of seconds an "
            "outbound request waits before giving up. Some steps set their "
            "own value instead (for example, the Ollama reachability check "
            "uses 1 second, and large ontology downloads use no limit at "
            "all). A typical value is between 10 and 60.\n\n"
            "http_max_retries (default 3, minimum 0): how many extra times "
            "a failed request is retried, and only for rate-limit (429), "
            "server (5xx), and network errors. 0 turns retrying off. "
            "Streaming downloads never retry. The wait grows between "
            "attempts (1 second, then 2, then 4). A typical value is 0 to "
            "5.\n\n"
            "http_user_agent (default knowledge-agent/0.x): the identifier "
            "sent with every request. Some services (GitHub, OpenAlex) "
            "base their rate limits on it. If you query them a lot, it is "
            "good practice to add a contact email, for example "
            "knowledge-agent/0.x (you@example.com).\n\n"
            "pipeline_max_concurrent_chunks (default 8, allowed 1 to 64): "
            "within a single document, how many chunks are worked on at "
            "once during the two model-heavy graph steps, entity "
            "extraction (L6) and triple extraction (L8). Higher is faster "
            "but makes more model calls at the same time, which can run "
            "into a provider's rate limit. Embedding is not affected: it "
            "goes out as one batched call."
        ),
        beginner=(
            "These are advanced settings that apply to the whole app, the "
            "same for every corpus. They are shown here only so you can "
            "see them. You change them in a text file called .env, and a "
            "restart is needed. Most people never need to. Here is what "
            "each one means in plain terms:\n\n"
            "http_default_timeout: how long the app waits for a reply from "
            "the internet before giving up (default 30 seconds).\n\n"
            "http_max_retries: how many times it tries again when an "
            "internet request fails (default 3; set to 0 to never "
            "retry).\n\n"
            "http_user_agent: the name the app gives when it contacts a "
            "service (default knowledge-agent/0.x). You can add your email "
            "so those services can identify you.\n\n"
            "pipeline_max_concurrent_chunks: how many pieces of one "
            "document are processed at the same time during the heavier "
            "graph-building steps (default 8). Higher is faster but works "
            "the language model harder."
        ),
        technical=(
            "All four are global Settings fields (config.py), not "
            "CorpusConfig, so they are process-wide and read here via "
            "_read_global (which falls back to each field's Pydantic "
            "default when get_settings() is unavailable). They are not "
            "surfaced in the Settings tab; edit .env or the environment "
            "and restart. get_settings() is lru_cached (cleared on "
            "key/provider/corpus changes), so an .env edit is reliably "
            "applied only on restart.\n\n"
            "http_default_timeout (float, default 30.0, no bound): passed "
            "to httpx.AsyncClient(timeout=...) at client build and used as "
            "the per-call default in _http_client.request() and .stream(). "
            "Steps may override it: the Ollama daemon probe passes "
            "timeout=1.0 and max_retries=0 (llm_lifecycle.py); the FIBO "
            "tree API passes 60; ontology streaming downloads pass "
            "timeout=None (unbounded).\n\n"
            "http_max_retries (int, default 3, ge=0): the retry budget in "
            "_http_client.request(); the request loop runs range(retries "
            "+ 1) times. Retries fire only on 429, 5xx, and httpx "
            "NetworkError/TimeoutException, with exponential backoff "
            "(2**attempt seconds). .stream() never retries. 0 means a "
            "single attempt.\n\n"
            "http_user_agent (str, default knowledge-agent/0.x): set as "
            "the User-Agent header on the shared AsyncClient, so it rides "
            "every outbound request (OpenAlex, GitHub tree API, ontology "
            "downloads, Ollama probe).\n\n"
            "pipeline_max_concurrent_chunks (int, default 8, ge=1 le=64): "
            "sizes the asyncio.Semaphore bounding the per-chunk fan-out "
            "for L6 entity extraction (pipeline.py) and L8 triple "
            "extraction. It does NOT bound embedding, which is a single "
            "batched embed_chunks(chunks) call."
        ),
    ),
    "ingest.resolve_paper_metadata": InfoText(
        title="Resolve paper metadata (L1-L4)",
        standard=(
            "This is for scientific papers. When it is on, ingesting a paper "
            "looks it up on OpenAlex (a free scholarly database) by the "
            "paper's DOI and, from that single lookup, fills in the paper's "
            "details (title, authors, venue, year) and builds the graph "
            "around it: the works it cites (L1), its authors (L2), its venue "
            "(L3), and its topics (L4).\n\n"
            "It is off by default and opt-in, like the other layers. When it "
            "is off, no OpenAlex lookup happens at all, so papers come in "
            "with just their text and no OpenAlex details. Only documents "
            "labelled Paper are ever looked up (they are the ones with a "
            "DOI); other documents are never affected either way. Turn it on "
            "for a corpus of research papers; leave it off otherwise. It "
            "needs internet access.\n\n"
            "You do not have to re-ingest to add this later. If you ingest "
            "papers with the box off and want their details afterwards, run "
            "Bulk Resolve (under Bulk operations), which fetches the metadata "
            "for papers already in the corpus without re-ingesting.\n\n"
            "openalex_mailto (read-only, shown below): your email address for "
            "OpenAlex's polite pool, which gives faster and more reliable "
            "lookups. It is optional. Lookups still work without it, just "
            "with tighter rate limits. It is a shared, process-wide setting, "
            "edited in the .env file (not the Settings tab)."
        ),
        beginner=(
            "Use this if your documents are scientific papers. When it is on, "
            "the app looks each paper up in a big free research database "
            "called OpenAlex (using its DOI, a paper's unique ID) and fills "
            "in useful facts: its title, who wrote it, where it was "
            "published, what it cites, and its topics.\n\n"
            "It starts off and is opt-in. When it is off, the app does not "
            "contact OpenAlex at all, so papers come in with just their text. "
            "Only documents you have labelled Paper are ever looked up. Turn "
            "it on for a library of research papers; leave it off otherwise. "
            "It needs an internet connection.\n\n"
            "If you add papers with this off and want their details later, "
            "you do not have to add them again: use the Bulk Resolve button "
            "(under Bulk operations) to fetch the details for papers already "
            "in your corpus.\n\n"
            "openalex_mailto: an email address the app can send to OpenAlex "
            "so look-ups are faster and more reliable. It is optional, shown "
            "here read-only, and set in a file called .env if you want it."
        ),
        technical=(
            "Toggles layers.openalex_papers (default False, opt-in like every "
            "other layer). During ingest, the OpenAlex step runs only when "
            "is_doi_eligible(sub_label) AND config.layers.openalex_papers "
            "(pipeline.py). Paper is the only DOI-eligible sub-label, so a "
            "non-Paper doc is never looked up, and with the box off NO "
            "document is looked up: no HTTP call, no LanceDB metadata fill, "
            "no L1-L4, metadata_status stays baseline. This is the gate that "
            "makes the box control the lookup, not just the graph.\n\n"
            "When on, resolution is one HTTP GET to OpenAlex "
            "/works/doi:{doi} (ingestion/metadata.py resolve_doi), after a "
            "DOI is collected from the paper (JATS/XML structured metadata "
            "via source_path first, else a regex over the first few chunks). "
            "One work record fills the LanceDB doc columns (title, "
            "authors_display, venue, year, doi, openalex_id, ...) and feeds "
            "all four writers in kg/openalex_writes.py: L1 citations "
            "(:CITES), L2 (:Author + :AUTHORED), L3 (:Venue + :PUBLISHED_IN), "
            "L4 (:Topic + :ABOUT_TOPIC). metadata_status becomes enriched "
            "(resolved), pending (a DOI was found but did not resolve), or "
            "baseline (no DOI, non-Paper, or the box off).\n\n"
            "Bulk Resolve (Bulk operations) is the explicit, on-demand path "
            "via resolve_openalex; it reads a doc's existing chunks and "
            "patches metadata + L1-L4 in place with no re-parse, so you never "
            "need to re-ingest to add this later. There is no reconcile for "
            "L1-L4, so turning the box off does not wipe a graph already "
            "built. openalex_mailto (config.py, str, default None) adds the "
            "mailto query param for the polite pool; process-wide, read via "
            "_read_global, set in .env or the environment (not the Settings "
            "tab). The OpenAlex base URL is a fixed constant, not a setting."
        ),
    ),
    "ingest.chunks_embeddings": InfoText(
        title="Chunks and embeddings (L5)",
        standard=(
            "This section controls how each document is split into searchable "
            "pieces (chunks) and, together with the embedder, turned into the "
            "vectors that power search. The chunks layer (L5) is the "
            "foundation of retrieval, so it is on and effectively always "
            "needed: with it off, a document has no searchable text.\n\n"
            "Chunking, how the text is split.\n\n"
            "Chunker strategy (default hybrid): hybrid respects the document's "
            "structure (headings, paragraphs, tables) and also splits any "
            "piece longer than the token cap below. hierarchical follows "
            "structure only, with no size cap, so chunks match sections "
            "exactly but can be very large. Hybrid suits passage search; "
            "hierarchical suits whole-section retrieval.\n\n"
            "Chunk max tokens (default 512, minimum 64): the size cap for "
            "hybrid chunking (hierarchical ignores it). Higher means broader "
            "context per chunk but fewer, larger chunks and more storage "
            "each. 256 to 1024 is a normal range.\n\n"
            "Merge peers (default on): a hybrid option (hierarchical ignores "
            "it). On, it greedily merges small adjacent pieces in the same "
            "section into fewer, larger chunks; off leaves each structural "
            "unit on its own.\n\n"
            "Changing the strategy or the token cap affects only newly "
            "ingested documents; existing documents keep their old chunk "
            "shapes until you re-ingest them (a backfill does not re-chunk), "
            "so a corpus can end up with mixed chunk sizes.\n\n"
            "Reading images (OCR).\n\n"
            "Enable pdf ocr (default off): read text from scanned or "
            "image-only PDFs. Most research PDFs are born-digital (their text "
            "is already extractable), so this is off by default; turn it on "
            "for scanned documents.\n\n"
            "Enable image ocr (default on): read text from standalone image "
            "files (PNG, JPG, TIFF). On by default, since without it an image "
            "has no searchable text.\n\n"
            "Images scale (default 1.0): how finely PDFs and images are "
            "rendered. Higher gives more detail (clearer OCR on small text, "
            "and sharper extracted figures) but is slower. 1.0 to 3.0 is "
            "typical.\n\n"
            "Figures (multimodal, all off by default).\n\n"
            "Extract figures (default off): pull figure images out of PDF, "
            "Word, and PowerPoint documents and save them alongside the "
            "corpus. Standalone image files are always referenced in place "
            "regardless. Leave off for text-only corpora.\n\n"
            "Embed images (default off): go further and make figures "
            "searchable by sending each image plus its caption to the "
            "embedder. This needs a multimodal embedder (Voyage today). With "
            "it off, figures are saved but not searchable, even if extract "
            "figures is on.\n\n"
            "Min figure bytes (default 2048): a size filter that drops tiny "
            "decorative pictures (page banners, logos, icons), usually a few "
            "hundred bytes, while keeping real diagrams. Set to 0 to keep "
            "every picture. It applies only when extract figures is on.\n\n"
            "Indexing.\n\n"
            "Optimize indexes per ingest (default on): rebuild the search "
            "indexes after each document so search stays current. Turn it off "
            "during a big bulk ingest to defer the rebuild, then rebuild once "
            "at the end.\n\n"
            "min_rows_for_vector_index (read-only, shown below): a corpus "
            "needs about this many chunks before the vector index is built; "
            "below it, search simply scans everything, which is fast at small "
            "scale. Not something you tune.\n\n"
            "The embedding model for this corpus has its own (i) in the "
            "Embedding model sub-section below."
        ),
        beginner=(
            "This section is about how the app breaks each document into "
            "small pieces (chunks) so it can search inside it, plus a few "
            "options for reading images and pictures. The chunks part is the "
            "heart of search, so it stays on: without it, a document cannot "
            "be searched at all. Most people can leave everything here at its "
            "defaults.\n\n"
            "How documents are split.\n\n"
            "Chunker strategy: how the text is cut up. The default (hybrid) "
            "follows the document's structure and also keeps pieces from "
            "getting too long. The other option (hierarchical) follows only "
            "the structure and can make very large pieces. The default is "
            "fine for most.\n\n"
            "Chunk max tokens: how big each piece can get (default 512). "
            "Bigger pieces hold more context but there are fewer of them.\n\n"
            "Merge peers: whether small neighbouring pieces get joined into "
            "bigger ones (default on).\n\n"
            "If you change how documents are split, only documents you add "
            "afterwards use the new setting; ones already added keep their "
            "old pieces until you add them again (re-ingest).\n\n"
            "Reading images.\n\n"
            "Enable pdf ocr (default off): turn on to read text from scanned "
            "PDFs (ones that are pictures of pages). Normal PDFs do not need "
            "it.\n\n"
            "Enable image ocr (default on): reads text out of image files "
            "like PNG or JPG. Leave on.\n\n"
            "Images scale (default 1.0): how sharply images are rendered "
            "before reading them. Higher is clearer but slower; it affects "
            "reading text from images and the sharpness of any extracted "
            "figures.\n\n"
            "Pictures and figures (all off by default, advanced).\n\n"
            "Extract figures: save the figures out of PDF, Word, and "
            "PowerPoint files.\n\n"
            "Embed images: also make those figures searchable (needs the "
            "Voyage embedder). Off means figures are saved but not "
            "searchable.\n\n"
            "Min figure bytes: skips tiny junk images like logos and icons; "
            "set to 0 to keep everything.\n\n"
            "Keeping search up to date.\n\n"
            "Optimize indexes per ingest (default on): refreshes the search "
            "index after each document. You can turn it off while adding a "
            "big batch to make it faster, then let it refresh once at the "
            "end.\n\n"
            "min_rows_for_vector_index (read-only): a technical number about "
            "when the fast search index kicks in. You do not need to change "
            "it."
        ),
        technical=(
            "Chunking, OCR, and figures are per-corpus CorpusConfig fields; "
            "the header toggles layers.chunks (default True; a validator "
            "requires chunks when entities is on). Controls reflect from cfg "
            "and write via per-field handlers plus a bool-checkbox map.\n\n"
            "chunker_strategy (hybrid|hierarchical, default hybrid): docling "
            "HybridChunker vs HierarchicalChunker. chunk_max_tokens (int, "
            "default 512, ge=64) and merge_peers (bool, default True) are "
            "passed only to HybridChunker and are IGNORED under hierarchical. "
            "Changing strategy or chunk_max_tokens does not touch existing "
            "chunks (there is no reconcile for L5); only a full re-ingest "
            "re-chunks. backfill_chunks and re_embed read existing chunk text "
            "out of LanceDB and never re-parse, so they do NOT change chunk "
            "shapes.\n\n"
            "enable_pdf_ocr (default False; PDFs are usually born-digital), "
            "enable_image_ocr (default True; standalone PNG/JPG/TIFF), "
            "images_scale (float, default 1.0, gt=0): docling render scale "
            "for PDF and image inputs; affects OCR fidelity AND "
            "extracted-figure fidelity.\n\n"
            "extract_figures (default False): save figure PNGs to "
            "<corpus_root>/figures/<doc_id>/<i>.png. PDF figures are rendered "
            "from the page (docling generate_picture_images, set here); Word "
            "and PowerPoint embed their images, so docling's backends attach "
            "them directly with no flag. The save loop calls "
            "picture.get_image(doc) and keeps whatever it returns, so all "
            "three formats produce figures; standalone image files are "
            "referenced in place regardless. embed_images (default False): "
            "emit content_type='figure' chunks and send image plus caption to "
            "the multimodal embedder (Voyage); off means no figure chunks "
            "even if extract_figures is on. min_figure_bytes (int, default "
            "2048, ge=0): drop saved PNGs below this many bytes (filters "
            "roughly 400 to 1000 byte decorative art), 0 disables the filter, "
            "applies only when extract_figures is on.\n\n"
            "optimize_indexes_per_ingest (default True): ingest_document "
            "calls LanceClient.ensure_indexes() after each successful LanceDB "
            "write; off defers it. min_rows_for_vector_index (Settings "
            "global, default 256, ge=1, read-only via _read_global): LanceDB "
            "IVF_PQ needs about this many rows to train; below it a "
            "brute-force scan is used; the FTS index has no threshold and is "
            "always created.\n\n"
            "The embedder (provider + model + dimension) is per-corpus, "
            "pinned at LanceDB table creation, and has its own (i) in the "
            "Embedding model sub-section; changing it after ingest is "
            "destructive (needs a Re-embed)."
        ),
    ),
    "ingest.entities": InfoText(
        title="Entities (L6)",
        standard=(
            "This section extracts named entities (genes, diseases, chemicals, "
            "people, organisations, places, and so on) from each chunk and "
            "stores them in the knowledge graph. It requires the chunks layer, "
            "since entities are found in chunk text. The entities layer is the "
            "base for the graph layers above it: ontology linking (L7), triples "
            "(L8), and both cross-doc layers (L9, L10) all require it. It is "
            "token-costly when the LLM extractor is used, so it is off by "
            "default.\n\n"
            "Extractors.\n\n"
            "You pick one or more of four extractors and put them in priority "
            "order. They run as a union: the top one owns any span that two of "
            "them both find, and each lower one adds only the spans the ones "
            "above it missed, so you never get duplicate entities for the same "
            "text. Each extractor is a full pass over every chunk, so runtime "
            "(and LLM cost, if the LLM is in the list) grows with each one "
            "added; a third rarely adds much new coverage. One extractor is the "
            "common case. When you combine a domain NER with the LLM, the useful "
            "order is the NER first (its precise domain types own the overlaps) "
            "then the LLM last (it fills the gaps the NER's fixed vocabulary "
            "cannot reach).\n\n"
            "The four extractors: LLM categorises entities with your configured "
            "language model (bundled, no download, but it makes one API call per "
            "chunk and is non-deterministic). GLiNER is a local, downloaded, "
            "multilingual model that finds whatever label types you ask for. "
            "GLiNER-BioMed is the same kind of model tuned for biomedical text. "
            "HunFlair2 is a local biomedical model with a fixed set of types. "
            "The three local models must first be installed and their weights "
            "downloaded on the Installs tab; until then their rows are greyed "
            "out.\n\n"
            "Types to extract (shared by the selected extractors).\n\n"
            "A comma-separated list of the entity types you care about (for "
            "example GENE, DISEASE, CHEMICAL). Leaving it empty means each "
            "extractor uses its own default: the LLM categorises freely (open "
            "vocabulary), GLiNER falls back to PERSON, ORGANIZATION, LOCATION, "
            "EVENT, DATE, MISC, and GLiNER-BioMed falls back to DISEASE, "
            "CHEMICAL, GENE, PROTEIN, SPECIES, CELL_LINE, CELL_TYPE, ANATOMY. "
            "HunFlair2 always emits its own fixed set (DISEASE, CHEMICAL, GENE, "
            "SPECIES, CELL_LINE) and ignores this field, so when HunFlair2 is "
            "the only extractor the field is greyed out.\n\n"
            "Replace or add (shown only when a GLiNER extractor is selected): "
            "Replace (default) uses your typed list alone; Add merges your typed "
            "list with that model's default labels, so you can extend the "
            "defaults with an extra type without re-listing them all. It only "
            "affects the GLiNER models (the ones that have defaults); the LLM "
            "has no fixed defaults and HunFlair2 ignores types entirely.\n\n"
            "LLM extractor settings (shown when the LLM extractor is "
            "selected).\n\n"
            "Model (default Haiku): the language model used for LLM extraction, "
            "per corpus. Haiku is cheap and fast, which suits one short "
            "structured call per chunk; pick a stronger model for a corpus that "
            "needs higher-quality extraction. Temperature (default 0.0): 0.0 is "
            "deterministic, which works best for the structured output this task "
            "returns; the normal range is 0.0 to a few tenths. Some models (for "
            "example Opus 4.8) ignore temperature, and the slider greys out for "
            "them.\n\n"
            "Changing the extractors, types, or model does not alter documents "
            "already ingested until you re-ingest them or run Backfill entities, "
            "which re-extracts L6 from the existing chunk text without "
            "re-parsing."
        ),
        beginner=(
            "An entity is a named thing in the text: a gene, a disease, a "
            "chemical, a person, a company, a place, and so on. This section "
            "finds those names in each document and records them in the "
            "knowledge graph, which is what lets the app answer questions about "
            "how things connect. It needs the chunks layer on, and it is the "
            "foundation for the graph layers below it, so those stay off until "
            "this is on. It is off by default because, with the AI extractor, it "
            "can cost API usage.\n\n"
            "The extractor is the tool that finds the entities. You can pick "
            "more than one and order them; the app runs them together and avoids "
            "duplicates. Most people use one. There are four: LLM uses your AI "
            "model (no download, but it makes a paid call per chunk); GLiNER and "
            "GLiNER-BioMed are free local models you download once (general and "
            "biomedical); HunFlair2 is another local biomedical model. The three "
            "local ones have to be installed on the Installs tab first, or their "
            "rows stay greyed out.\n\n"
            "Types to extract: a comma-separated list of the kinds of entity you "
            "want (like GENE, DISEASE, CHEMICAL). Leave it empty to let each "
            "extractor use its own sensible defaults. HunFlair2 always uses its "
            "own fixed list and ignores this box.\n\n"
            "Replace or add (only appears with a GLiNER model): whether your "
            "typed list replaces that model's built-in list or adds to it.\n\n"
            "If you picked the LLM, two more settings appear: which model to use "
            "(the default, Haiku, is the cheap fast one) and a temperature "
            "(leave it at 0). Changing any of these only affects documents you "
            "add afterwards, unless you run Backfill entities to redo the "
            "existing ones."
        ),
        technical=(
            "The header toggles layers.entities (default False). CorpusConfig "
            "validators require layers.chunks whenever entities is on and refuse "
            "layers.entities=true with no [entities] section; enabling the layer "
            "in the UI seeds an EntityConfig from the current selection (default "
            "extractors=['llm']). L7 (ontology_*), L8, L9, and L10 all "
            "dependency-require layers.entities.\n\n"
            "EntityConfig.extractors is an ordered list (min_length 1) of "
            "adapter names from entity_extractors/. extract_union runs the "
            "selected adapters concurrently per chunk and merges by priority: "
            "index 0 owns a normalized span, each later adapter contributes only "
            "unclaimed spans, and every adapter that found a span is recorded on "
            "Mention.sources. The pipeline pre-flight calls validate_entity_types "
            "and is_extractor_ready for every selected adapter and raises at "
            "kickoff (WeightsNotDownloadedError) rather than mid-run; the UI "
            "greys out unready rows (pip package plus pinned weights). "
            "validate_entity_types is currently a no-op for all four adapters "
            "(each declares KNOWN_LABELS=None).\n\n"
            "EntityConfig.entity_types (list[str], default []) and "
            "entity_types_mode (replace|add, default replace) are shared across "
            "the selected adapters; effective_entity_types applies the mode per "
            "adapter. Empty list = each adapter's own fallback. mode=add merges "
            "the typed list with that adapter's DEFAULT_LABELS (order-preserving "
            "dedup); adapters with no DEFAULT_LABELS (llm, hunflair2) treat add "
            "as replace, so the Replace/Add radio is shown only when a GLiNER is "
            "selected, and the entity_types field is disabled for a "
            "HunFlair2-only selection.\n\n"
            "Adapters: llm (bundled; KNOWN_LABELS=None open vocabulary; one "
            "structured-output LLM call per chunk; offset and confidence always "
            "None). gliner (urchade/gliner_multi-v2.1; DEFAULT_LABELS PERSON, "
            "ORGANIZATION, LOCATION, EVENT, DATE, MISC; score threshold 0.5). "
            "gliner_biomed (Ihor/gliner-biomed-bi-large-v1.0; DEFAULT_LABELS "
            "DISEASE, CHEMICAL, GENE, PROTEIN, SPECIES, CELL_LINE, CELL_TYPE, "
            "ANATOMY; threshold 0.5). hunflair2 (hunflair/hunflair2-ner; fixed "
            "DISEASE, CHEMICAL, GENE, SPECIES, CELL_LINE; extract() ignores its "
            "entity_types argument).\n\n"
            "entity_extractor_model (per-corpus, default "
            "PROVIDER_NODE_DEFAULTS[provider]['entity_extractor'], "
            "claude-haiku-4-5 for Anthropic) and entity_extractor_temperature "
            "(float, default 0.0, ge=0 le=1) are forwarded via llm_kwargs to the "
            "llm adapter only, whenever 'llm' is anywhere in extractors (not just "
            "index 0). The temperature slider greys out where "
            "supports_temperature is false. Changing extractors, entity_types, "
            "or the model does not touch existing docs; re-ingest or "
            "backfill_entities re-extracts L6 from existing chunk text (no "
            "re-parse) and re-links L7."
        ),
    ),
    "ingest.ontology_linking": InfoText(
        title="Ontology linking (L7)",
        standard=(
            "This section canonicalises the entities from L6 against curated "
            "reference vocabularies (ontologies), so that different ways of "
            "naming the same thing resolve to one shared identity. When an "
            "ontology is enabled, its terms are imported into the knowledge "
            "graph and each matching entity is linked to its canonical term "
            "(for example the entities 'heart attack' and 'myocardial "
            "infarction' can both link to the same disease term). This lets the "
            "graph answer by concept rather than by exact wording, and it is "
            "the base for concept-level cross-document links (L10). It requires "
            "the entities layer.\n\n"
            "Enabling an ontology takes two steps. First you download the "
            "ontology's source file on the Installs tab; the app never "
            "downloads it during ingest, so an ontology that is not downloaded "
            "is greyed out here and cannot be ticked. Second you tick it in the "
            "list below to link this corpus against it. The first ingest after "
            "you enable it imports that whole ontology into the graph once "
            "(this can take a while for large vocabularies); later ingests "
            "reuse the import and just link their own new entities. There are 18 "
            "ontologies; most are biomedical, with a few for other domains "
            "(food, environment, finance). Each row's tooltip says what that "
            "ontology covers.\n\n"
            "Matching (per ontology: exact or fuzzy). exact (default) links an "
            "entity only when its text exactly matches a term's label or one of "
            "its synonyms (compared in lower case). fuzzy tries exact first, "
            "then also tries a few simple variants of the wording: singular "
            "against plural (a trailing s) and swapping hyphens for spaces. It "
            "is not general spell-correction. Start with exact; if it leaves too "
            "many entities unlinked, switch that ontology to fuzzy and re-run "
            "the linking pass, at some risk of wrong matches.\n\n"
            "Cross-ontology equivalence edges (xrefs). Ontologies declare when "
            "one of their concepts equals a concept in another ontology (the "
            "same drug in MeSH and ChEBI, the same disease in MONDO and DOID). "
            "This three-way control decides what happens with those "
            "declarations. Do not materialize (default) ignores them. Collect "
            "only extracts them and stores the raw list on each term but writes "
            "no edges yet, so you can turn the edges on later with the Backfill "
            "xrefs operation without re-importing. Materialize edges now also "
            "writes the equivalence edges between term nodes at import, for "
            "every target ontology already imported. Storage is small (tens of "
            "megabytes across the biomedical set). Two things to know: raising "
            "this setting does not retro-apply to an ontology that is already "
            "imported (the import is skipped when it is already present), so set "
            "it before the first import or re-import that ontology; and the "
            "cross-doc xrefs layer (L10) requires this be set to Materialize "
            "edges now."
        ),
        beginner=(
            "An ontology is a big curated dictionary for a subject: its "
            "official terms, their synonyms, and how they relate. This section "
            "matches the entities found in L6 to those official terms, so that "
            "two ways of saying the same thing (like 'heart attack' and "
            "'myocardial infarction') get tied to one shared entry. That lets "
            "the app answer by meaning, not just by exact words. It needs the "
            "entities layer on.\n\n"
            "To use an ontology you first download it on the Installs tab (the "
            "app will not download it for you during ingest), then tick it in "
            "the list here. Ones you have not downloaded are greyed out. The "
            "first time you ingest after ticking one, the app loads that whole "
            "ontology into the graph once, which can take a little while; after "
            "that it is quick. There are 18 to choose from, mostly medical with "
            "a few others (food, environment, finance); hover a row to see what "
            "it covers.\n\n"
            "Matching (next to each ontology): exact means only link when the "
            "wording matches a real term or synonym; fuzzy also tries a few "
            "simple variants (singular or plural, and hyphens versus spaces). It "
            "is not spell-check. Start with exact and switch a stubborn one to "
            "fuzzy if too few things link.\n\n"
            "Cross-ontology equivalence edges (xrefs) is an advanced option "
            "about linking equal concepts across two different ontologies. "
            "Leave it off unless you specifically want those links (they are "
            "also required for the cross-doc xrefs layer, L10). The middle "
            "choice collects the information now so you can switch the links on "
            "later without redoing the import."
        ),
        technical=(
            "The header toggles the 18 layers.ontology_<name> booleans (all "
            "default False). Each requires layers.entities (editor "
            "_dependency_error_for plus the _check_ontology_requires_entities "
            "validator); the linking step needs :Entity nodes to link to. "
            "Enabling is gated on the source file being downloaded: the editor "
            "hard-disables (not just greys) any ontology whose "
            "is_ontology_downloaded probe is False, because ingest never "
            "auto-downloads (that is a Library, Installs action).\n\n"
            "Per ingest, for each enabled ontology the pipeline calls "
            "ensure_ontology_imported(name, xrefs_mode=layers.xrefs) then "
            "link_entities_to_ontology(name, OntologyConfig.matching, "
            "doc_id=...). ensure_ontology_imported is idempotent and returns "
            "was_already_imported; on the run that actually imports, linking "
            "runs globally (doc_id=None, over every existing entity), and on "
            "later runs it links only the current doc. Import writes the "
            "ontology's term nodes plus its hierarchy edges (for example "
            ":MeSHTerm and :MESH_BROADER); linking writes :CANONICAL_TO edges "
            "from :Entity to the term.\n\n"
            "OntologyConfig.matching (exact|fuzzy, default exact) is per "
            "ontology (the [ontology.<name>] section, auto-populated when the "
            "layer is on). exact matches label or synonym equality, lowercased. "
            "fuzzy adds a small fixed set of string variants as a fallback: a "
            "trailing-s plural/singular flip, a hyphen/space swap, and the two "
            "combined (heuristic confidences 0.9/0.85/0.75). It is variant "
            "lookup against the term index, not edit-distance or "
            "spell-correction. The matching dropdown stores its value even while "
            "the layer is off.\n\n"
            "layers.xrefs (none|collect_only|use, default none) is forwarded as "
            "xrefs_mode into the import. none writes nothing. collect_only "
            "stores the verbatim declarations as a dangling_xrefs property on "
            "each source term but writes no edges (flip to use later via the "
            "backfill_xrefs bulk op, no re-import). use additionally writes "
            "resolved :<X>_XREF edges for targets that already exist; unresolved "
            "targets stay in dangling_xrefs until backfill. The idempotent "
            "import short-circuit does NOT re-apply xref logic to an "
            "already-imported ontology, so raising xrefs after import needs "
            "backfill_xrefs (only helps if the data was collected) or a force "
            "re-import. layers.cross_doc_xrefs (L10) requires xrefs=use (the "
            "_check_cross_doc_xrefs_requires_entities_and_xrefs_use validator)."
        ),
    ),
    "ingest.triples": InfoText(
        title="Triples (L8)",
        standard=(
            "This section extracts typed relationships between the entities "
            "that L6 found, so the graph captures not just which things are "
            "mentioned but how they relate: 'X inhibits Y', 'drug A treats "
            "disease B', 'gene G is expressed in tissue T'. For every chunk "
            "that contains at least one entity, one LLM call reads the chunk "
            "and proposes (subject, predicate, object) triples drawn only from "
            "that chunk's own entities. It requires the entities layer, and it "
            "is token-costly (one call per chunk that has entities), so it is "
            "off by default.\n\n"
            "The predicate set is fixed in code: 15 types (INHIBITS, ACTIVATES, "
            "REGULATES, INTERACTS_WITH, BINDS_TO, IS_PART_OF, IS_A, CAUSES, "
            "ASSOCIATED_WITH, TREATS, MEASURED_BY, EXPRESSED_IN, LOCATED_IN, "
            "USES, COMPARED_WITH). Keeping the list closed is deliberate: it "
            "stops the graph fragmenting into near-duplicate relations (EDITS, "
            "MODIFIES, CHANGES) that would each need a separate query. The "
            "predicate becomes the edge type in the graph, so a stored relation "
            "is a direct '(entity) INHIBITS (entity)' edge. Subject and object "
            "must both be entities L6 already found in that same chunk; the "
            "model is told to use only those and to pick one of the 15 "
            "predicates, and anything it invents outside those is dropped. Each "
            "edge also stores a short verbatim evidence snippet from the chunk, "
            "so the app can cite the sentence that supports the relation.\n\n"
            "Direction and repetition matter: 'A inhibits B' is a different edge "
            "from 'B inhibits A', and if the same relation appears in five "
            "chunks it is stored as five edges, each with its own evidence.\n\n"
            "Model (default Haiku): the language model used for this "
            "extraction, per corpus. Haiku is cheap and fast and suits the "
            "short, constrained task; bump to a stronger model for a dense "
            "corpus where the model must disambiguate many candidate relations. "
            "Temperature (default 0.0): 0.0 is deterministic, which the "
            "constrained structured output benefits from; the normal range is "
            "0.0 to a few tenths. Some models (for example Opus 4.8) ignore "
            "temperature and the slider greys out for them.\n\n"
            "Changing the model or temperature does not alter documents already "
            "ingested until you re-ingest them or run Backfill triples, which "
            "re-extracts L8 from the existing chunk text and the existing L6 "
            "entities, without re-parsing or re-running entity extraction."
        ),
        beginner=(
            "This section looks at the entities L6 found and works out how they "
            "are related to each other, then records those relationships in the "
            "knowledge graph. For example, it can capture that one drug treats a "
            "disease, or that one thing causes another. For each chunk that has "
            "entities, it asks the AI to read the text and list relationships "
            "between them. It needs the entities layer on, and because it makes "
            "an AI call for every chunk with entities, it can cost API usage, so "
            "it is off by default.\n\n"
            "The kinds of relationship are a fixed list of 15 (things like "
            "INHIBITS, CAUSES, TREATS, IS_A, USES). The app only keeps "
            "relationships between entities that are actually in the same chunk, "
            "and only from that fixed list; anything else the AI suggests is "
            "thrown away. Each stored relationship also keeps the exact sentence "
            "it came from, so the app can show you the evidence.\n\n"
            "Model (default Haiku): which AI model does the work. The default is "
            "the cheap fast one; switch to a stronger one if your text is dense "
            "and the relationships are hard to pin down. Temperature (leave at "
            "0): keeps the output stable.\n\n"
            "Changing the model or temperature only affects documents you add "
            "afterwards, unless you run Backfill triples to redo the existing "
            "ones (it reuses the entities already found, so it does not re-read "
            "the original files)."
        ),
        technical=(
            "The header toggles layers.triples (default False). It requires "
            "layers.entities (editor _dependency_error_for plus the "
            "_check_triples_requires_entities validator) and only runs when "
            "kg_entities_ok. Per chunk that has L6 mentions, the pipeline builds "
            "entity_vocab = [(raw_text.lower(), entity_type)] and calls "
            "triples_extractor.extract(text, entity_vocab, "
            "model=config.triples_extractor_model, "
            "temperature=config.triples_extractor_temperature) under the "
            "settings.pipeline_max_concurrent_chunks semaphore; chunks with no "
            "entities are skipped without an LLM call.\n\n"
            "One structured-output LLM call per chunk. The prompt hands the "
            "model the chunk's entity keys and the 15 predicates "
            "(schema.TRIPLE_PREDICATE_RELS) and tells it to use only those. "
            "_filter_and_convert_triples drops any triple whose subject_key or "
            "object_key is not in the chunk vocab or whose predicate is not one "
            "of the 15; write_triples drops out-of-vocab predicates again "
            "defensively.\n\n"
            "The 15 predicates are INHIBITS, ACTIVATES, REGULATES, "
            "INTERACTS_WITH, BINDS_TO, IS_PART_OF, IS_A, CAUSES, "
            "ASSOCIATED_WITH, TREATS, MEASURED_BY, EXPRESSED_IN, LOCATED_IN, "
            "USES, COMPARED_WITH. Each is written as the relationship TYPE: "
            "(:Entity)-[:PREDICATE {chunk_id, doc_id, evidence_span}]->(:Entity), "
            "matched on the composite (key, entity_type). CREATE not MERGE (one "
            "edge per chunk assertion; the same triple across five chunks is "
            "five edges); delete_triples_by_doc_id wipes the doc's edges across "
            "all 15 types before each re-write so re-ingest leaves nothing "
            "stale.\n\n"
            "triples_extractor_model (per-corpus, default "
            "PROVIDER_NODE_DEFAULTS[provider]['triples_extractor'], "
            "claude-haiku-4-5 for Anthropic) and triples_extractor_temperature "
            "(float, default 0.0, ge=0 le=1) are the only knobs; both are "
            "disabled in the editor when the layer is off, and the temperature "
            "slider greys out where supports_temperature is false. Changing them "
            "does not touch existing docs; re-ingest or backfill_triples "
            "re-extracts L8 from existing chunk text and the existing L6 "
            "entities (get_entities_by_chunk), with no re-parse and no re-run of "
            "L6."
        ),
    ),
    "ingest.cross_doc": InfoText(
        title="Cross-doc (L9)",
        standard=(
            "This section links whole documents to each other when they share "
            "enough of the same entities, so you can see at a glance that two "
            "documents are about overlapping things without having to search "
            "for it. When it is on, each document gets a RELATED_TO link to "
            "every other document (or artifact) that shares at least a set "
            "number of the entities L6 found in both. It requires the entities "
            "layer, and it is off by default.\n\n"
            "The link uses the raw L6 entities, not the ontology canonicals, so "
            "it works whether or not you run ontology linking and it picks up "
            "your own wording. It also spans types: a dataset or other artifact "
            "can be linked to a paper, not just paper to paper. Each link is "
            "stored once between the pair, and records which entities were "
            "shared, how many, and when it was computed, so the app can show "
            "why two documents were linked.\n\n"
            "Threshold (minimum shared entities, default 2). This is how many "
            "distinct entities two documents must share before they are linked. "
            "1 is the noise floor: a single shared common term (like 'method' "
            "or 'patient') would link almost everything, so 2 is the default "
            "(two shared entities is a real signal). Raise it to 3 to 5 to "
            "tighten the graph into fewer, stronger links; lower it to 1 only "
            "for a very small corpus where every overlap matters.\n\n"
            "There is no model or temperature here: L9 is a pure graph "
            "computation, no LLM and no API cost. It is recomputed for a "
            "document every time you ingest it, and you can rebuild it across "
            "the corpus with Backfill cross-doc (for example after changing the "
            "threshold). Because the links hang off the document itself, "
            "deleting a document also removes its links automatically."
        ),
        beginner=(
            "This section connects whole documents that are about similar "
            "things. When two documents mention enough of the same entities "
            "(the names L6 pulled out), the app draws a link between them, so "
            "you can see they are related without searching. It needs the "
            "entities layer on, and it is off by default.\n\n"
            "It can also link across types, for example connecting a dataset to "
            "a paper. Each link remembers which entities the two shared and how "
            "many, so you can see why they were connected.\n\n"
            "Threshold (default 2): how many shared entities it takes to draw a "
            "link. 1 would link almost everything (one common word is enough), "
            "so 2 is the sensible default. Turn it up (3 to 5) if you want "
            "fewer, stronger links; turn it down to 1 only for a very small "
            "collection.\n\n"
            "There is no AI model here and no cost: it is just the app "
            "comparing entities. It updates a document's links each time you add "
            "it, and you can rebuild them all with Backfill cross-doc."
        ),
        technical=(
            "The header toggles layers.cross_doc (default False). It requires "
            "layers.entities (editor _dependency_error_for plus the "
            "_check_cross_doc_requires_entities validator). It runs when "
            "kg_entities_ok, concurrently with L10 via asyncio.gather.\n\n"
            "recompute_cross_doc_edges(doc_id, threshold) is the only write "
            "primitive. Per ingest it wipes every :RELATED_TO edge incident to "
            "the doc, then walks focal <-[:PART_OF]- :Chunk -[:MENTIONS]-> "
            ":Entity <-[:MENTIONS]- :Chunk -[:PART_OF]-> other_focal, collects "
            "distinct shared e.key per (this, other) pair, keeps pairs with "
            "size(shared) >= threshold, and MERGEs an undirected "
            "(d)-[:RELATED_TO]-(other) with shared_entities, shared_count, and "
            "computed_at. Both sides match :Document|Artifact, so artifacts "
            "participate on equal footing with documents.\n\n"
            "The link signal is raw L6 :Entity nodes, NOT ontology canonicals, "
            "so L9 has no ontology dependency and canonical churn does not "
            "invalidate its edges. Edges are incident to the focal node, so "
            "delete_doc removes them via the focal's DETACH DELETE; this is why "
            "L9, unlike L8, has no separate delete primitive.\n\n"
            "CrossDocConfig.threshold (int, default 2, ge=1) is the only knob "
            "(the [cross_doc] section, auto-populated when the layer is on). The "
            "editor field clamps non-positive input to 1 and reverts "
            "non-integers silently; recompute_cross_doc_edges also raises for "
            "threshold < 1. There is no LLM: L9 is a pure Cypher pass. Changing "
            "the threshold applies on the next ingest of a doc or via "
            "backfill_cross_doc across the corpus."
        ),
    ),
    "ingest.cross_doc_xrefs": InfoText(
        title="Cross-doc xrefs (L10)",
        standard=(
            "This section links whole documents that talk about the same "
            "concepts, even when they use different vocabulary for them. It is "
            "the concept-level companion to Cross-doc (L9). Where L9 links "
            "documents that share the same entity words, L10 links documents "
            "whose entities point to the same underlying concept in a reference "
            "vocabulary, including across two different ontologies (for example "
            "the same drug named under MeSH in one document and under ChEBI in "
            "another). When it is on, each document gets a RELATED_BY_XREF link "
            "to every other document (or artifact) that shares at least a set "
            "number of these canonical concepts.\n\n"
            "It has two prerequisites: the entities layer, and the ontology "
            "xrefs setting must be on 'Materialize edges now' (the 'use' mode "
            "in Ontology linking). That is because L10 rides on the equivalence "
            "edges between ontology terms that the xrefs setting writes; without "
            "them there is nothing to join equal concepts across ontologies. "
            "The editor will not let you turn L10 on until both are set.\n\n"
            "L9 and L10 are complementary, not either/or. L9 is robust and "
            "catches your own terminology even without ontologies; L10 is "
            "invariant to wording and surfaces cross-ontology equivalence, but "
            "only sees concepts that were actually linked to an ontology. You "
            "can run both; they write separate links.\n\n"
            "Threshold (minimum shared concepts, default 2). How many distinct "
            "concepts two documents must share before they are linked. 1 is the "
            "noise floor (a single high-level concept like 'Disease' shared via "
            "a MeSH to MONDO equivalence would link almost everything), so 2 is "
            "the default. Raise it to tighten the graph into fewer, stronger "
            "links.\n\n"
            "There is no model or temperature here: L10 is a pure graph "
            "computation, no LLM and no API cost. Each link records which "
            "concepts were shared, how many, and when. It is recomputed for a "
            "document every time you ingest it, and you can rebuild it across "
            "the whole corpus with Backfill xrefs or Recompute cross-doc xrefs "
            "(useful after importing a new ontology that resolves previously "
            "unmatched equivalences). Deleting a document removes its links "
            "automatically."
        ),
        beginner=(
            "This section connects whole documents that are about the same "
            "ideas, even when they call them by different names. It is a smarter "
            "version of Cross-doc (L9): instead of matching the exact entity "
            "words, it matches the underlying concepts from the reference "
            "vocabularies (ontologies), including the same concept named "
            "differently in two different ontologies.\n\n"
            "For it to work you need two things on: the entities layer, and the "
            "ontology xrefs option set to 'Materialize edges now' (in the "
            "Ontology linking section). Those xref links are what let it tell "
            "that two differently-named terms mean the same thing. The app will "
            "not let you switch this on until both are set.\n\n"
            "Threshold (default 2): how many shared concepts it takes to draw a "
            "link. 1 would connect almost everything, so 2 is the sensible "
            "default. Turn it up for fewer, stronger links.\n\n"
            "There is no AI model here and no cost: it is just the app "
            "comparing concepts. Each link remembers which concepts the two "
            "shared. It updates a document's links whenever you add it, and you "
            "can rebuild them all with Backfill xrefs (handy after adding a new "
            "ontology)."
        ),
        technical=(
            "The header toggles layers.cross_doc_xrefs (default False). It "
            "requires layers.entities=true AND layers.xrefs='use', enforced in "
            "the editor (_dependency_error_for returns a distinct error for "
            "each) and by the "
            "_check_cross_doc_xrefs_requires_entities_and_xrefs_use validator. "
            "It runs when kg_entities_ok, concurrently with L9 via "
            "asyncio.gather.\n\n"
            "recompute_cross_doc_xrefs_edges(doc_id, threshold) wipes every "
            ":RELATED_BY_XREF edge incident to the doc, then walks this "
            "<-[:PART_OF]- :Chunk -[:MENTIONS]-> :Entity -[:CANONICAL_TO]-> "
            "(t1:OntologyTerm) and the same for other/t2, joins with WHERE t1 = "
            "t2 OR EXISTS a :<xref>_XREF edge between them (a pipe-union of the "
            "18 ONTOLOGY_XREF_RELS types, not a suffix scan), collects DISTINCT "
            "t1.id AS shared_concepts on the focal side, keeps pairs with size "
            ">= threshold, and MERGEs an undirected "
            "(this)-[:RELATED_BY_XREF]-(other) with shared_concepts, "
            "shared_count, computed_at. Both sides match :Document|Artifact.\n\n"
            "L10 is parallel to L9, not a replacement: L9 links on raw :Entity "
            "keys (robust, no ontology needed), L10 links on canonical "
            ":OntologyTerm concepts via the xref graph that L7's 'use' mode "
            "materialises (invariant to label variation, catches the same "
            "concept across two ontologies). Both edges can coexist. Because "
            "the equivalence rides on xref edges, flipping ontology xrefs to "
            "'use' after an ontology was already imported does not retro-add "
            "them (see the Ontology linking section); backfill_xrefs plus a "
            "graph-wide L10 rebuild is the way to catch up.\n\n"
            "CrossDocXrefsConfig.threshold (int, default 2, ge=1) is the only "
            "knob (the [cross_doc_xrefs] section, auto-populated when the layer "
            "is on). The editor field clamps non-positive input to 1 and "
            "reverts non-integers silently; recompute raises for threshold < 1. "
            "No LLM: pure Cypher. Per-doc recompute runs on ingest and "
            "backfill_cross_doc_xrefs; recompute_cross_doc_xrefs_global rebuilds "
            "the whole graph (WHERE id(this) < id(other) to dedupe pairs) and "
            "backs the backfill_xrefs and recompute_cross_doc_xrefs bulk ops. "
            "delete_doc removes incident edges via the focal DETACH DELETE (no "
            "separate L10 delete primitive)."
        ),
    ),
    "ingest.labels_section": InfoText(
        title="Labels and sub-labels",
        standard=(
            "These pick the type label applied to documents in your next ingest. "
            "They're per-run settings — not saved to the corpus — so they apply to "
            "whichever action you run next (Ingest folder, Re-ingest, Sync, or single "
            "file). They label NEW documents; for a document already in the corpus, "
            "whether they change its label depends on the Overwrite box below.\n\n"
            "Main label is the broad kind: Document (papers, notes, reports…) or "
            "Artifact (datasets, code, figures, audio…). Sub-label is an optional "
            "finer type under it — Paper, Note, Report… under Document; Dataset, Code, "
            "Figure… under Artifact — or leave it (none). The sub-label list shows the "
            "types your corpus allows that are also valid for the chosen main label, "
            "so changing the main label changes the choices (and a list that shows "
            "only (none) usually means none of the allowed types fit the current main "
            "label — try switching the main label — not necessarily that the corpus "
            "forbids sub-labels).\n\n"
            "One sub-label matters beyond description: Paper. OpenAlex enrichment "
            "(the L1-L4 authors/citations/venue/topics) needs two things together: the "
            "document must be labelled Paper, and the corpus must have the "
            "paper-metadata layer on (the Resolve paper metadata section below). A "
            "non-Paper document (including the default (none)) is never looked up even "
            "with the layer on, and a Paper is not looked up when the layer is off. So "
            "if you want OpenAlex, label it Paper and turn that layer on. Otherwise the "
            "sub-label is a pure tag: it doesn't change how the file is parsed or "
            "chunked, only how it's categorised and queried.\n\n"
            "Overwrite existing labels only matters for documents already in the "
            "corpus. Off (default), re-ingesting or syncing a document keeps its "
            "stored labels — so if you pick a new label and it doesn't change, that's "
            "why: tick Overwrite to force the labels above onto an already-ingested "
            "document. It never affects brand-new files, and it changes only the "
            "label, not the document's content."
        ),
        beginner=(
            "Every document you add gets a type label, and this is where you choose "
            "it. These are per-run choices — they aren't saved into the corpus; they "
            "apply to your next ingest. They set the label on NEW documents (for one "
            "already added, the Overwrite box below decides whether the label "
            "changes).\n\n"
            "Main label is the big category: Document (things you read — papers, notes, "
            "reports, articles) or Artifact (supporting material — datasets, code, "
            "figures, audio, video). Sub-label is a more specific type inside that "
            "category (like Paper or Note under Document), and it's optional — you can "
            "leave it as (none). The sub-label choices depend on your main label and on "
            "what your corpus allows, so they change when you switch the main label. If "
            "you see only (none), it usually just means none of the allowed types fit "
            "the current main label — try switching the main label.\n\n"
            "One sub-label does something special: Paper. To get a document's "
            "scholarly details looked up online (authors, the works it cites, the "
            "journal, topics, from OpenAlex), two things must both be on: you label it "
            "Paper, and the corpus has the paper look-up turned on (the Resolve paper "
            "metadata section below). Choosing Paper but leaving that look-up off means "
            "no details, and leaving a paper at (none) means no details even with the "
            "look-up on. So if you're adding research papers and want those details, "
            "choose Paper and turn that look-up on. For everything else the sub-label "
            "is just a tag; it doesn't change how the file itself is read.\n\n"
            "The Overwrite existing labels checkbox only comes into play for a document "
            "that's already in your corpus. Left off (the default), re-adding or "
            "syncing a document keeps whatever label it already had — so if you change "
            "a label and it seems to stick to the old one, tick this box to make your "
            "new choice win. For brand-new documents it makes no difference, and it "
            "only ever changes the label, never the document's contents."
        ),
        technical=(
            "Per-run ingest args read live from these three controls via "
            "get_ingest_args() at action time — not part of CorpusConfig, never "
            "written to corpus.toml (unlike the layer sections below). Main label and "
            "Overwrite persist until changed (never reassigned after construction); "
            "the SUB-label does NOT — _refresh_sub_label_options silently resets it to "
            "(none) whenever the held value isn't valid for the new option set (fires "
            "on every build(), including a corpus switch).\n\n"
            "Main label options MAIN_LABELS = (Document, Artifact), default Document. "
            "Sub-label options = the corpus's allowed_types filtered by "
            "SUB_LABEL_TO_MAIN to the chosen main, plus (none) (default) — so the "
            "dropdown shows only (none) when no allowed type is compatible with the "
            "current main (e.g. allowed_types=['Paper'] with Main=Artifact), NOT "
            "necessarily an empty allowed_types. The 14-way taxonomy is 8 Document "
            "sub-labels "
            "(Paper/Book/Note/Report/Presentation/Article/Correspondence/Transcript) + "
            "6 Artifact (Dataset/Code/Figure/Diagram/Audio/Video); allowed_types "
            "defaults to all 14, restricted only by hand-editing corpus.toml "
            "(case-sensitive — 'paper' ≠ 'Paper'; an unknown entry is rejected at "
            "corpus.toml LOAD time, not just at ingest). At ingest, ingest_document "
            "validates before any write: main must be in MAIN_LABELS (ValueError "
            "otherwise), sub must be in allowed_types AND SUB_LABEL_TO_MAIN[sub]==main "
            "(so e.g. (Document, Dataset) raises ValueError). The chosen (main, sub) "
            "become Neo4j node labels (sub is a SECOND label alongside "
            ":Document/:Artifact, so a (none) doc stays fully queryable via the "
            "top-level label) and are mirrored onto the LanceDB chunk rows.\n\n"
            "The sub-label is functionally inert except for one value: "
            "is_doi_eligible(sub_label) is true ONLY for 'Paper' "
            "(DOI_ELIGIBLE_SUB_LABELS = frozenset({PAPER_LABEL})). DOI extraction + "
            "OpenAlex L1-L4 fire only when the doc is a Paper AND layers.openalex_papers "
            "is on (the ingest gate is is_doi_eligible AND config.layers.openalex_papers): "
            "a non-Paper (incl. the default (none)) leaves work None even with the layer "
            "on, and a Paper leaves work None when the layer is off. Either way the "
            "L1-L4 writes are skipped and metadata_status stays 'baseline'; only Paper "
            "docs with the layer on reach 'enriched'/'pending'. Sub-label never affects "
            "parsing (dispatch is by file extension; labelling a file Audio/Dataset is a "
            "pure tag, it doesn't invoke transcription/structured parsing).\n\n"
            "Overwrite is preserve_existing_labels inverted (default off → preserve "
            "on). When a doc's focal node already exists, preserve keeps its stored "
            "(main, sub) and ignores the picked values; overwrite forces them. So the "
            "picked labels apply to NEW docs always but to an ALREADY-PRESENT doc only "
            "when Overwrite is on. It's a no-op for Ingest folder (add only touches "
            "new doc_ids) and first-time ingests; it bites on Re-ingest, Sync's EDITED "
            "bucket, and re-ingesting an existing file as a single file. It gates only "
            "the label swap — content, chunks, embeddings and graph are rebuilt from "
            "fresh config either way."
        ),
    ),
    "ingest.ingestion_summary": InfoText(
        title="Ingestion summary",
        standard=(
            "This previews what your next ingest will do differently: the corpus "
            "settings you've changed in the panel on the right but haven't saved "
            "yet, each shown as 'setting  current → new' (current in grey, the new "
            "value in amber), under a '{n} pending changes:' header. It updates as "
            "you edit, so it reflects your unsaved changes.\n\n"
            "When nothing's changed it reads 'No pending changes — new ingest = "
            "previous.' — your next ingest will behave like your last one. Discard "
            "(below) throws the pending changes away and restores the settings you "
            "last saved (any ingest or bulk operation saves them).\n\n"
            "It covers the persisted corpus settings only — not the per-run document "
            "labels (main/sub-label and Overwrite, in the section at the top of the "
            "same panel), and not the last ingest's results (those appear in "
            "Progress, above)."
        ),
        beginner=(
            "Before you ingest, this box tells you what's about to change. Whenever "
            "you edit a setting in the panel on the right, the change shows up here "
            "as 'setting  was → will be', so you can double-check before running an "
            "ingest. It updates as you go.\n\n"
            "If you haven't changed anything, it says 'No pending changes — new "
            "ingest = previous.', meaning your next ingest will work just like your "
            "last one. The Discard button undoes all the pending changes and puts "
            "every setting back to how it was the last time you saved — which "
            "happens whenever you ingest or run a bulk operation.\n\n"
            "A little dot (●) also appears whenever you have unsaved changes — the "
            "same changes this box lists.\n\n"
            "This is only about the corpus settings on the right. It doesn't show "
            "the document type labels (those you pick fresh each run, in the section "
            "at the top of the same panel), and it doesn't show how your last ingest "
            "went — that's the Progress line just above."
        ),
        technical=(
            "Renders config_diff(_baseline_config, _corpus_config) — one row per "
            "field where the in-memory draft differs from the baseline. The baseline "
            "is the config as last written to corpus.toml, which happens on any "
            "ingest action OR bulk op (try_save_and_get_error), and is also "
            "snapshotted on corpus load. Non-empty output is a '{n} pending "
            "change{s}:' header over one columnar Row per field — [name (width 180)  "
            "current  →  new], current in GREY_400 and new in AMBER_300 (there is no "
            "literal 'name: current → new' string). Empty → 'No pending changes — "
            "new ingest = previous.'. It re-renders on every config-editor edit and "
            "on Discard (via on_draft_changed); the Select tab shows a twin card off "
            "the same shared config_diff (its single source of truth).\n\n"
            "config_diff mirrors the ● dirty dot for every editable field: "
            "is_dirty() is a full _corpus_config != _baseline_config compare, and "
            "config_diff now diffs each ontology's matching mode "
            "(OntologyConfig.matching, exact/fuzzy) alongside the enable flags — so "
            "a matching-only change shows here too. The only field is_dirty compares "
            "that config_diff skips is allowed_types, and it isn't editable in this "
            "editor, so it can't drift. (Pending changes also survive an app restart "
            "via the session sidecar — restored with ● lit and this card "
            "populated.)\n\n"
            "Covered fields: layer flags openalex_papers / chunks / entities / "
            "triples / cross_doc / cross_doc_xrefs (on/off), xrefs "
            "(none/collect_only/use), the 18 per-ontology flags (and each ontology's "
            "matching mode), and the entities subsection's extractors + "
            "entity_types_mode + entity_types. Scalars "
            "(_SCALAR_FIELDS): chunker_strategy, chunk_max_tokens, merge_peers, "
            "enable_pdf_ocr, enable_image_ocr, images_scale, extract_figures, "
            "embed_images, min_figure_bytes, optimize_indexes_per_ingest, "
            "embedding_provider/model/dims, and entity_/triples_extractor_model + "
            "_temperature. Plus cross_doc.threshold and cross_doc_xrefs.threshold "
            "(rendered as 2 when the subsection is absent; None entities/subsections "
            "render '—', an empty entity_types renders '(default)'). Excluded: "
            "labels / sub-label / overwrite (per-run args, not on CorpusConfig); "
            "allowed_types is a persisted CorpusConfig field config_diff does NOT "
            "diff (no longer editable here, so it can't desync).\n\n"
            "Discard reverts the draft to the baseline. The ingest RESULT counts "
            "(succeeded / failed) are not here — they write to the Progress status "
            "line above."
        ),
    ),
    "ingest.bulk_operations": InfoText(
        title="Bulk operations",
        standard=(
            "Bulk operations re-run one layer of the knowledge graph or search "
            "store across the documents ALREADY in this corpus, reading from "
            "what's stored (the chunk text in LanceDB and the graph in Neo4j) — "
            "they never re-read or re-parse your original files. Each button "
            "targets the whole corpus, not a folder or a selection.\n\n"
            "Use them when you've changed a setting that only affects how a layer "
            "is built — a new embedder, a different entity extractor, an ontology "
            "or matching mode, a cross-doc threshold — and want to apply it to "
            "documents that are already ingested. A plain re-ingest only touches "
            "new files; these buttons are how existing documents pick up the "
            "change. They're also how you backfill a layer you turned on AFTER "
            "ingesting, or re-run a layer that failed the first time (e.g. the "
            "model was unreachable).\n\n"
            "They share a flow: clicking one first saves your ingestion "
            "settings (so the run uses exactly what's shown — and that save "
            "sticks even if you then cancel), shows a confirm dialog with a short "
            "summary, then runs. The buttons that rebuild a graph layer also "
            "reconcile first — before rebuilding, they remove data the rebuilt "
            "layer no longer covers if you've turned it off, across the whole "
            "corpus. So running one of those after disabling its layer deletes "
            "that layer's data everywhere, not just for new documents. (Re-embed "
            "and Resolve don't reconcile — they only add.)\n\n"
            "A few things to know: the graph ones need Neo4j running (Re-embed "
            "is the exception — it only needs the embedder and LanceDB), and the "
            "model-based ones need the relevant provider key; the heavy ones "
            "(re-extract entities/triples, rebuild from chunks) can run for "
            "minutes on a large corpus and can't be cancelled once started; and "
            "the per-document ones report a succeeded / failed count, so a few "
            "failures are normal rather than an all-or-nothing error. Each button "
            "below has its own (i) with the specifics."
        ),
        beginner=(
            "These buttons update your knowledge base for documents you've "
            "ALREADY added, without re-reading the original files. They work from "
            "what's already stored, and each one acts on the whole corpus.\n\n"
            "Why you'd use them: if you change how something is built — say you "
            "pick a new embedding model, a different entity extractor, or turn on "
            "a layer you didn't use before — that change normally only applies to "
            "new documents you add next. These buttons are how you apply it to "
            "the documents already in the corpus. They're also how you retry a "
            "step that failed earlier.\n\n"
            "How they behave: clicking one first saves your current settings (and "
            "that save stays even if you then cancel the confirmation), asks you "
            "to confirm, then runs. Importantly, if you've turned a graph layer "
            "OFF, running the button that rebuilds that layer removes its data "
            "for the whole corpus — so they can delete, not just add.\n\n"
            "Good to know: the graph ones need the database running (Re-embed "
            "only needs its embedding model), the big ones can take several "
            "minutes and can't be stopped once going, and the per-document ones "
            "report how many documents succeeded or failed. Each button has its "
            "own (i) explaining exactly what it does."
        ),
        technical=(
            "The buttons map to `ingestion/bulk_ops.py` plan/execute pairs "
            "dispatched in `IngestTab._run_bulk_op`. All read existing state "
            "(`get_chunks_by_doc_id`, the KG) — none call `parse.parse_document`. "
            "The per-doc ops scope the whole corpus via "
            "`search_client.list_indexed_docs()`; the global xref ops are "
            "graph-wide and don't enumerate docs. There is no subset picker "
            "either way.\n\n"
            "Flow: `_on_bulk_op_clicked` → "
            "`config_editor.try_save_and_get_error()` (writes the WHOLE draft to "
            "corpus.toml BEFORE the confirm; an invalid draft blocks with a "
            "dialog and nothing runs; the save persists even if you cancel) → "
            "`_run_bulk_op` loads the just-saved config and builds `plan` → "
            "`_show_ingest_confirm` (modal; shows `plan.summary`; Cancel = no "
            "execute; no re-validation between plan and execute) → "
            "`_execute_bulk_op` (spinner only — no progress bar, not "
            "cancellable).\n\n"
            "The rebuild ops run `kg/reconcile.py` first: Rebuild-from-chunks "
            "and Re-extract-entities reconcile all five L6–L10 layers, the "
            "narrower rebuilds only their own layer (a corpus-wide wipe of what "
            "that layer no longer covers — DETACH DELETE for L6/L7 nodes, edge "
            "DELETE for L8–L10; fail-hard), then rebuild per doc. Re-embed, "
            "Resolve, and the global xref ops don't reconcile. Layers cascade "
            "L5→L10, so rebuilding chunks or entities rebuilds their dependents. "
            "Reconcile exists only for L6–L10; changing an L1–L5 setting never "
            "wipes.\n\n"
            "5 of 10 buttons grey out with their layer "
            "(`refresh_bulk_op_gating`: resolve / entities / triples / cross_doc "
            "/ cross_doc_xrefs) — running one with its layer off would "
            "reconcile-wipe that layer and report every doc failed, so it's "
            "disabled; the other 5 (rebuild-from-chunks, re-embed, ontology "
            "re-link, materialize / clear xrefs) stay clickable. Per-doc ops are "
            "fail-soft (n_succeeded / "
            "n_failed); backfill_xrefs / recompute_cross_doc_xrefs / "
            "clear_xref_edges are single global operations. All KG ops require "
            "Neo4j; model-based ops need the provider key (no pre-flight)."
        ),
    ),
    "ingest.bulk_resolve": InfoText(
        title="Resolve paper metadata (L1–L4)",
        standard=(
            "Resolve fills in paper metadata from OpenAlex (a free scholarly database) "
            "for papers already in your corpus — without re-ingesting them. For every "
            "document labelled Paper, it looks the paper up by its DOI and writes its "
            "L1–L4 graph (authors, citations, publication venue, topics) plus its "
            "searchable doc fields (title, year, DOI, venue, authors, language, OpenAlex "
            "id, and a source URL), replacing whatever was there. It needs an internet "
            "connection and works through the corpus one paper at a time, so on a large "
            "corpus it takes a while.\n\n"
            '"Replacing whatever was there" is literal and worth care: if OpenAlex\'s '
            "current record is thinner than what you had, a field can be blanked — a "
            "venue or source URL you had before can be wiped if the new record lacks it. "
            '(The "Skip manually edited" checkbox below protects fields you edited by '
            "hand.)\n\n"
            "After the run you get counts — resolved / not found / failed — but read "
            'them loosely: "resolved" only means OpenAlex returned a record, not that '
            "every write succeeded (a metadata- or graph-write error on that paper is "
            'logged, but the paper still counts as "resolved", not "failed"); and '
            '"not found" also covers papers whose chunks couldn\'t be read or that had '
            "no chunks — not only papers genuinely absent from OpenAlex. A paper needs "
            "its chunks already in the index (it reads those, it doesn't re-read the "
            'file); one with no chunks is reported as "not found".\n\n'
            "It only touches these paper-metadata layers (L1–L4) — not the document's "
            "text, its search chunks, or the entity/ontology/triple layers built from "
            "that text, so refreshing metadata never puts them out of step. The button "
            "is greyed out when the openalex_papers layer is off in the settings panel "
            "(it tracks your unsaved draft toggle, not just the saved corpus).\n\n"
            "Use it to add or refresh this metadata across the whole corpus at once — "
            "e.g. if you ingested papers before turning the OpenAlex layer on: switch "
            "the layer on, then Resolve to backfill them. Because Resolve overwrites, "
            'the "Skip manually edited" checkbox (on by default) protects any paper '
            "whose metadata you edited by hand. Untick it only when you deliberately "
            "want OpenAlex to overwrite your hand-edits; that also drops their "
            '"manual" protection going forward.'
        ),
        beginner=(
            "A paper usually has rich details attached — its authors, the journal it "
            "appeared in, the works it cites, its topics. Resolve fetches those from "
            "OpenAlex (a free online catalogue of research) for the papers already in "
            "your corpus and fills them in — no need to add the papers again. It works "
            "through them one at a time and needs internet, so on a big corpus it takes "
            "a while.\n\n"
            "It works on documents labelled Paper only (it skips books, notes, and so "
            "on), and finds each paper by its DOI (a paper's unique ID). It only fills "
            "in this paper information — it doesn't touch the document's text or the "
            "other things the app builds from that text, so nothing else gets thrown out "
            "of sync.\n\n"
            "Two things to know. First, Resolve replaces the existing details with what "
            "OpenAlex has — so if OpenAlex's record is thinner than yours, a detail you "
            'had can be blanked. The "Skip manually edited" checkbox (on by default) '
            "leaves hand-edited papers alone so your own edits aren't overwritten; only "
            "untick it if you truly want OpenAlex's version to replace what you typed. "
            'Second, the after-run counts are rough: "not found" also covers papers '
            "the app couldn't read, not only ones truly missing from OpenAlex. (The "
            "button is greyed out unless the paper-metadata layer is switched on for "
            "this corpus.)"
        ),
        technical=(
            "bulk_resolve_openalex: sequentially, one doc at a time (runtime scales with "
            "paper count × 1+ API calls each), for every indexed doc that is "
            'DOI-eligible (is_doi_eligible → sub-label "Paper" only; the eligible set '
            "is a single-member frozenset today but is written to be extensible — if "
            "widened, the extra sub-labels become targets, see the gate note below). Per "
            "doc calls resolve_openalex. It writes 8 LanceDB doc fields (title, year, "
            "doi, openalex_id, venue, source_url, authors_display, language) and stamps "
            "metadata_status='enriched'. authors_display is a capped cache — first 3 "
            'names + "et al." — distinct from the L2 :Author graph nodes. Overwrite '
            "takes every field from the new work, so a sub-field the new record lacks is "
            "written as None: a refresh with a sparser record blanks a "
            "previously-populated column (e.g. no primary_location → venue=None, "
            "source_url=None). Reads chunks from LanceDB — no re-parse.\n\n"
            "DOI lookup: stored DOI first; the chunk-text fallback (regex over the first "
            "3 chunks, trying each deduped candidate DOI as a separate API call) fires "
            "whenever no work resolved — i.e. no DOI stored OR the stored DOI 404'd OR "
            "the lookup raised. Never a title search. Unlike ingest, Resolve does not "
            "use the JATS <article-id>/front-matter DOI path (it calls resolve_metadata "
            "with no source_path), so for .xml/.nxml papers whose DOI is only in "
            "front-matter, Resolve's fallback can miss a DOI ingest would find. Requests "
            "use the OpenAlex polite pool (mailto from settings.openalex_mailto when "
            "set) and the shared HTTP client's retry/backoff on 429/5xx/network "
            "errors.\n\n"
            "The L1–L4 KG rewrite is more than edges: delete_doc_l1_l4_edges removes the "
            "doc's :CITES/:AUTHORED/:PUBLISHED_IN/:ABOUT_TOPIC edges and GCs orphaned "
            ":Author/:Topic/:Venue/shadow-:Document nodes; the writes then MERGE new "
            ":Author/:Venue/:Topic nodes and shadow :Document nodes for cited works. "
            "write_citations also mutates the focal :Document — sets in_corpus=true, "
            "doi, openalex_id, and absorbs a pre-existing same-openalex_id shadow "
            "(re-pointing its incoming :CITES). Properties written include "
            ":AUTHORED.position + is_corresponding, :ABOUT_TOPIC.score, :Venue "
            "name/type/issn_l. The whole KG stage (ensure_constraints → delete → 4 "
            "writes) is one all-or-nothing try/except → kg_l1_l4_ok=False on any "
            "raise.\n\n"
            "Result buckets are fail-soft per doc but loose: n_resolved counts "
            "work_resolved==True alone — resolve_openalex catches its own LanceDB-patch "
            "failure and KG-rewrite failure and still returns work_resolved=True, so a "
            'doc with a failed write is counted "resolved", never "failed". The '
            '"failed" bucket only counts exceptions that escape resolve_openalex (rare '
            "— read errors, no-chunks, per-candidate errors, the LanceDB write, and the "
            'whole KG stage are all caught internally). "not-in-OpenAlex" '
            "(work_resolved=False) also fires on a LanceDB read failure and on no "
            'chunks, so it conflates "absent", "unreadable", and "no chunks". '
            "Precondition: chunks in LanceDB; a doc with none aborts work_resolved=False "
            '→ bucketed "not found".\n\n'
            "Scope is L1–L4 only: no L5–L10 calls, and nothing downstream reads OpenAlex "
            "output — entities/triples from chunk text, ontology links from entities, "
            "cross-doc L9 from shared entities (not topics/venue), L10 from ontology "
            "terms — so a resolve never makes a downstream layer stale (verified). Gate "
            "subtlety: the L1–L4 KG writes are gated sub_label=='Paper' inside "
            "_apply_resolved_work, but the LanceDB metadata patch is not — via the "
            'per-doc "Look up DOI online" Edit dialog (lookup_known_doi) a non-Paper '
            "doc's LanceDB metadata WOULD be overwritten while its KG is skipped; the "
            "bulk button avoids this only because its plan filters non-Paper out first. "
            "Execution trusts the plan — target set frozen at plan time, execute calls "
            'resolve_openalex(skip_manual=False), so a doc hand-edited to "manual" '
            "AFTER the confirm dialog is still resolved (only the plan-side filter "
            "protects manual docs). The confirm dialog itemises the breakdown (N "
            "non-Paper skipped, K manual skipped). The grey-out reads the unsaved DRAFT "
            "config, not corpus.toml.\n\n"
            "Skip manually edited (default on → skip_manual=True): excludes Paper docs "
            "whose metadata_status=='manual' (set when you hand-edit in the Documents "
            "view); filtering is plan-side (execute does not re-check). Untick to "
            "include them — overwrites the hand-edited fields AND flips manual→enriched, "
            "so they lose manual protection on later runs. Non-Paper docs excluded "
            "either way."
        ),
    ),
    "ingest.bulk_chunks": InfoText(
        title="Chunks and embeddings (L5)",
        standard=(
            "These two buttons both work on the corpus's existing chunks (the pieces "
            "each document was split into) — without re-reading the original files. Each "
            "runs across every indexed document.\n\n"
            "Re-embed all chunks re-computes the search vectors for every chunk with the "
            "current embedding model. It calls the embedding provider once per chunk, so "
            "on a paid provider (or a large corpus) it costs time and money. Use it "
            "after switching the embedding model or its settings; it doesn't change the "
            "chunks or the knowledge graph. It can't change the vector size — that needs "
            "a full re-ingest, and it reports those docs as failures, but your existing "
            'chunks are left untouched (no data lost). If you have the "optimize '
            'indexes" setting on, it also refreshes the search index afterwards.\n\n'
            "Rebuild graph from chunks rebuilds the knowledge-graph side from the "
            "existing chunks: it re-creates the chunk nodes, then re-runs the graph "
            "layers built from them — entities, ontology links, triples, and both "
            "cross-document layers — so the graph matches your current settings "
            "(rebuilding the layers that are on, clearing any you've turned off). This "
            "is the heavy button: it re-runs full entity extraction (and triples) with "
            "the language model on every chunk, so on a large corpus it's slow and, with "
            "an LLM extractor, costly. (It does not re-make the search vectors — that's "
            "the other button.) Use it if the graph was wiped or you turned a graph "
            "layer on after adding documents. It doesn't re-split or re-read the files — "
            "to change chunk size or the splitter, do a full Re-ingest.\n\n"
            "Both report per-document success/failure counts. A document with no stored "
            "chunks is reported as a failure; for Rebuild, so is running it while the "
            "chunks layer itself is off (that still clears the downstream layers first)."
        ),
        beginner=(
            "Every document you add is split into small pieces called chunks, and each "
            'chunk gets a numeric "fingerprint" (an embedding) used for searching. '
            "These two buttons refresh those from the chunks already stored — no need to "
            "re-add the files. Both run over every document in the corpus.\n\n"
            "Re-embed all chunks makes fresh fingerprints for every chunk with the "
            "current settings — use it if you changed which embedding model the app "
            "uses, so search keeps working well. It only updates the search side, not "
            "the text or the knowledge graph. It contacts the embedding service once per "
            "chunk, so on a paid model or a big corpus it costs time and money. If you "
            "switch to a model with a different fingerprint size it can't do that here "
            "(it reports those documents as failed) — but it never deletes your existing "
            "chunks.\n\n"
            "Rebuild graph from chunks rebuilds the knowledge-graph parts made from your "
            "chunks: first it re-creates the chunk nodes, then the entities found in "
            "them, links to reference vocabularies, relationships, and connections "
            "between documents — so the graph reflects your current settings. This is "
            "the heavy one — it re-runs the entity- and relationship-finding (which use "
            "the language model) over every chunk, so it's slow and can cost money. Use "
            "it if the graph was cleared, or you switched a graph feature on (or off) "
            "after adding documents, and you want it brought up to date from the chunks "
            "you already have. It doesn't re-split your documents (for that, re-add "
            "them). A document with no stored chunks is reported as failed."
        ),
        technical=(
            "Both read chunk data from LanceDB; neither re-parses the source. Each "
            "targets every indexed doc (list_indexed_docs), corpus-wide, one doc at a "
            "time, fail-soft per doc (one doc's failure is caught, counted, loop "
            "continues).\n\n"
            "Re-embed all chunks (bulk_re_embed → re_embed): one embedding-provider call "
            "per chunk (text chunks via the text embedder, figure/image chunks via the "
            "multimodal path), so cost/time scale with chunk count on a paid provider. "
            "LanceDB has no in-place column update, so it delete-then-re-inserts the "
            "doc's ENTIRE row set (every doc-level field rewritten, not just the vector) "
            "and stamps a new ingested_at on every chunk. Success needs BOTH embed_ok "
            "AND lancedb_ok; a doc with zero LanceDB chunks → embed_ok=False (a "
            "failure). A dimension change is refused BEFORE the delete (lancedb_ok=False, "
            "chunks left intact, no data loss; change dims via full re-ingest). On a "
            "failed rewrite it rolls back by re-writing the old rows — but if that "
            "rollback itself fails the doc's chunks can be lost (logged ERROR). Index "
            "refresh runs only when optimize_indexes_per_ingest is on, and even then the "
            "vector/ANN index is skipped below min_rows_for_vector_index (default 256; "
            "brute-force below that) while the FTS index is always refreshed. No KG "
            "cascade — nothing in the graph reads the vector. Needs the embedding "
            "provider installed/keyed or every doc fails.\n\n"
            "Rebuild graph from chunks (bulk_backfill_chunks → backfill_chunks): first "
            "reconciles the downstream layers to config corpus-wide — wipes L6-L10 data "
            "for any layer now off, AND prunes :Entity nodes whose type is no longer in "
            "entity_types even while L6 stays on. This reconcile is fail-hard: a wipe "
            "error aborts the whole op (unlike the fail-soft per-doc loop). Then per doc "
            "it deletes + rewrites the KG :Chunk/:PART_OF from existing LanceDB chunks "
            "(labels recovered from the first row) and cascades backfill_entities (L6) → "
            "backfill_ontology (L7) → backfill_triples (L8) → backfill_cross_doc (L9) → "
            "backfill_cross_doc_xrefs (L10), each a no-op when its layer is off. It "
            "re-runs full L6 entity extraction (extract_union — an LLM/GLiNER call per "
            "non-empty chunk; empty/whitespace chunks skip it; per-chunk extraction is "
            "fail-soft) and L8 triple extraction — L6/L8 are the token-costly layers, so "
            "it's the expensive button. Success is judged ONLY on the L5 chunk write "
            "(chunks_ok): a failure deeper in the cascade does NOT mark the doc failed. "
            "A doc is reported failed when the chunks layer is disabled in config (every "
            "doc chunks_ok=False — reconcile still runs first) or when it has no LanceDB "
            "chunks / the read fails. Unlike re-embed there is NO rollback on the KG "
            "chunk write: if delete_chunks_by_doc_id succeeds but write_chunks fails, "
            "the doc's :Chunk nodes are left deleted. Needs the entity extractor/LLM "
            "installed for the cascade. Does NOT re-chunk (chunk_max_tokens / "
            "chunker_strategy changes need re-ingest)."
        ),
    ),
    "ingest.bulk_entities": InfoText(
        title="Entities (L6)",
        standard=(
            "Re-extract all entities re-runs entity extraction over the corpus's "
            "existing chunks — the named things (people, organisations, terms, …) the "
            "app pulls from your documents' text — without re-reading the original "
            "files. It's a heavy operation: it runs the entity extractor (and, in the "
            "cascade, the relationship/triples extractor) with the language model on "
            "every chunk, so on a large corpus it's slow and, with an LLM extractor, "
            "costly.\n\n"
            "It deletes as it goes, so nothing stale is left. Two kinds of deletion "
            "happen. First, a corpus-wide reconcile clears any downstream layer you've "
            "switched off (ontology, triples, both cross-document layers) — and, even "
            "when entities stay on, it prunes entity nodes whose type is no longer in "
            "your entity-types list. Then, document by document, it deletes that "
            "document's old data at each stage before writing the fresh version — its "
            "old entities/mentions, then its ontology links, then its triples, then its "
            "cross-document links (both layers) — so old and new are never mixed. Each "
            "layer is only rebuilt if it's turned on.\n\n"
            "Use it after you change the entity extractor or the entity types, or after "
            "enabling entities once documents are already in. (If you only turned on a "
            "single downstream layer — ontology, triples, or cross-doc — its own "
            "dedicated button is cheaper than this, which re-runs the whole extraction.) "
            "It reads the stored chunks, so it can't change how documents are split — "
            "that needs a full Re-ingest.\n\n"
            "It reports per-document success/failure counts. A document with no stored "
            "chunks is reported as failed; running it while the entities layer itself is "
            "off wipes all entities and rebuilds nothing (every document is reported "
            "failed). Entities are shared between documents, so this refreshes each "
            "document's own mentions but keeps entities other documents still use."
        ),
        beginner=(
            "As the app reads your documents, it picks out entities — the notable things "
            "mentioned, like people, organisations, places, or key terms. This button "
            "re-does that entity-finding for the documents you've already added, using "
            "the chunks already stored (no need to re-add the files). It's the heavy "
            "kind of button — it runs the language model over every chunk to find "
            "entities and their relationships, so it's slow and can cost money.\n\n"
            "It cleans up as it rebuilds, so you never get old and new mixed together. "
            "Two things get deleted: anything belonging to a graph feature you've "
            "switched off is cleared out completely; and then, document by document, it "
            "removes that document's old entities before finding them again — and does "
            "the same at each following step (the vocabulary links, the relationships, "
            "the document-to-document connections): the old version out, the new version "
            "in. (Entities are shared between documents, so it refreshes this document's "
            "own mentions but keeps entities that other documents still use.)\n\n"
            "When would you use it? After you change which entity-finder or which kinds "
            "of entities the app uses, or if you switched a graph feature on after adding "
            "documents and want it filled in from the chunks you already have. It can't "
            "change how your documents are split into pieces — for that you'd re-add "
            "them. A document with no stored chunks is reported as failed."
        ),
        technical=(
            "bulk_backfill_entities → backfill_entities per doc, after a corpus-wide "
            "reconcile. Targets every indexed doc; the per-doc loop is fail-soft (a "
            "doc's failure is caught, counted, loop continues), but the initial "
            "reconcile is fail-hard — a wipe error aborts the whole op.\n\n"
            "Reconcile first (bulk_backfill_entities_execute): reconciles ontologies / "
            "entities / triples / cross_doc / cross_doc_xrefs to config — wipes "
            "corpus-wide any of those layers you've turned off (no stale disabled-layer "
            "data), like ingest. reconcile_entities also prunes :Entity nodes whose type "
            "is no longer in entity_types even while L6 stays on (and wipes all entities "
            "when L6 is off).\n\n"
            "Then per doc (reported a failure — entities_ok=False — if the doc has no "
            "LanceDB chunks; the cascade is gated on entities_ok): backfill_entities "
            "runs delete_entities_by_doc_id (drops this doc's :MENTIONS, GCs entities "
            "left with no mentions from any chunk — :Entity nodes are "
            "corpus-shared/MERGEd, so ones other docs mention survive), then re-runs "
            "extract_union — the priority-ordered UNION of config.entities.extractors "
            "(can be several; merged by span overlap with per-span provenance) under "
            "entity_types / entity_types_mode (add = extend the adapter's defaults, "
            "replace = use only your types) / model + temperature — and writes fresh "
            ":Entity + :MENTIONS. That's an extractor call per non-empty chunk across "
            "every doc (LLM/GLiNER), so cost/time scale with the corpus; L6 and the L8 "
            "triples pass are the token-costly layers. If the KG :Chunk nodes were "
            "wiped, write_entities silently drops the :MENTIONS (it MATCHes existing "
            ":Chunk by id) — this button alone can't restore mentions; run Rebuild graph "
            "from chunks first.\n\n"
            "Cascade (per doc, each a no-op when its layer is off), in code order: "
            "backfill_ontology (L7) FIRST — deletes this doc's :CANONICAL_TO via "
            "delete_canonical_links_for_doc then re-links (MERGE), and can trigger a "
            "full ontology import (ensure_ontology_imported) if an enabled ontology "
            "isn't yet present — then backfill_triples (L8) (delete_triples_by_doc_id → "
            "re-extract with its own triples_extractor_model/temperature, only for "
            "chunks that have entities), then backfill_cross_doc (L9) and "
            "backfill_cross_doc_xrefs (L10) (wipe this doc's :RELATED_TO / "
            ":RELATED_BY_XREF → recompute; edges created only when shared-entity / "
            "shared-concept counts meet config.cross_doc[_xrefs].threshold). "
            "Empty/whitespace chunks skip the extractor; per-chunk extraction is "
            "fail-soft. Needs the entity extractor/LLM installed. The confirm dialog "
            "shows only a doc count, no cost estimate. Reads chunks from LanceDB — no "
            "re-parse; chunk_max_tokens / chunker_strategy changes need a full "
            "Re-ingest."
        ),
    ),
    "ingest.bulk_ontology": InfoText(
        title="Ontology linking (L7)",
        standard=(
            "Ontology linking connects the entities pulled from your documents to "
            'standard vocabulary terms — the shared "canonical" concepts from '
            "vocabularies like MeSH or GO — so different wordings for the same thing "
            "line up. (A vocabulary must be installed on the Installs tab and enabled, "
            "and it's actually loaded into the graph the first time it's used; nothing "
            "here downloads anything.) This section has three tools. Note up front: if "
            "the cross-document xref layer (L10) is on, all three rebuild it graph-wide "
            "at the end — a heavy step that can take minutes on a large corpus.\n\n"
            "Re-link all entities re-runs that linking across the whole corpus. It only "
            "links entities that are already there (it doesn't re-find entities — if a "
            "document's entities are missing, run Re-extract all entities first). It "
            "first clears any vocabulary you've switched off, then, document by "
            "document, deletes each document's old links and rebuilds them against the "
            "enabled vocabularies using their current matching setting (exact, or fuzzy "
            "which also tries simple plural/hyphen variants) — so a stricter or looser "
            "match takes effect cleanly instead of piling on top of the old one. Use it "
            "after enabling a new vocabulary, or after changing a vocabulary's matching, "
            "once documents are already in.\n\n"
            "Materialize xref edges builds the equivalence links BETWEEN vocabularies "
            "(e.g. a MeSH term to the matching MONDO term) from cross-references the "
            "vocabularies carry, and — if L10 is on — rebuilds the cross-document links "
            "that ride on them. Important: those cross-references only exist if the "
            "vocabulary was imported with xref-collection turned on (the xrefs setting "
            'at "collect only" or "materialize"); a vocabulary imported with xrefs '
            "off carries none, and enabling xrefs afterwards does NOT backfill them — "
            "you'd have to re-import that vocabulary. It also resolves a link only when "
            "both vocabularies involved are loaded. Use it once your vocabularies were "
            "imported with xrefs on and you want the cross-vocabulary links built or "
            "refreshed.\n\n"
            "Clear xref edges… asks which vocabulary, then removes that one's outgoing "
            "cross-vocabulary links AND wipes its stored (unresolved) cross-references "
            "(links pointing IN from other vocabularies stay — clear those in turn to "
            "remove both directions). If L10 is on it rebuilds afterwards. Because it "
            "wipes the stored cross-references, you can't rebuild those edges with "
            "Materialize afterwards (Materialize would have nothing left to resolve) — "
            "that needs a re-import of the vocabulary. Use it as a genuine teardown, "
            "not as a step before re-materializing."
        ),
        beginner=(
            "As the app reads your documents it finds entities (notable things — "
            "people, genes, places, key terms). Ontology linking matches those "
            "entities to entries in standard reference vocabularies — like tagging "
            'each one with its "official" dictionary meaning, so "heart attack" and '
            '"myocardial infarction" can be recognised as the same idea. There are '
            "three buttons here. (A vocabulary must be installed and switched on first, "
            "and it's loaded into the graph the first time it's used — nothing here "
            "downloads anything.) One heads-up: if the cross-document link layer is on, "
            "all three rebuild it at the end, which can take a while.\n\n"
            "Re-link all entities re-does that matching for the documents you've "
            "already added. It only matches entities that are already found (it doesn't "
            "re-find them). It cleans up as it goes — it removes any vocabulary you've "
            "turned off, and for each document it clears the old matches and makes them "
            "fresh — so old and new never mix. Use it after you turn on a new "
            "vocabulary, or change how strictly a vocabulary matches.\n\n"
            "Materialize xref edges connects the vocabularies TO EACH OTHER, using "
            "cross-references they carry (so a term in one dictionary is tied to the "
            "equivalent term in another). Those cross-references are only there if the "
            "vocabulary was brought in with that feature on — if not, this button can't "
            "create them until the vocabulary is re-imported. Use it once your "
            "vocabularies have that data and you want the cross-vocabulary links "
            "built.\n\n"
            "Clear xref edges… asks you to pick one vocabulary, then removes its "
            "connections to the other vocabularies and wipes its stored cross-references "
            "(pick each vocabulary in turn to fully undo them). Because it wipes those "
            "stored cross-references, you can't rebuild the links with Materialize "
            "afterwards — that would need re-importing the vocabulary. So use it as a "
            "real teardown, not as a quick step before rebuilding."
        ),
        technical=(
            "Three ops on the L7 surface (bulk_ops.py), each plan → confirm → execute. "
            "The graph-wide L10 recompute all three can trigger "
            "(recompute_cross_doc_xrefs_global) is a full wipe-and-rebuild of every "
            ":RELATED_BY_XREF edge over all doc pairs (edges created only when "
            "shared-concept-via-xref counts meet config.cross_doc_xrefs.threshold, "
            "default 2) — heavy (minutes on a large corpus) and fail-soft (logged, not "
            "raised) in all three ops.\n\n"
            "Re-link all entities (bulk_backfill_ontology): targets every indexed doc "
            "(docs with no entities are iterated as no-op successes; it only LINKS, "
            "never re-extracts — if a doc's L6 entities are missing, run Re-extract all "
            "entities first). reconcile_ontologies_to_config first: DETACH DELETEs the "
            "TERM NODES of any ontology that is BOTH disabled AND currently imported "
            "(graph-wide, cascading to :CANONICAL_TO + xref edges; using it again needs "
            "a re-import). Then per doc, per enabled ontology: ensure_ontology_imported "
            "(no-op short-circuit if already imported; a first-time import is heavy; "
            "raises OntologyNotDownloadedError if not on disk — CAUGHT per-ontology → "
            "import_ok=False, other ontologies still run, never downloads), "
            "delete_canonical_links_for_doc (clears this doc's :CANONICAL_TO to that "
            "ontology and resets canonicalised=false only on entities left with NO "
            ":CANONICAL_TO to ANY ontology), then link_entities (re-MERGEs). Matching "
            "(OntologyConfig.matching): exact = entity key == a term's label/synonym "
            "(lowercased); fuzzy = exact first, then morphological variants "
            "(plural/singular flip 0.9, hyphen↔space 0.85, combined 0.75; first hit "
            "wins; strategy+confidence recorded). One entity can link to several terms "
            "(multiple synonyms, across ontologies) — each its own edge. Because "
            ":Entity is corpus-shared and :CANONICAL_TO is entity→term (not doc-scoped), "
            "clearing \"this doc's\" links touches the shared entity's "
            "edges/canonicalised flag. Linking is per-ontology fail-soft. Success = "
            "any(import_ok) across enabled ontologies, or zero enabled = no-op success "
            "(checks import_ok, not n_links). Delete-then-link = true rebuild. When "
            "layers.cross_doc_xrefs is on it recomputes L10 at the end; L8/L9 read raw "
            "entities, not canonicals, so they're untouched. (Enabling an ontology "
            "requires it downloaded — the config checkbox is disabled otherwise, "
            "defaulting open only on a probe error, with the ingest path hard-guarding "
            "regardless — but linking additionally requires it IMPORTED into Neo4j; "
            "installed-on-disk and the enabled flag are not the same as imported.)\n\n"
            "Materialize xref edges (backfill_xrefs): graph-wide over ALL 18 shipped "
            "ontology sub-labels (independent of which ontologies this corpus enables). "
            "xrefs.backfill_resolved_xrefs resolves each ontology's dangling_xrefs into "
            ":<X>_XREF equivalence edges (MERGE, idempotent; n_edges_attempted counts "
            "matched+created rows, not new edges) and strips resolved entries (leaving "
            "dangling_xrefs=[]). An xref resolves only if BOTH endpoints are imported — "
            "a target not present (unimported ontology, unshipped URI) is skipped and "
            "stays dangling for a later run. Precondition: dangling_xrefs exist ONLY if "
            "the ontology was imported under layers.xrefs='collect_only' or 'use'; "
            "imported under 'none' it holds NO xref data, so Materialize resolves "
            "nothing for it until a force re-import. Under 'use', edges are already "
            "written at import (Materialize mainly catches delayed cross-ontology "
            "resolution); under 'collect_only', edges are NEVER written at import and "
            "Materialize is the only way to create them. Per-ontology resilient (one "
            "ontology's Cypher failure → zeros, walk continues) and whole-op fail-soft. "
            "Then recompute_cross_doc_xrefs_global when layers.cross_doc_xrefs is on. "
            "No-op when layers.xrefs='none'.\n\n"
            "Clear xref edges… (clear_xref_edges): the GUI dropdown lists ALL 18 "
            "ontologies regardless of installed/enabled/imported state (picking one "
            "with no data is a harmless no-op). clear_xref_edges_for_ontology DELETEs "
            "that term-label's OUTBOUND :<X>_XREF edges AND does a full REMOVE of its "
            "dangling_xrefs property (vs backfill's strip, which leaves an empty list). "
            "Inbound edges from other ontologies are left in place (run per-ontology to "
            "clear both directions). n_cleared = edges deleted + properties removed; "
            "the clear and the L10 recompute are both fail-soft. Recomputes L10 "
            "afterwards when the layer's on. Because Clear REMOVEs dangling_xrefs and "
            "Materialize's resolve requires them, a literal Clear → Materialize "
            "re-creates ZERO edges for that ontology — rebuilding those xref edges "
            "requires a force re-import, not Materialize."
        ),
    ),
    "ingest.bulk_triples": InfoText(
        title="Triples (L8)",
        standard=(
            "Triples are typed relationships the app pulls between entities that "
            'appear in the SAME chunk of a document — statements like "X inhibits '
            'Y" or "A is part of B" — using a language model to read each chunk '
            "and name how those entities relate. (Two entities in different chunks "
            "of the same document are never linked.) The relationships are directed "
            "(X inhibits Y is not the same as Y inhibits X) and come from a fixed "
            "set of 15 types (inhibits, activates, causes, treats, is-a, …).\n\n"
            "Re-extract all triples re-runs that extraction across the whole corpus "
            "from the chunks and entities already stored — it doesn't re-read your "
            "files or re-find entities. Document by document it deletes that "
            "document's old triples first, then calls the language model once per "
            "chunk that has at least one entity (chunks with no entities are "
            "skipped). It's slower/costlier on larger corpora, but — unlike the "
            "entity and chunk rebuild buttons, which re-run this triples step AND "
            "more — it's the lightest of the heavy buttons. It can't be cancelled "
            "once it starts.\n\n"
            "One caution: because it deletes each document's old triples before "
            "re-extracting, if the language model is unavailable partway through, "
            "that document can end up with its old triples gone and no new ones — "
            "and it's still reported as succeeded (a model or provider problem "
            "produces zero triples, not an error). So run it when your "
            "model/provider is reachable.\n\n"
            "The button is available only while the triples layer is on (running it "
            "with the layer off would wipe all triples and mark every document "
            "failed). Clicking it first saves any pending settings edits and, if "
            "they're invalid, blocks with an error. It reports per-document "
            "succeeded/failed counts (a document with no stored chunks is reported "
            "failed; one with no entities simply produces no triples, counted a "
            "success). Use it after enabling triples on a corpus that already has "
            "documents. Nothing else depends on triples, so this doesn't cascade to "
            "other layers."
        ),
        beginner=(
            "Entities are the notable things in your documents; triples go a step "
            "further and capture how two of them — mentioned in the same passage — "
            'relate, e.g. "drug X treats disease Y" or "protein A is part of B". '
            "A language model reads each passage and writes those relationships "
            'down, choosing from 15 fixed types (and the direction matters: "X '
            'causes Y" isn\'t "Y causes X").\n\n'
            "Re-extract all triples re-does this for everything you've already "
            "added, using the passages and entities already stored (no re-reading "
            "files, no re-finding entities). For each document it clears the old "
            "relationships first, then asks the model about every passage that has "
            "entities. It's slower on bigger corpora, but it's actually the lightest "
            'of the three "heavy" rebuild buttons (the entity and chunk ones do '
            "this step and more). It can't be stopped once started.\n\n"
            "A caution: because it clears the old relationships before making new "
            "ones, if the model isn't reachable partway through, a document can lose "
            "its old relationships without getting new ones — and it still shows as "
            "done (a model problem just yields no relationships, not an error). So "
            "run it when your model is available.\n\n"
            "It's only available when the triples layer is on. Clicking it also "
            "saves any unsaved settings first. Use it after turning triples on for "
            "documents you've already added. A document with no entities just gets "
            "no triples; one with no stored chunks is reported as failed."
        ),
        technical=(
            "bulk_backfill_triples → reconcile_triples_to_config then "
            "pipeline.backfill_triples per doc. Targets every indexed doc (frozen at "
            "plan time; includes Artifacts and chunk-less docs). The button is "
            "disabled unless layers.triples is on (refresh_bulk_op_gating, reading "
            "the live DRAFT config and updating as you toggle) — running with the "
            "layer off would wipe all triples and report every doc failed. Clicking "
            "first calls try_save_and_get_error (saves the pending config draft to "
            "corpus.toml; an invalid config blocks with a dialog, nothing runs). The "
            "confirm dialog shows only a raw doc count. Not cancellable once started "
            "(bulk ops don't arm the Cancel button).\n\n"
            "Reconcile first: reconcile_triples_to_config wipes every triple edge "
            "corpus-wide when layers.triples=false (one Cypher over the 15 "
            "TRIPLE_PREDICATE_RELS); no-op when on. Fail-hard (a wipe error aborts "
            "the op). The per-doc loop is fail-soft (a doc's exception is caught, "
            "counted failed, loop continues).\n\n"
            "Per doc: reads chunk text from LanceDB (get_chunks_by_doc_id; a "
            "missing-or-unreadable read → triples_ok=False) + the per-chunk entity "
            "vocab from Neo4j (get_entities_by_chunk — existing :Entity/:MENTIONS, "
            "never re-extracts). Triples are CHUNK-scoped: the LLM sees one chunk's "
            "entity vocab per call, so a triple can only relate two entities "
            "co-mentioned in the SAME chunk (cross-chunk pairs are never related). "
            "delete_triples_by_doc_id wipes this doc's edges BEFORE the extract loop "
            "(no rollback), then one triples_extractor.extract call per chunk with "
            "≥1 entity (config.triples_extractor_model + _temperature; with "
            "retry/backoff; zero-entity chunks skipped; per-chunk extraction "
            "fail-soft → []), then write_triples (CREATE; one edge per chunk "
            "assertion, so the same triple in N chunks = N edges; each edge stores "
            "an evidence_span snippet + chunk_id + doc_id; directed). Two "
            "consequences: (1) a model/provider outage for a whole doc leaves it "
            "with old triples deleted and none written, yet triples_ok=True — silent "
            "data loss reported as success; a missing/uninstalled LLM provider "
            "likewise degrades to 0 triples, not a failure. (2) n_triples (extractor "
            "output, pre-write) is NOT surfaced — the bulk executor keeps only "
            "triples_ok and returns per-doc succeeded/failed counts. write_triples "
            "silently skips a CREATE for an absent subject/object entity and drops "
            "any out-of-vocab predicate (near-zero: the extractor pre-validates "
            "entity-in-vocab and predicate-in-the-15). A doc is failed on: no chunks "
            "/ LanceDB read raise / get_entities_by_chunk raise / delete_triples "
            "raise / layer disabled / any escaping exception. Missing entities → "
            "soft success (0 triples).\n\n"
            "Leaf layer: L9 (shared :Entity keys) and L10 (:CANONICAL_TO / xref) "
            "read raw entities/canonicals, never triple edges, so re-extracting "
            "triples cascades to nothing — and it's the cheapest of the three heavy "
            "buttons (bulk_backfill_chunks and bulk_backfill_entities both re-run "
            "this triples pass plus additional layers). Predicates are the fixed "
            "15-member schema.TRIPLE_PREDICATE_RELS "
            "(INHIBITS/ACTIVATES/CAUSES/TREATS/IS_A/…); only triples_extractor_model "
            "+ _temperature are per-corpus."
        ),
    ),
    "ingest.bulk_cross_doc": InfoText(
        title="Cross-doc links (L9)",
        standard=(
            "This rebuilds the cross-document links (L9) across your whole "
            "corpus: it connects any two documents that share several of the same "
            "entities, so the app and your queries can hop between related "
            "documents. 'Rebuild cross-doc links' wipes and recomputes these "
            "links for every document, using your current threshold — two "
            "documents are linked when they share at least that many distinct "
            "entities (default 2).\n\n"
            "It reads the entities already extracted for each document; it "
            "doesn't extract anything or call a model, so there's no per-document "
            "LLM cost — but on a large corpus it still takes a while, because it "
            "compares every document against all the others. A document with no "
            "entities yet simply gets no links (a normal result, not an error). "
            "Because re-extracting entities already rebuilds these links as part "
            "of its own run, you mainly need this button when you turned the "
            "Cross-doc layer on after ingesting, or changed the threshold.\n\n"
            "The button is greyed out while the Cross-doc layer is off. Clicking "
            "it first saves any pending settings changes to the corpus, then asks "
            "you to confirm (the confirm box names the threshold it will use); "
            "when it finishes, the status reports how many documents it processed "
            "and how many links it wrote."
        ),
        beginner=(
            "This finds documents that are about similar things and links them "
            "together, so the app can jump from one document to the related ones. "
            "Two documents get linked when they mention several of the same "
            "entities (people, places, concepts, and so on) — by default, at "
            "least two of the same.\n\n"
            "'Rebuild cross-doc links' clears these links and works them out "
            "again for your whole collection. It uses the entities the app "
            "already pulled out of each document, so it doesn't read your "
            "documents again or use an AI model — but on a big collection it "
            "still takes a little while. A document that has no entities yet just "
            "won't get any links, and that's fine (not an error).\n\n"
            "You mostly need this button if you turned this feature on after "
            "adding documents, or you changed how many shared entities count as a "
            "link. It's greyed out when the feature is off. Clicking it saves any "
            "settings you've changed, asks you to confirm, and then tells you how "
            "many documents it did and how many links it made."
        ),
        technical=(
            "The bulk op behind 'Rebuild cross-doc links' (op key "
            "bulk_backfill_cross_doc). It targets every indexed doc (from LanceDB "
            "list_indexed_docs) and, per doc, wipes and recomputes that doc's L9 "
            ":RELATED_TO edges. An edge links two focal nodes (:Document / "
            ":Artifact) that share at least config.cross_doc.threshold (default "
            "2) DISTINCT L6 entity keys — the RAW lowercased extractor text, "
            "matched via a :MENTIONS double-hop (chunk→entity→chunk), NOT "
            "ontology canonicals, so ontology import/delete never affects L9. The "
            "shared count is over DISTINCT e.key and ignores entity_type, so "
            "same-text/different-type entities (e.g. 'insulin' as Drug and as "
            "Protein) collapse to one shared key. Edges are undirected — one per "
            "unordered doc pair — carrying shared_entities, shared_count, and "
            "computed_at; linking is cross-type (a paper can relate to a "
            "dataset/audio/code artifact).\n\n"
            "Flow: the button is greyed while layers.cross_doc is off (like the "
            "triples button). Clicking it runs try_save_and_get_error first — "
            "committing the whole config draft to corpus.toml (this persists even "
            "if you then cancel the confirm dialog) — then plan → confirm → "
            "execute. The plan is a cheap list_indexed_docs read; its confirm "
            "summary names the threshold. Execute calls "
            "reconcile_cross_doc_to_config first (a no-op while the layer is on; "
            "it only wipes edges corpus-wide when the layer is off, a path the "
            "grey-out now keeps you out of), then loops per-doc "
            "pipeline.backfill_cross_doc. The final status reports n_succeeded / "
            "n_failed and the total number of links written.\n\n"
            "Cascade: it needs L6 entities already in the graph — a doc with none "
            "silently yields zero edges (a success, not an error); it never runs "
            "extraction. It does NOT recompute L10 (:RELATED_BY_XREF is a "
            "separate button/edge type; L9 anchors nothing downstream). The same "
            "per-doc recompute already runs during normal ingest and inside the "
            "entities (L6) backfill cascade, so running this right after an "
            "entities backfill is redundant. Deleting a doc removes its "
            ":RELATED_TO edges automatically. Cost is pure Cypher (no LLM) but "
            "roughly O(docs²): it loops a per-doc overlap query over every doc, "
            "evaluates each pair from both ends, and wipes-then-rebuilds each "
            "doc's edges — so a large corpus is slow even though it's model-free. "
            "Success is per-doc cross_doc_ok; zero edges is a valid success; a "
            "failure means the recompute Cypher crashed for that doc."
        ),
    ),
    "ingest.bulk_cross_doc_xrefs": InfoText(
        title="Cross-doc xref links (L10)",
        standard=(
            "This rebuilds the concept-level cross-document links (L10) for your "
            "whole corpus. Where the L9 links connect documents that share the "
            "same raw entities, these connect documents that share the same "
            "underlying concepts — recognised through your ontologies, so the "
            "same idea counts even when two documents name it differently (for "
            "example the same drug under MeSH and under ChEBI). 'Rebuild cross-doc "
            "xref links' wipes and recomputes every one of these links in a single "
            "pass, linking any two documents that share at least the threshold "
            "number of distinct concepts (default 2).\n\n"
            "It builds on work other steps already did — your entities (L6), their "
            "ontology links (L7 canonicals), and the cross-ontology xref edges — "
            "so it needs those in place; a document without them simply gets no "
            "links (a normal result, not an error). It's pure database work with "
            "no model calls, but because it compares every document against all "
            "the others it can take minutes on a large corpus. You rarely need "
            "this button on its own: re-linking entities or materializing xref "
            "edges already rebuild these links for you. Reach for it when you've "
            "changed the threshold or hand-edited the ontology graph and want the "
            "links refreshed without touching anything else.\n\n"
            "The button is greyed out while the Cross-doc xrefs layer is off. "
            "Clicking it first saves any pending settings changes, then shows a "
            "confirm box with the threshold and how many links exist now; when it "
            "finishes, the status reports how many links it wrote (or a clear "
            "'failed' message with the error if the rebuild couldn't complete)."
        ),
        beginner=(
            "This links documents that are about the same ideas, even when they "
            "use different words for them. It uses your ontologies (built-in "
            "dictionaries of concepts) to tell that, say, one document's 'aspirin' "
            "and another's 'acetylsalicylic acid' are the same thing, and then "
            "connects the two documents. It's a companion to the L9 links: L9 "
            "connects documents that share the exact same terms, while this one "
            "connects them through shared concepts.\n\n"
            "'Rebuild cross-doc xref links' clears these concept links and works "
            "them all out again for your whole collection, in one pass. It relies "
            "on earlier steps having pulled out the entities and matched them to "
            "the ontologies, so those need to be done first; a document that "
            "hasn't been through that just won't get any links (which is fine). "
            "There's no AI model involved, but on a big collection it can take a "
            "few minutes. You usually don't need this button by itself — "
            "re-linking entities or building the xref edges already refreshes "
            "these links — so it's mainly for when you changed the setting for how "
            "many shared concepts count as a link.\n\n"
            "It's greyed out when the feature is off. Clicking it saves any "
            "settings you've changed, shows you the threshold and how many links "
            "exist now, and asks you to confirm; then it tells you how many links "
            "it made (or a clear failure message if something went wrong)."
        ),
        technical=(
            "The bulk op behind 'Rebuild cross-doc xref links' (op key "
            "recompute_cross_doc_xrefs). Unlike the per-doc L9 button, this is a "
            "GLOBAL one-query wipe-and-rebuild: recompute_cross_doc_xrefs_global "
            "deletes every :RELATED_BY_XREF edge and rewrites the lot over all doc "
            "pairs (id(this) < id(other) to dedupe). An edge links two focal nodes "
            "(:Document / :Artifact) that share at least "
            "config.cross_doc_xrefs.threshold (default 2) DISTINCT ontology "
            "concepts, where 'shared concept' means the same :OntologyTerm OR two "
            "terms joined by any of the 18 :<X>_XREF edges. The signal is L6 "
            "entities canonicalised to ontology terms "
            "(:Entity-[:CANONICAL_TO]->:OntologyTerm) plus L7 xref equivalence — "
            "NOT raw entity keys (that's L9). Edges are undirected — one per "
            "unordered pair — carrying shared_concepts, shared_count, and "
            "computed_at; linking is cross-type.\n\n"
            "Flow: the button is greyed while layers.cross_doc_xrefs is off. "
            "Clicking it runs try_save_and_get_error first (committing the whole "
            "config draft to corpus.toml, even if you then cancel the confirm "
            "dialog), then plan → confirm → execute. The plan "
            "(RecomputeCrossDocXrefsPlan) runs one live count of existing "
            ":RELATED_BY_XREF edges (a directed match, so the confirm summary "
            "shows the true count, not a doubled one) and names the threshold. "
            "Execute calls the global recompute and returns n_edges_written (the "
            "true count); if the recompute Cypher crashes it's logged and surfaces "
            "as 'Rebuild cross-doc xref links failed: <error>' (no longer "
            "swallowed into a fake success). The off-branch (reconcile-wipe "
            "corpus-wide + layer_skipped=True) still exists but the grey-out keeps "
            "you out of it.\n\n"
            "Requires more than L9: L6 entities, their L7 canonical links "
            '(:CANONICAL_TO), and L7 xref edges (xrefs="use") — the config '
            "validators enforce that cross_doc_xrefs=true needs entities=true and "
            'xrefs="use". Missing pieces yield zero edges, not an error. It does '
            "NOT cascade downstream (L10 is the top layer; only the agent's "
            "query-time Cypher reads these edges, and only when the layer is on). "
            "It is REDUNDANT after several other buttons that end in this same "
            "global recompute — Re-link all entities and Materialize xref edges — "
            "and after the per-doc rebuilds inside ingest, Re-extract all "
            "entities, and Rebuild graph from chunks; its unique use is a "
            "standalone refresh (changed threshold, hand-edited ontology graph) "
            "with no other layer touched. Cost is pure Cypher (no LLM) but a "
            "graph-wide join over every doc pair — minutes on thousands of docs."
        ),
    ),
    "ingest.settings_reconcile": InfoText(
        title="How these settings are saved and applied",
        standard=(
            "The panel on the right holds this corpus's ingestion settings — its "
            "document labels, which knowledge-graph layers are on, the entity "
            "extractor, ontologies, and thresholds. Editing here doesn't take effect "
            "on its own: a dot marks unsaved changes, and Discard reverts them.\n\n"
            "The changes are saved the next time you run ANY ingest OR bulk operation "
            "(Ingest folder, Re-ingest, Sync, single file, or a Bulk-operations "
            "button). Saving happens the moment you click — before the confirm dialog "
            "— so if you then cancel, or there's nothing to do, your settings are "
            "already written even though nothing was ingested. An invalid setting "
            "blocks the action with an error instead of saving badly.\n\n"
            'Applying the settings to the corpus (the "reconcile" step) happens as '
            "part of actually ingesting/rebuilding: if you turned a graph layer OFF, "
            "its data is removed across the whole corpus. Because disabling a base "
            "layer cascades, turning entities off also removes the things built from "
            "them (ontology links and relationships). This wipe fires only when at "
            "least one document is actually processed — a cancelled or empty run saves "
            "your settings but doesn't wipe anything until the next real run. "
            "(Narrowing rather than turning off prunes in only a couple of cases — a "
            "shorter entity-types list, or disabling one of several ontologies; "
            "raising a threshold or turning xref materialization down does not "
            "retroactively remove existing data.)\n\n"
            "Turning a layer ON is the opposite: new documents get it immediately, but "
            "documents already in the corpus gain it only when you Re-ingest them or "
            "run that layer's backfill under Bulk operations."
        ),
        beginner=(
            "These settings on the right control how documents are processed for this "
            "corpus — what type they're labelled, how much of the knowledge graph is "
            "built, and which tools are used. Changing one here doesn't do anything by "
            "itself: a small dot shows you have unsaved changes, and Discard undoes "
            "them. (Your unsaved changes are also remembered if you close and reopen "
            "the app.)\n\n"
            "Your changes are saved the next time you run any ingest or any "
            "Bulk-operations button — and saving happens the instant you click, before "
            "the confirm dialog, so even if you cancel, the settings are already "
            "saved. A setting that's invalid stops the action with a message "
            "instead.\n\n"
            "The settings are actually applied to the corpus while it processes "
            "documents. The part to be aware of: if you switch a graph layer OFF, the "
            "data that layer built is removed for every document in the corpus — not "
            "only the ones you're adding now — and switching off a base layer also "
            "removes what was built from it (its vocabulary links and relationships). "
            "This removal only happens once a real run processes at least one "
            "document; a cancelled or empty run just saves the settings.\n\n"
            "Switching a layer ON works the other way: documents you add from now on "
            "include it, but documents already in the corpus only get it after you "
            "Re-ingest them (or run that layer's backfill in the Bulk operations "
            "section)."
        ),
        technical=(
            "The right pane is CorpusConfigEditor; edits are continuously persisted to "
            "a session sidecar (survive an app close/reopen, dot still lit) and become "
            "the corpus config only on save. Every ingest action AND every "
            "Bulk-operations button calls try_save_and_get_error() at click time — "
            "persisting the draft to corpus.toml and clearing the dirty dot BEFORE the "
            "plan/confirm dialog (invalid config → _show_invalid_config_dialog, "
            "nothing runs). So the save is committed at click, decoupled from ingest: "
            'cancelling the confirm, or an empty plan ("nothing to do"), leaves the '
            "config saved but nothing reconciled/ingested.\n\n"
            "Reconcile runs only INSIDE pipeline.ingest_document "
            "(reconcile_ontologies/entities/triples/cross_doc/cross_doc_xrefs_to_config, "
            "kg/reconcile.py), so it fires only when ≥1 doc is actually ingested — a "
            "cancelled/empty/move-or-delete-only run never reconciles. Bulk ops "
            "reconcile too: bulk_backfill_chunks/entities run ALL FIVE reconcilers (so "
            '"rebuild chunks" or "re-extract entities" can wipe OTHER now-disabled '
            "layers corpus-wide), the narrower backfills run their own.\n\n"
            "Destructive in one direction, fail-hard (a Cypher error propagates rather "
            "than half-reconciling). It wipes corpus-wide: ontology term nodes of "
            "disabled ontologies (+ cascade to :CANONICAL_TO/xref edges); all :Entity "
            "when entities off (cascading to :MENTIONS, :CANONICAL_TO, triple edges) OR "
            "entities whose entity_type left the kept set; all 15-predicate triple "
            "edges when triples off; all :RELATED_TO when cross_doc off; all "
            ":RELATED_BY_XREF when cross_doc_xrefs off. It PRUNES a still-enabled layer "
            "only for a narrowed entity_types set or a disabled-but-still-others "
            "ontology — it does NOT prune on a raised cross_doc[_xrefs] threshold, and "
            "there is NO reconcile helper for the xrefs setting (downgrading "
            "'use'→'collect_only'/'none' leaves existing :<X>_XREF edges until the "
            "ontology is disabled or you use Clear xref edges). It does not populate "
            "newly-enabled layers — those reach existing docs only via Re-ingest or "
            "the matching bulk_backfill_*."
        ),
    ),
    "select.selected_dataset": InfoText(
        title="Selected dataset",
        standard=(
            "This column is a read-only summary of the corpus that is "
            "currently selected (the one named in the header). It is a browse "
            "view: to change any of these settings you use the Ingest tab, not "
            "here. Which corpus you are looking at is chosen from the Corpus "
            "menu in the top bar.\n\n"
            "Identity.\n\n"
            "Name is the label you gave this corpus in the picker (renaming it "
            "changes only this label, not the folders on disk). Neo4j URI and "
            "Neo4j user are the address and username of this corpus's Neo4j "
            "database. LanceDB path is the on-disk folder holding this corpus's "
            "vector store (the chunk text and embeddings). corpus.toml is the "
            "path to this corpus's configuration file.\n\n"
            "Data.\n\n"
            "Counts shows docs, chunks, and mentions: how many documents and "
            "text chunks are in the vector store, and how many entity mentions "
            "(L6) are in the graph. Documents and chunks come from the vector "
            "store together; mentions comes from the graph separately, and each "
            "source shows a dash on its own if it cannot be read. Status breaks "
            "the documents down by their metadata state: baseline (no OpenAlex "
            "lookup, or not a Paper), pending (a DOI was found but not "
            "resolved), enriched (OpenAlex metadata was resolved), manual (you "
            "hand-edited it in the Documents list), and unknown (not recorded). "
            "Layers is a row of pills, one per pipeline layer (L1-L4, L5, L6, "
            "L7, L8, L9, L10); a pill is green when that layer is switched on "
            "for this corpus AND the corpus has at least one ingested document, "
            "and grey otherwise (off, or on but empty). Hover a pill to see "
            "which layer it is. Ontologies lists which L7 vocabularies are "
            "enabled (MeSH, GO, and so on) or 'none enabled'. Extractor shows "
            "the L6 entity extractor(s) in priority order (plus the Replace/Add "
            "mode and any custom types), or 'not configured'. Xrefs shows the "
            "L7 cross-ontology xref mode: none, collect_only, or use.\n\n"
            "Ingest settings.\n\n"
            "Chunking summarises how documents are split (strategy, the token "
            "cap, and whether small pieces are merged). OCR shows the two "
            "text-from-image toggles (pdf and image). Figures shows whether "
            "figure images are extracted, the render scale, and the minimum "
            "figure size kept.\n\n"
            "Models.\n\n"
            "Embedder is the active corpus's embedding model (provider, model, "
            "and vector dimension); it is per-corpus, pinned when the corpus "
            "was first ingested. LLM shows just the language-model provider, "
            "because the individual models (for the answer synthesizer, the "
            "Cypher builder, and so on) are set on the Settings tab.\n\n"
            "Changed since last ingest.\n\n"
            "This section appears only when you have edited the corpus's "
            "configuration on the Ingest tab but have not re-ingested yet. It "
            "lists each changed setting as old to new, so you can see what a "
            "re-ingest would apply. It stays hidden when there is nothing "
            "pending.\n\n"
            "The status line.\n\n"
            "Below the card, a status line reports the outcome of corpus "
            "management. Renaming, relocating, and removing a corpus are done "
            "from the top bar's Manage dialog (opened with the gear button), "
            "and their result lands here: for example 'renamed …', 'relocated "
            "…', or 'removed … from the corpus list', along with 'refreshed' "
            "when the view is refreshed, and any error if one of those could "
            "not be saved. It reads 'No recent corpus changes.' before "
            "anything has happened, and the last result stays until the next "
            "one. (Input errors like an empty or duplicate new name stay "
            "inside the rename dialog, not here.)"
        ),
        beginner=(
            "This whole column is a read-only summary of the corpus you have "
            "selected (named at the top). Think of it as the corpus's ID card: "
            "where its data lives, how much it holds, and how it is set up. You "
            "cannot change anything here; you edit settings on the Ingest tab. "
            "You pick which corpus to view from the Corpus menu in the top "
            "bar.\n\n"
            "Where it lives: the name you gave it, its Neo4j database address "
            "and user, the folder holding its searchable text, and its settings "
            "file.\n\n"
            "How much it holds: Counts is the number of documents, text chunks, "
            "and entity mentions. Status shows how many documents have had "
            "their reference metadata looked up (a dash means that part could "
            "not be read). Layers are little pills for each processing stage; "
            "green means that stage is turned on and the corpus has documents, "
            "grey means off or empty. Ontologies, Extractor, and Xrefs describe "
            "the knowledge-graph settings (which reference dictionaries, how "
            "entities are found, and how cross-dictionary links are "
            "handled).\n\n"
            "How it processes documents: Chunking, OCR, and Figures summarise "
            "how documents are split and whether images are read.\n\n"
            "Which models: the embedding model this corpus uses, and which AI "
            "provider answers questions.\n\n"
            "Changed since last ingest only shows up if you changed a setting "
            "on the Ingest tab but have not re-run ingestion yet; it lists what "
            "changed.\n\n"
            "The status line.\n\n"
            "Under the card is a status line that tells you the result of "
            "corpus housekeeping. You rename, move, or remove a corpus from "
            "the top-bar Manage dialog (the gear button), and the confirmation "
            "shows up here, such as 'renamed …' or 'removed … from the corpus "
            "list'. Before you have done anything it says 'No recent corpus "
            "changes.', and the last message stays until the next one."
        ),
        technical=(
            "Read-only card built by SelectDatasetTab._populate_info_card from "
            "the active CorpusEntry (GuiConfig), the loaded corpus.toml "
            "(CorpusConfig), and global Settings; config editing lives on the "
            "Ingest tab.\n\n"
            "Identity: info_name/uri/user/lancedb/toml come straight off the "
            "CorpusEntry (name, neo4j_uri, neo4j_user, lancedb_path, "
            "corpus_config_path).\n\n"
            "Counts (info_counts) is filled async by _reload_counts: docs = "
            "len(list_indexed_docs()), chunks = sum(n_chunks) from LanceDB "
            "(one shared read), mentions = kg_client.count_mentions() (a "
            "separate Neo4j read of :MENTIONS edges); each read degrades to '—' "
            "on its own. Status (info_breakdown) is a Counter over each doc's "
            "metadata_status (baseline | pending | enriched | manual, plus "
            "'unknown' for null). Layers (info_layers) renders one _badge per "
            "_LAYER_BADGES entry; green iff the layer is configured (LayerFlags "
            "attr, or _ONTOLOGY_ANY for L7) AND has_data (n_docs > 0 from the "
            "async count), grey otherwise; it starts all-grey (has_data=False) "
            "until the count returns. Ontologies/Extractor/Xrefs come from "
            "CorpusConfig via _ontologies_str / _extractor_str / "
            "cfg.layers.xrefs.\n\n"
            "Ingest settings: _chunking_str (chunker_strategy, chunk_max_tokens, "
            "merge_peers), _ocr_str (enable_pdf_ocr, enable_image_ocr), "
            "_figures_str (extract_figures, images_scale, min_figure_bytes) - "
            "all per-corpus CorpusConfig fields.\n\n"
            "Models: _populate_models reads get_settings(); Embedder = "
            "embedding_provider/model/dims, LLM = llm_provider. The embedder "
            "shown is the active corpus's because "
            "apply_active_corpus_embedding_to_env bridges the corpus.toml "
            "embedder into the env on startup + every switch (falls back to the "
            "global default only if the corpus file is missing/unparseable). "
            "Per-role LLM models are not shown (they live in Settings).\n\n"
            "Changed since last ingest (info_pending_section): config_diff("
            "baseline, draft) where baseline = the loaded corpus.toml and draft "
            "= the Ingest tab's persisted unsaved session draft; hidden when "
            "there is no draft or no diff. Refreshed live via "
            "refresh_pending_changes (wired to the editor's on_draft_changed).\n\n"
            "Status line: self.status (ft.Text), default _STATUS_IDLE, written "
            "directly (self.status.value = …), never auto-cleared. Writers are "
            "the corpus-management handlers, DEFINED here but invoked from the "
            "top-bar gear → Manage dialog (app.py _open_manage_dialog), not "
            "from the Select body: on_refresh_clicked ('refreshed'; also fired "
            "by create_new_dataset after a corpus is created), _rename_corpus "
            "(renamed / could-not-migrate-password / could-not-save-rename), "
            "on_relocate_clicked + _relocate_corpus (folder-picker error / "
            "no-corpus.toml / could-not-save-relocation / relocated), "
            "_remove_corpus (removed / could-not-save-remove). In-dialog "
            "validation (empty or duplicate name) stays in the rename dialog's "
            "own error text. The Documents pane's own Refresh button is a "
            "different handler (DocumentsView._on_refresh_clicked) that writes "
            "neither status line."
        ),
    ),
    "select.documents": InfoText(
        title="Documents",
        standard=(
            "This column lists every document in the selected corpus, newest "
            "first, as one card per document. The count next to the Documents "
            "title shows how many there are (or 'X of Y' while a filter is "
            "active), and reads 0 when the corpus is empty.\n\n"
            "Filter narrows the list as you type. It is a plain substring match "
            "over each document's title and its file path (so the filename "
            "works too), case-insensitive, and it filters the list already on "
            "screen without re-reading anything. Clear it to see everything "
            "again.\n\n"
            "Refresh re-reads the document list from the store (LanceDB). Use "
            "it after ingesting, deleting, or editing; the list also reloads on "
            "its own when you switch corpus or run an ingest. Refresh keeps "
            "whatever is typed in the filter.\n\n"
            "Each card shows a colour-coded status chip for the document's "
            "metadata state, then the title (or the filename if there is no "
            "title), the document type, the date it was ingested, the number "
            "of chunks (and figures, for documents with images), and the "
            "source file path.\n\n"
            "What the status chip means. baseline: no reference metadata was "
            "pulled in from OpenAlex, because the document is not a Paper, or "
            "the Resolve paper metadata layer was off, or it is a Paper but no "
            "DOI was found in its text; it has only the metadata read from the "
            "file itself. pending: a DOI was found in the document but its "
            "OpenAlex record was not resolved yet (not found, or the lookup did "
            "not complete), so it is eligible for enrichment but not enriched. "
            "enriched: the document's DOI was looked up on OpenAlex and its "
            "details (citations, authors, venue, topics) were pulled in. "
            "manual: you hand-edited the document's metadata in the Edit "
            "dialog, which also shields it from a corpus-wide Resolve unless "
            "you choose to include manually-edited documents.\n\n"
            "Each card has three actions: Edit (open the metadata editor), "
            "Re-ingest (re-run the full pipeline on the source file with the "
            "corpus's current settings), and Delete (remove the document from "
            "both stores).\n\n"
            "The status line.\n\n"
            "Below the Table contents row, a status line reports what the "
            "Re-ingest and Delete buttons are doing. It reads 'No re-ingest or "
            "delete running.' when idle, and otherwise shows progress and "
            "results: that a document is being re-ingested, that it was "
            "re-ingested, or why an action could not run (no source file is "
            "recorded, the source file was not found, a re-ingest is already "
            "running, the corpus.toml could not be loaded, or a delete "
            "failed). A successful delete shows no message; the card simply "
            "disappears. The last message stays until the next action."
        ),
        beginner=(
            "This is the list of documents in the corpus you have selected, "
            "newest first. Each document is a card, and the number by the "
            "Documents heading tells you how many there are (0 if the corpus is "
            "empty).\n\n"
            "Filter: type to narrow the list to documents whose title or "
            "filename contains what you typed. It searches the list already on "
            "screen (it does not re-download anything). Clear the box to see "
            "them all again.\n\n"
            "Refresh: re-read the list from storage, for after you have added, "
            "removed, or changed documents. The list also refreshes on its own "
            "when you switch corpus or ingest.\n\n"
            "Each card shows a coloured status tag, the title (or filename), "
            "the type, when it was added, how many chunks (and figures) it "
            "has, and where the file is.\n\n"
            "The status tag tells you where the document's reference details "
            "came from. baseline: nothing was looked up online (it is not a "
            "paper, the online-lookup layer was off, or no DOI was found in "
            "it), so it shows only what was read from the file. pending: a DOI "
            "was found but has not been looked up yet. enriched: the DOI was "
            "looked up online, so authors, venue, citations and so on were "
            "filled in. manual: you edited the details by hand (which also "
            "keeps a bulk look-up from overwriting them unless you ask it "
            "to).\n\n"
            "The three buttons are Edit (change its details), Re-ingest "
            "(process the original file again with the current settings), and "
            "Delete (remove it).\n\n"
            "The status line.\n\n"
            "Just under the Table contents row is a status line for the "
            "Re-ingest and Delete buttons. When nothing is happening it says "
            "'No re-ingest or delete running.'. When you use a button it tells "
            "you what is going on: that a document is being re-ingested, that "
            "it finished, or why it could not run (for example the original "
            "file is missing, or a re-ingest is already going). A successful "
            "delete just makes the card disappear. The message stays until you "
            "do the next action."
        ),
        technical=(
            "DocumentsView, the right pane of the Select tab. Data source: "
            "get_search_client().list_indexed_docs() (LanceDB) - one projection "
            "scan returns the denormalised doc rows plus per-doc n_chunks; "
            "lazy-loaded once per active corpus (guarded by _loaded_for), "
            "re-fetched on Refresh, corpus switch, or after an ingest/bulk-op "
            "(force=True). Rows sorted ingested_at desc.\n\n"
            "Filter (search_field): on_change calls _render_rows; "
            "_matches_filter lowercases and substring-matches the needle "
            "against (title + ' ' + source_path), so title and filename both "
            "hit. Client-side only, no re-fetch. coverage_text (drawn by the "
            "Select header, not here) shows '(N documents)' with no filter, "
            "'(X of Y documents)' with one, and '(0 documents)' when the corpus "
            "is empty.\n\n"
            "Refresh (refresh_button): _on_refresh_clicked → "
            "_schedule_reload(force=True) → reload(force=True), which re-reads "
            "list_indexed_docs and re-renders; the filter value is "
            "preserved.\n\n"
            "Cards (_render_doc_card): status chip coloured by metadata_status "
            "(_STATUS_COLORS; enriched/pending/baseline/manual, grey fallback), "
            "title or _basename(source_path), main+sub label, ingested date, "
            "chunk (+figure) counts, source_path. Actions: Edit "
            "(_open_edit_dialog metadata modal), Re-ingest (_do_reingest → "
            "pipeline.ingest_document with preserve_existing_labels=True, "
            "guarded by _op_busy + a source-file check), Delete "
            "(bulk_ops.delete_doc_plan → delete_doc_execute across LanceDB + "
            "Neo4j).\n\n"
            "Status line: op_status (ft.Text), initialised to _OP_STATUS_IDLE "
            "and thereafter written only through _set_op_status(msg), which "
            "stores msg or _OP_STATUS_IDLE so it is never blank. Re-ingest "
            "writes the pre-flight refusals (busy _op_busy guard, missing "
            "source_path, file-not-found, no active corpus, corpus.toml load "
            "failure), progress ('Re-ingesting …'), and outcome ('Re-ingested "
            "…' / 'Re-ingest of … failed: …'); _op_busy serialises re-ingests. "
            "Delete writes only failure / not-ok ('Delete failed: …' / 'Delete "
            "reported a problem …'); a successful delete writes nothing and "
            "relies on reload(force=True) dropping the card. The re-ingest and "
            "delete confirmations are separate modal AlertDialogs, not this "
            "line. Never auto-cleared."
        ),
    ),
    "select.documents_cards": InfoText(
        title="Cards and actions",
        standard=(
            "Each row in the list is one document, shown as a card. This "
            "explains what a card shows and what its three buttons do. The "
            "coloured status chip at the left of a card is the document's "
            "metadata state (baseline, pending, enriched, manual); the "
            "Documents (i) at the top of this column explains what each "
            "colour means.\n\n"
            "What a card shows.\n\n"
            "After the status chip: the title (or the file name, if the "
            "document has no title), then a line with the document type (for "
            "example Document Paper), the date it was ingested, and the chunk "
            "count (plus a figure count for documents that have images). Below "
            "that is the full path to the source file, which you can select "
            "and copy. All of these values are read from the vector store, so "
            "they match what search sees.\n\n"
            "The three buttons.\n\n"
            "Edit opens the metadata editor for that document (its own (i) "
            "explains every field).\n\n"
            "Re-ingest re-runs the whole ingest pipeline on the original file "
            "using the corpus's current settings (layers, extractors, "
            "ontologies) and replaces the existing version. It needs the "
            "source file still to be at the recorded path: if the path is "
            "missing or the file has moved, it does nothing and tells you so. "
            "Only one re-ingest runs at a time, and the document keeps its "
            "existing type across the re-ingest. Use it after you change "
            "corpus settings and want this document reprocessed to match.\n\n"
            "Delete removes the document from both stores (the vector store "
            "and the graph): its chunks, its entity mentions, and its "
            "reference metadata. It asks you to confirm first, showing the "
            "title and how many chunks will go. There is no undo; to bring the "
            "document back you re-ingest the file."
        ),
        beginner=(
            "Every document in the corpus is shown as a card. This note "
            "explains what is on a card and what its buttons do. The small "
            "coloured tag on the left is the document's status; the (i) next "
            "to the Documents heading tells you what each colour means.\n\n"
            "On the card you see: the title (or the file name if there is no "
            "title), the type of document, the date it was added, how many "
            "chunks (and figures) it has, and the location of the original "
            "file.\n\n"
            "Each card has three buttons.\n\n"
            "Edit lets you change the document's details (see the Edit dialog "
            "note for what each box does).\n\n"
            "Re-ingest processes the original file again from scratch, using "
            "the corpus's current settings, and replaces what was there. It "
            "only works if the original file is still where it was when you "
            "added it; if the file has moved or been deleted, the button just "
            "tells you it cannot find it. It runs one at a time.\n\n"
            "Delete removes the document completely, from both the search "
            "store and the knowledge graph. It asks you to confirm first and "
            "shows what will be removed. This cannot be undone; the only way "
            "back is to add the file again."
        ),
        technical=(
            "DocumentsView._render_doc_card. Card fields read from the "
            "list_indexed_docs row: metadata_status (chip, coloured by "
            "_STATUS_COLORS with a grey fallback), title or "
            "_basename(source_path), main_label + sub_label, "
            "_fmt_date(ingested_at), n_chunks (+ n_figures when nonzero), "
            "source_path (selectable). Every field comes from LanceDB, so the "
            "card and hybrid search stay consistent; the status-chip glossary "
            "is single-sourced in the select.documents entry.\n\n"
            "Edit: _open_edit_dialog (see select.documents_edit).\n\n"
            "Re-ingest: _on_reingest_clicked guards on _op_busy (one at a "
            "time); _open_reingest_confirm rejects an empty source_path or a "
            "Path(source_path) that is not a file (status message, no dialog); "
            "then _do_reingest loads load_corpus_config(corpus_config_path) "
            "and calls pipeline.ingest_document(path, config, main_label or "
            "'Document', sub_label, preserve_existing_labels=True). "
            "ingest_document is delete-then-write, so the doc is replaced, and "
            "preserve_existing_labels keeps the stored (main_label, "
            "sub_label). Reloads force=True after.\n\n"
            "Delete: _on_delete_clicked → _open_delete_dialog runs "
            "bulk_ops.delete_doc_plan(doc_id) (reads one chunk row for "
            "title/source_path/n_chunks; safe with n_chunks=0 if absent), "
            "shows plan.summary, and on confirm _do_delete → "
            "delete_doc_execute → pipeline.delete_doc. That wipes LanceDB "
            "chunks then the KG sequence delete_doc (L1-L4 focal "
            ":Document|:Artifact) → delete_chunks (L5) → delete_triples (L8) → "
            "delete_entities (L6 GC, cascading L7 :CANONICAL_TO via DETACH "
            "DELETE); every step runs even if an earlier one fails, ok = all "
            "succeeded. A failure or not-ok sets op_status and still reloads."
        ),
    ),
    "select.documents_edit": InfoText(
        title="Edit dialog",
        standard=(
            "The Edit dialog changes a document's reference metadata by hand. "
            "Open it with the Edit button on a card. It edits the stored "
            "metadata only; it never changes the document's text or its "
            "chunks.\n\n"
            "The fields.\n\n"
            "Title, Authors, Year, Venue, DOI, Source URL, and Language are "
            "free-text boxes, filled in with the current values. Authors is a "
            "single display string (for example Jane Doe; John Smith). Below "
            "them, OpenAlex ID is shown read-only for reference; it is filled "
            "in automatically when a DOI resolves and cannot be typed.\n\n"
            "Saving.\n\n"
            "Save writes all of these back and marks the document's status as "
            "manual, which also protects it from a corpus-wide Resolve unless "
            "you choose to include manually-edited documents. Save uses "
            "replace semantics for the text fields: whatever is in a box is "
            "written, so clearing a box clears that value. Year is the one "
            "exception: it only accepts a number, so leaving it blank or "
            "typing something non-numeric leaves the stored year unchanged "
            "(there is no way to clear a year here). Cancel closes the dialog "
            "and changes nothing.\n\n"
            "Look up DOI online.\n\n"
            "This checkbox (off by default) only does something if there is a "
            "DOI in the DOI box. With it checked, Save first stores your typed "
            "values, then looks that exact DOI up on OpenAlex. If the lookup "
            "succeeds it overwrites the metadata with OpenAlex's version "
            "(title, authors, venue, year, and so on), sets the status to "
            "enriched, and for a Paper it also refreshes the citations, "
            "authors, venue, and topics in the knowledge graph. So use the "
            "checkbox when you want OpenAlex's authoritative record, not to "
            "supplement your own edits: a successful lookup replaces "
            "everything you just typed. If the lookup finds nothing or fails, "
            "your manual edits stand and the status stays manual.\n\n"
            "Where your edits go, and updating the graph.\n\n"
            "A plain Save (checkbox off) updates the searchable metadata store "
            "only, not the knowledge graph. The graph's structured metadata "
            "(authors, venue, citations, topics) comes from OpenAlex, so a "
            "value you type by hand shows on the card and in search but does "
            "not change the graph. To bring the graph in line you re-resolve "
            "from OpenAlex: tick Look up DOI online before you save (for a "
            "Paper), or use Re-ingest on the card. Both rebuild the graph from "
            "the online record, so they replace rather than carry over your "
            "typed text; there is no way to push a purely hand-typed value "
            "into the graph."
        ),
        beginner=(
            "This dialog lets you fix or fill in a document's details by hand. "
            "You open it with the Edit button on a card. It changes only the "
            "saved details, never the document's actual text.\n\n"
            "The boxes are Title, Authors, Year, Venue, DOI, Source URL, and "
            "Language, each showing the current value. Authors is just one "
            "line of text (like Jane Doe; John Smith). OpenAlex ID underneath "
            "is shown for reference only and fills in by itself when a DOI is "
            "looked up; you cannot type in it.\n\n"
            "When you press Save, all the boxes are saved and the document is "
            "marked as edited by hand (which stops a bulk online look-up from "
            "overwriting your work unless you allow it). Whatever is in a box "
            "is what gets saved, so emptying a box clears that detail. The one "
            "exception is Year: it only takes a number, so leaving it empty or "
            "typing words leaves the old year as it was. Cancel closes without "
            "saving.\n\n"
            "The Look up DOI online checkbox only matters if the DOI box has a "
            "DOI in it. If you tick it and press Save, the app saves your text "
            "and then looks that DOI up online. If it finds the record, it "
            "fills the details in from that record (replacing what you typed) "
            "and marks the document enriched. If it finds nothing, your typed "
            "details stay. So tick it when you want the official online "
            "details, not when you want to keep your own wording.\n\n"
            "Good to know: a normal Save updates what search shows, but not "
            "the knowledge graph (the network of authors, venues, and "
            "citations). Those come from the online look-up, so a detail you "
            "type by hand appears on the card but not in the graph. To update "
            "the graph, tick Look up DOI online before saving (for a paper) or "
            "use Re-ingest on the card; both refill it from the online record "
            "rather than from what you typed."
        ),
        technical=(
            "_open_edit_dialog builds a modal from _EDIT_FIELD_SPEC: seven "
            "TextFields (title, authors_display, year, venue, doi, source_url, "
            "language) prefilled by _field_value, a read-only openalex_id "
            "Text, and the 'Look up DOI online' Checkbox (default False). "
            "authors_display is the denormalised display string, not the graph "
            ":Author nodes.\n\n"
            "Save (_save → _collect_edit_patch → _schedule_save → "
            "_save_metadata): _collect_edit_patch writes the six "
            "_EDIT_STRING_KEYS with strip() (blank clears to ''), writes year "
            "only when it parses as int (blank/garbage omitted, so year is "
            "never cleared), and always stamps metadata_status='manual'. "
            "_save_metadata calls search.update_doc_metadata(doc_id, patch), a "
            "LanceDB-only native partial update (chunk text + embeddings "
            "untouched, no Neo4j). Cancel just pop_dialog().\n\n"
            "Look up DOI online: _save passes lookup_doi only when "
            "checkbox.value and the DOI field is non-empty. _save_metadata "
            "then calls metadata_resolution.lookup_known_doi(doc_id, doi) "
            "AFTER the manual write. lookup_known_doi hits resolve_doi (no "
            "chunk-extraction fallback); on a hit, _apply_resolved_work "
            "re-patches the LanceDB metadata columns from OpenAlex "
            "(overwriting the just-typed values) and, for sub_label == Paper, "
            "does the surgical KG L1-L4 rewrite and sets status enriched; a "
            "miss or exception is non-fatal and leaves the manual save in "
            "place. Net: checkbox-off Save is LanceDB-only, and a hand-typed "
            "value never propagates to Neo4j (the graph's L1-L4 metadata is "
            "OpenAlex-sourced), so the graph is reconciled only by a "
            "successful Paper lookup or a re-ingest. Then reload(force=True)."
        ),
    ),
    "manage.corpus": InfoText(
        title="Manage corpus",
        standard=(
            "This dialog manages your corpora. It opens from the gear button "
            "in the top bar. At the top is a read-only summary of the active "
            "corpus (the same fields the Selected dataset (i) explains); below "
            "it are the tools.\n\n"
            "Actions (these apply to the active corpus).\n\n"
            "Rename changes only the label shown in the corpus picker. It does "
            "not rename the corpus folder, its corpus.toml, the LanceDB "
            "folder, or the Neo4j database on disk. Relocate repoints the app "
            "at a corpus you moved to a new folder: you pick the folder, which "
            "must contain the corpus.toml, and no re-ingest happens (the data "
            "moved with the folder). Refresh re-reads this card from your "
            "saved settings.\n\n"
            "All corpora.\n\n"
            "This lists every corpus registered in the app. Each row shows the "
            "name, an (active) tag on the one in use, and a (missing) tag when "
            "the corpus's corpus.toml is no longer on disk (for example after "
            "you delete its folder). Remove unregisters that corpus: it clears "
            "the entry and its stored Neo4j password, but never deletes "
            "anything on disk (the Neo4j database, the LanceDB folder, and the "
            "corpus.toml are left as they are). For a corpus flagged (missing) "
            "it just clears the stale entry. Remove works by name, so you can "
            "prune any corpus without selecting it first."
        ),
        beginner=(
            "This window is where you look after your corpora (your document "
            "collections). You open it with the gear button at the top. It "
            "shows details of the corpus you are using now, and tools to "
            "manage all of them.\n\n"
            "The buttons act on the corpus you are using now. Rename just "
            "changes the name shown in the list; it does not rename any files "
            "or folders on your computer. Relocate is for when you have moved "
            "a corpus's folder somewhere else: you point the app at the new "
            "folder, which must still contain its corpus.toml file. Refresh "
            "re-reads the details shown here.\n\n"
            "Under All corpora is a list of every corpus the app knows about. "
            "Each one shows its name, an (active) tag if it is the one in use, "
            "and a (missing) tag if its corpus.toml file is gone from your "
            "computer (for example you deleted the folder). Remove takes a "
            "corpus off this list. It does not delete any of your files (the "
            "database, the search folder, and the corpus.toml all stay); it "
            "just makes the app forget the corpus. For a corpus marked "
            "(missing), Remove simply clears the leftover entry. You can "
            "remove any corpus straight from the list, without switching to it "
            "first."
        ),
        technical=(
            "GuiApp._open_manage_dialog. The card is build_corpus_card(app): "
            "read-only and static (see select.selected_dataset). The action "
            "buttons reach the SelectDatasetTab handlers through _act, which "
            "pops this dialog first so the action's own dialog or picker does "
            "not stack: Rename = on_rename_clicked (relabels the CorpusEntry, "
            "migrates the keyring entry), Relocate = on_relocate_clicked "
            "(async folder picker, rewrites lancedb_path + corpus_config_path, "
            "requires a corpus.toml in the chosen folder), Refresh = "
            "on_refresh_clicked (re-populates the card).\n\n"
            "All corpora: one row per self.gui_config.corpora entry. (active) "
            "= c.name == active_corpus_name; (missing) = not "
            "Path(c.corpus_config_path).is_file(). Remove routes through _act "
            "→ select_tab.open_remove_confirm(name, missing) → "
            "_remove_corpus(name), which filters the entry out of the "
            "registry, deletes its keyring password (ignored if absent), "
            "reassigns the active corpus to the first remaining (or None) when "
            "the removed one was active, saves, re-bridges env, and calls "
            "refresh_after_corpus_change() (which re-syncs the top-bar "
            "dropdown). It never reads or deletes the corpus folder, so a "
            "(missing) corpus removes cleanly. Removal is by name, independent "
            "of which corpus is active."
        ),
    ),
    "search.chat": InfoText(
        title="Chat",
        standard=(
            "This is where you chat with your corpus. Type a question on the "
            "left, and the answer with its sources appears in the panel on the "
            "right; the chat itself keeps the running conversation and short "
            "status lines.\n\n"
            "Asking.\n\n"
            "Type in the box and press Enter to send (Shift+Enter inserts a "
            "newline for a multi-line question). Send does the same as Enter. "
            "In conversational mode a lightweight router first decides whether "
            "to answer directly, ask you to clarify, or run a retrieval; it "
            "distils your wording into the actual search. While a search runs "
            "the input, Send, Save, and Clear are disabled and Stop is "
            "enabled.\n\n"
            "The buttons.\n\n"
            "Send submits your question. Stop cancels a search that is "
            "running. Clear empties the conversation (and the answer history "
            "on the right). Save writes the transcript to a folder as "
            "Markdown, text, or Word (a chat has no JSON form); the first save "
            "asks for a folder and remembers it.\n\n"
            "What shows where.\n\n"
            "Your messages and the assistant's conversational replies appear "
            "as bubbles here, along with a short status line after each answer "
            "(how many text-chunk and knowledge-graph sources it used). The "
            "full answer and its sources render in the right panel, not in the "
            "chat."
        ),
        beginner=(
            "This is the chat where you ask questions about your documents. "
            "You type on the left; the answer and where it came from show up "
            "in the panel on the right. The chat keeps the back-and-forth and "
            "short notes.\n\n"
            "To ask something, type in the box and press Enter (hold Shift and "
            "press Enter if you want a new line in a longer question). The Send "
            "button does the same thing. While the app is working on your "
            "question, the box and the Send, Save, and Clear buttons are "
            "greyed out, and only Stop is active.\n\n"
            "The four buttons: Send asks your question. Stop cancels a search "
            "in progress. Clear wipes the conversation so you can start fresh. "
            "Save writes the conversation to a file (Word, Markdown, or plain "
            "text) in a folder you choose the first time.\n\n"
            "Good to know: your questions and the assistant's short replies "
            "show here as bubbles, with a small note saying how many sources "
            "each answer used. The full answer and its sources appear in the "
            "panel on the right, not in the chat."
        ),
        technical=(
            "ChatPanel (gui/chat_panel.py), the left column of the Search tab; "
            "the four actions call back into GuiApp. input_field is a "
            "multiline TextField (shift_enter=True, so Enter triggers on_send "
            "and Shift+Enter inserts a newline). set_busy(True) at the start "
            "of on_send disables input/Send/Save/Clear and enables Stop (all "
            "re-enabled in the finally).\n\n"
            "Send (on_send): no-op if already busy or empty; preflight checks "
            "(get_settings, active-provider key, corpus.toml) each surface a "
            "system line instead of crashing. In input_mode 'conversational' "
            "it runs get_chat_router (clarify-vs-retrieve + query "
            "distillation); a not-ready result just replies. Retrieval loads "
            "the graph via asyncio.to_thread and awaits graph.ainvoke; the "
            "AgentAnswer goes to right_panel.push_answer, and only a chunk/KG "
            "source count is appended to the chat.\n\n"
            "Stop (on_stop): cancels _send_task (on_send's own task); "
            "CancelledError propagates through the router/graph, and the outer "
            "handler appends 'search cancelled' and re-raises. Clear "
            "(on_clear): clears messages + last_answer/query and resets the "
            "placeholder and the right-panel history. Save (on_save_chat): "
            "save_chat to chat_dir (picker once, then remembered), formats "
            "filtered to CHAT_FORMATS (md/txt/docx; no JSON).\n\n"
            "Message rendering: append_user/assistant/system draw bubbles into "
            "chat_output (auto-scroll Column; placeholder cleared on first "
            "message). A chat-router failure rolls back via messages.pop() + "
            "pop_last() + set_input(text), so the transcript and messages stay "
            "in sync and the question is restored for retry."
        ),
    ),
    "search.view": InfoText(
        title="View result",
        standard=(
            "This is the result display for the Search tab. It shows the answer "
            "from your most recent question, an answer file you opened, or a "
            "placeholder before you have asked anything. An answer renders as "
            "formatted text with its text-chunk and knowledge-graph sources "
            "listed below; if it cited any figures, they appear as thumbnails "
            "you can click to open full size.\n\n"
            "Save Result and Open Result.\n\n"
            "Save Result writes the shown answer to a folder in the formats you "
            "chose (Markdown, text, Word, or JSON); the first save asks for a "
            "folder and remembers it. Open Result loads a previously saved .md "
            "or .txt answer back into this view, either through the file picker "
            "or by pasting its path into the field beside the button and "
            "pressing Enter.\n\n"
            "The pager.\n\n"
            "The app keeps the last five things you viewed here (answers and "
            "opened files). The < n/N > controls flip back and forth through "
            "them, and the red X removes the one on screen. The pager starts "
            "empty on each launch (it is not saved between sessions), and Clear "
            "empties it."
        ),
        beginner=(
            "This panel shows the result of your search. After you ask a "
            "question, the answer appears here with a list of where it came "
            "from (the text passages and knowledge-graph facts it used), and "
            "any pictures it referred to show as small thumbnails you can click "
            "to enlarge. Before you ask anything, it shows a short "
            "placeholder.\n\n"
            "Save Result saves the answer on screen to a folder on your "
            "computer, as Word, Markdown, plain text, or JSON. The first time, "
            "it asks which folder to use and then remembers it. Open Result "
            "does the opposite: it loads a saved answer file (.md or .txt) back "
            "into this panel, either by picking the file or by pasting its path "
            "into the box next to the button and pressing Enter.\n\n"
            "The little < n/N > arrows and the red X are a short history: the "
            "app remembers the last five things you looked at here (answers and "
            "opened files), the arrows step back and forth through them, and "
            "the X removes the one you are looking at. This history is not kept "
            "after you close the app, and the Clear button empties it."
        ),
        technical=(
            "RightPanel View sub-tab (gui/right_panel.py). view_container "
            "renders one of three, rebuilt on each navigation: an answer "
            "(LatestView: render_answer_markdown + a figure thumbnail gallery "
            "for chunk_sources with content_type='figure' and an on-disk "
            "image_ref, click-to-expand modal), an opened file (FileView: .txt "
            "monospace, else themed_markdown), or the empty placeholder.\n\n"
            "Save Result = on_save_answer (save_answer(last_answer, "
            "last_query, results_dir, save_formats)); Open Result = "
            "on_open_result (md/txt picker); the paste-path field = "
            "on_open_path (.md/.txt + is_file check). Both load via "
            "_load_file_into_view then push_file. Formats and the default "
            "folders live in Settings.\n\n"
            "Pager: an in-memory list of up to _MAX_HISTORY=5 _ViewItem slots "
            "(MODE_LATEST answers + MODE_FILE files), snapshotted at push time. "
            "push_answer/push_file append and jump to the newest; < and > "
            "(_on_prev/_on_next) walk the cursor (greyed at the ends); the X "
            "(_on_close_item) drops the shown slot (and now clears "
            "app.loaded_file when it was the loaded file). History is not "
            "persisted; reset_history (Clear) empties it, re-seeding a kept "
            "loaded_file."
        ),
    ),
    "installs.overview": InfoText(
        title="Installs",
        standard=(
            "The one place to set up the optional pieces the app can use: LLM "
            "providers, embedding providers, document parsers, entity extractors, "
            "and ontologies. Nothing here is bundled, so you install only what a "
            "corpus needs.\n\n"
            "Installing or uninstalling a package (a provider, parser, or extractor "
            "adapter) needs an app restart to take effect. Downloading or deleting "
            "files (extractor weights, ontology source files) does not: those are "
            "read on the next ingest. Each section below has its own help icon with "
            "the details."
        ),
        beginner=(
            "This tab is where you add or remove the optional add-ons the app can "
            "use. None come pre-installed, so you set up only the ones you actually "
            "need.\n\n"
            "There are five kinds: LLM providers (the models that reason and "
            "answer), embedding providers (turn text into searchable vectors), "
            "parsers (read extra file types like audio or code), entity extractors "
            "(find names and terms in documents), and ontologies (curated "
            "vocabularies).\n\n"
            "Rule of thumb for restarts: adding or removing a software package "
            "needs a restart; downloading or deleting data files (model weights, "
            "ontology files) does not. Each section's icon explains its own case."
        ),
        technical=(
            "The global install surface. Adapters ship as opt-in pip extras "
            "(llm-*, embed-*, parsers-*, entities-*); this is the only GUI path "
            "that runs pip install/uninstall. The active-provider choice lives in "
            "the LLMs and Embedding tabs, which read install state to offer only "
            "installed providers.\n\n"
            "Restart contract: pip install/uninstall returns restart_required and "
            "the app must restart to load or release the module. File operations "
            "(extractor weights via HF snapshot, ontology source files) mutate disk "
            "only and are picked up at next ingest, no restart. The editable "
            "settings here (Ollama base URL, ontology_downloads_dir) bridge to env "
            "and call reset_after_settings_change(), so they apply in-process "
            "without a restart. Each installable uses the lifecycle plan/execute "
            "pattern behind a confirm dialog."
        ),
    ),
    "installs.llm_providers": InfoText(
        title="LLM providers",
        standard=(
            "Install or uninstall each LLM provider's adapter (a pip package): "
            "Anthropic, OpenAI, Google, and Ollama (local). Nothing is bundled, so "
            "install only what you use. Both install and uninstall need an app "
            "restart to take effect (the status line says so). The active provider "
            "cannot be uninstalled.\n\n"
            "You choose which model each step uses in the LLMs tab (under Search), "
            "not here.\n\n"
            "Ollama is local and has three parts: the adapter (installed here), the "
            "daemon (a separate program from ollama.com that must be running; the "
            "line under the Ollama row checks it), and the models (pulled on the "
            "command line with 'ollama pull', not in this app). The box shows where "
            "Ollama stores models on disk. The Ollama base URL is where the app "
            "reaches the daemon; editing it applies immediately, no restart."
        ),
        beginner=(
            "This is where you set up the AI providers whose models answer your "
            "questions. None come pre-installed, so add the one(s) you have an "
            "account for: Anthropic, OpenAI, Google, or Ollama (which runs models "
            "locally on your own machine).\n\n"
            "Press Install to add a provider's software, or Uninstall to remove it. "
            "Either way, restart the app afterwards so the change takes effect. You "
            "cannot uninstall the provider currently in use.\n\n"
            "Ollama takes a few more steps because it runs locally: install its "
            "adapter here, install and start the Ollama program from ollama.com, and "
            "download the models by typing 'ollama pull <model>' in a terminal. The "
            "Ollama base URL is the address the app uses to reach it; the line under "
            "the row tells you whether it is reachable.\n\n"
            "You pick which model does what in the LLMs tab (under Search), not here."
        ),
        technical=(
            "Rows install/uninstall the pip adapter for each provider in "
            "LLM_PROVIDER_REGISTRY via llm_lifecycle (plan/execute). Both set "
            "restart_required on success; the app must restart to load or release "
            "langchain-<provider>. Uninstalling the active provider "
            "(settings.llm_provider) is blocked in the lifecycle, and the GUI greys "
            "the Uninstall button for any provider a node model uses or the active "
            "one.\n\n"
            "Ollama is three independent parts: adapter (langchain-ollama, pip, "
            "here), daemon (external binary from ollama.com, reachability probed "
            "async and shown in the status line), and model weights (ollama pull on "
            "the CLI; the backend has pull ops but they are not wired into this "
            "GUI). The read-only box resolves the Ollama models directory "
            "(OLLAMA_MODELS env or the platform default). The base URL field "
            "persists to GuiConfig, bridges via apply_llm_to_env (OLLAMA_BASE_URL), "
            "and calls reset_after_settings_change(), so it applies in-process; "
            "empty is rejected."
        ),
    ),
    "installs.embedding_providers": InfoText(
        title="Embedding providers",
        standard=(
            "Install or uninstall each embedding provider's adapter (a pip "
            "package): Voyage, OpenAI, Google, and HuggingFace. Both install and "
            "uninstall need an app restart to take effect. The active embedder "
            "cannot be uninstalled; switch it in the Embedding settings first.\n\n"
            "You choose the active embedder and model in the Embedding settings, "
            "not here. Only HuggingFace downloads models to your machine (shown in "
            "the 'on disk' box); Voyage, OpenAI, and Google are remote APIs."
        ),
        beginner=(
            "Embedding providers turn your document text into the numeric vectors "
            "that make search work. This is where you install or remove them. None "
            "come pre-installed.\n\n"
            "Press Install to add one, Uninstall to remove it, and restart the app "
            "afterwards. You cannot uninstall the embedder you are currently using; "
            "change it in the Embedding settings first.\n\n"
            "Three of them (Voyage, OpenAI, Google) run in the cloud, so there is "
            "nothing to download. HuggingFace runs on your machine and downloads its "
            "model the first time it is used; the box shows where those files live."
        ),
        technical=(
            "Rows install/uninstall the pip adapter for each provider in "
            "EMBEDDER_PROVIDER_REGISTRY via embedder_lifecycle (plan/execute); both "
            "set restart_required on success. Uninstalling the active embedder "
            "(settings.embedding_provider) is blocked in the lifecycle.\n\n"
            "Voyage/OpenAI/Google are remote (require an API key, no local model "
            "op). Only HuggingFace downloads weights (to the shared HF Hub cache, "
            "the read-only box); it auto-downloads on first use, so there is no "
            "explicit weight-download button here. The active embedder and model "
            "are set per corpus in the Embedding settings."
        ),
    ),
    "installs.parsers": InfoText(
        title="Parsers",
        standard=(
            "Optional Docling parser extras. Install or uninstall each as a pip "
            "package; both need an app restart to take effect.\n\n"
            "ASR (audio and video transcription): installing pulls openai-whisper "
            "plus a bundled ffmpeg. The Whisper model weights (about 1.5 GB) are "
            "downloaded by Docling on first ingest use, not here; the box shows "
            "where they land. AST-aware code parsing ships its grammars inside the "
            "pip package, so nothing extra to download."
        ),
        beginner=(
            "Parsers let the app read extra kinds of files. There are two optional "
            "ones: transcription of audio and video, and structure-aware parsing of "
            "source code. Neither is installed by default.\n\n"
            "Press Install to add one, Uninstall to remove it, and restart the app "
            "afterwards. The transcription parser downloads its speech model "
            "automatically the first time you actually ingest audio or video (about "
            "1.5 GB); the box shows where it is stored. The code parser needs no "
            "separate download."
        ),
        technical=(
            "Rows install/uninstall the parser pip extra via parser_lifecycle "
            "(plan/execute); both set restart_required on success. The ASR extra "
            "installs openai-whisper plus imageio-ffmpeg; Whisper Turbo weights are "
            "fetched by Docling on first ASR ingest into the shared HF Hub cache "
            "(the read-only box), not by this tab. The code parser's tree-sitter "
            "grammars ship in-wheel, so the pip install (and its restart) is all "
            "that is needed."
        ),
    ),
    "installs.entity_extractors": InfoText(
        title="Entity extractors",
        standard=(
            "The L6 named-entity extractors: GLiNER, GLiNER-BioMed, and HunFlair2. "
            "Each needs two things: its adapter (a pip package) and its model "
            "weights (downloaded separately).\n\n"
            "Install/Uninstall handle the adapter and need an app restart. Download "
            "weights / Delete download handle the model files and do NOT need a "
            "restart (they are read at ingest time). There is no auto-download: if "
            "the weights are missing, extraction fails, so download them before "
            "ingesting. The box shows where weights are stored.\n\n"
            "LLM-based extraction is not listed here; it uses your installed LLM "
            "provider, and you pick its model in Ingest."
        ),
        beginner=(
            "These find named things (people, places, genes, and so on) in your "
            "documents. There are three, and each has two parts to set up: the "
            "software (the adapter) and the actual model files (the weights).\n\n"
            "Use Install and Uninstall for the software, then restart the app. Use "
            "Download weights and Delete download for the model files; those take "
            "effect right away, no restart. Important: the weights do not download "
            "automatically, so download them before you ingest, or extraction will "
            "fail. The box shows where they are kept.\n\n"
            "There is also an LLM-based option that is not shown here, because it "
            "just uses whichever LLM provider you already installed; you choose it "
            "in the Ingest settings."
        ),
        technical=(
            "Two independent axes per row. Adapter: install/uninstall the pip extra "
            "via extractor_lifecycle (plan/execute), restart_required on success. "
            "Weights: download/delete via the HF snapshot ops; those results carry "
            "no restart_required (files on disk, loaded at inference, so no "
            "restart). No auto-download at first inference; extraction raises if "
            "weights are absent. The read-only box resolves the HF Hub cache. "
            "LLM-based extraction is excluded from this list (it dispatches through "
            "the installed LLM provider; model chosen in the Ingest/corpus config)."
        ),
    ),
    "installs.ontologies": InfoText(
        title="Ontologies",
        standard=(
            "Download or delete each ontology's source file(s) on disk. No restart "
            "is needed either way: the Neo4j term nodes are written during ingest, "
            "not by these buttons, so downloading is safe (no schema change), and "
            "deleting a download leaves any existing nodes untouched.\n\n"
            "ontology_downloads_dir sets where the files are saved; leave it blank "
            "to use the backend default. The Browse button picks a folder, and the "
            "'on disk' line shows the path actually in use. Editing this applies "
            "immediately, no restart."
        ),
        beginner=(
            "Ontologies are curated vocabularies (for genes, diseases, chemicals, "
            "and so on) the app can use during ingest. Here you just download their "
            "source files to disk, or delete them to free space. Neither needs a "
            "restart.\n\n"
            "Downloading a file does not change your data on its own; the terms only "
            "enter the knowledge graph when you next ingest with that ontology "
            "enabled. Deleting a download does not remove anything already in the "
            "graph.\n\n"
            "The ontology_downloads_dir box is where the files are saved. Leave it "
            "blank to use the default location, or Browse to choose your own; the "
            "line below shows where they actually go."
        ),
        technical=(
            "Download/delete source files via kg/ontologies/lifecycle "
            "(plan/execute); neither result carries restart_required (disk-only; "
            "Neo4j writes happen at ingest, not here). ontology_downloads_dir "
            "persists to GuiConfig, bridges via apply_ontology_downloads_dir_to_env "
            "(blank deletes the env var so the backend default wins), and calls "
            "reset_after_settings_change(), so it applies in-process without a "
            "restart. The effective-path box echoes the resolved location (override "
            "when set, backend default when blank)."
        ),
    ),
    "keys.overview": InfoText(
        title="Keys",
        standard=(
            "Your provider API keys, one field per provider: Anthropic, OpenAI, "
            "Google, Voyage, and the optional LangSmith. Fill in only the ones the "
            "providers you actually use need; none is required.\n\n"
            "Keys are stored in your operating system's secure keyring (Windows "
            "Credential Manager, macOS Keychain, or Linux Secret Service), never "
            "written to disk in plain text. Each field saves on its own when you "
            "click away from it, and the new key takes effect immediately, with no "
            "restart. The eye button shows or hides a key, and the '(saved)' or "
            "'(not set)' tag by each label shows whether a key is already stored.\n\n"
            "Leaving a field blank keeps whatever key is already saved; clearing a "
            "field does not delete it. To remove a key, use the delete button next "
            "to it (it asks you to confirm).\n\n"
            "Voyage is used only for embeddings and Anthropic only for the chat "
            "models; the OpenAI and Google keys serve both chat and embeddings. "
            "LangSmith is different: it is optional and only used to trace "
            "evaluation runs (in the Evaluation Run tab), so leave it blank if you "
            "do not trace. Neo4j database passwords are not set here: they are per "
            "corpus, set when you create a corpus in the Library tab."
        ),
        beginner=(
            "These are the keys (like passwords) that let the app use AI providers "
            "on your behalf. There is one box per provider: Anthropic, OpenAI, "
            "Google, Voyage, and LangSmith. Fill in only the ones for services you "
            "actually use, so it is normal to leave several blank.\n\n"
            "Your keys are kept in your computer's built-in secure store (the same "
            "place other saved passwords live), not in a plain file. Type a key and "
            "click elsewhere and it saves by itself, right away. The small eye "
            "button lets you check what a key is, and each label says '(saved)' or "
            "'(not set)' so you can see which keys are in place.\n\n"
            "Leaving a box empty keeps the key you already saved; emptying a box "
            "does not remove it. To actually remove a saved key, use the delete "
            "button beside it and confirm.\n\n"
            "A few specifics: Voyage is only for turning text into search vectors, "
            "and Anthropic is only for the chat models; the OpenAI and Google keys "
            "are used for both. LangSmith is different: it is only for recording "
            "evaluation runs, so you can ignore it otherwise. The password for your "
            "Neo4j database is not set here; you set that when you create a corpus "
            "in the Library tab."
        ),
        technical=(
            "Five keyring-backed secrets (anthropic, openai, google, voyage, "
            "langsmith), stored via the OS keyring under the app's service name, "
            "never on disk. Each field saves on blur: a value unchanged from the "
            "stored one, or an empty field, is a no-op. On a real change it writes "
            "the keyring, then apply_keys_to_env bridges the value to the matching "
            "env var (ANTHROPIC_API_KEY, and so on) and reset_after_settings_change "
            "drops the cached Neo4j, LLM, and embedder singletons that captured the "
            "old key, so the new key applies in-process without a restart (if that "
            "cache reset fails, the key is saved but a restart is needed before it "
            "is picked up).\n\n"
            "Keys are validated lazily (at first use), never at startup, so an "
            "unused provider's key stays blank without error. LangSmith is "
            "auth-only: the key is bridged, but LANGSMITH_TRACING is never set "
            "globally; tracing opts in per evaluation run (the Run tab's trace "
            "checkbox, or ka eval --trace) through a context manager. Voyage is "
            "embedder-only and Anthropic is LLM-only; the OpenAI and Google keys "
            "serve both their chat and embedding endpoints.\n\n"
            "Emptying a field is a no-op (so an accidental clear never wipes a "
            "stored key); deletion is an explicit action via the per-key delete "
            "button, which calls set_api_key with an empty value (deletes the "
            "keyring entry), clears that key's env var, and resets the caches, so "
            "it takes effect without a restart. The Neo4j password is not managed "
            "here: it is per corpus (keyring neo4j-<corpus>), set in the Library tab."
        ),
    ),
    # ---- Settings tab (AppTab) ----
    "settings.save_results": InfoText(
        title="Save results",
        standard=(
            "Sets where and in which format(s) the Search tab's Save Result button "
            "writes an answer. Tick any of Markdown (.md), Plain text (.txt), Word "
            "(.docx), or JSON (.json), and Save Result writes one file per ticked "
            "format (JSON holds the answer data on its own). At least one format "
            "always stays ticked. Browse chooses the default folder shown here; when "
            "no folder is set, the first Save Result asks for one and remembers it. "
            "Save Chat, in the next section, has its own separate formats and folder."
        ),
        beginner=(
            "This is where your saved answers go, and in what file types. When you "
            "press Save Result on the Search tab, the app writes the answer into the "
            "folder shown here, making one file for each type you have ticked. "
            "Markdown and Word open as nicely formatted documents, plain text is just "
            "the words, and JSON is a data format that other programs can read. You "
            "can tick more than one, and one always has to stay ticked so there is "
            "something to save. Press Browse to choose the folder; if you never "
            "choose one, the first save will ask you to pick a folder and then "
            "remember it. Saving your chat is set up separately, just below."
        ),
        technical=(
            "Drives GuiConfig.save_formats (default ['md']) and results_dir. The "
            "checkboxes come from ANSWER_FORMATS (md, txt, docx, json) with "
            "FORMAT_LABELS. on_save_format_changed persists the ticked set in "
            "canonical order, refuses an empty set (it re-ticks the prior "
            "selection), and rolls back on a ConfigError. Browse calls the OS folder "
            "picker (file_picker.get_directory_path) and persists results_dir, also "
            "with rollback. The Save Chat section is a separate chat_save_formats / "
            "chat_dir pair, and the same artifacts.py renderers are shared by the CLI."
        ),
    ),
    "settings.save_chat": InfoText(
        title="Save chat",
        standard=(
            "Sets where and in which format(s) the Search tab's Save Chat button "
            "writes the full conversation transcript. These are its own settings, "
            "separate from Save Result above. Tick any of Markdown (.md), Plain text "
            "(.txt), or Word (.docx). A transcript has no JSON form, so JSON is not "
            "offered here. At least one format always stays ticked. Browse chooses "
            "the default folder; when none is set, the first Save Chat asks for one "
            "and remembers it."
        ),
        beginner=(
            "This is the same idea as Save results just above, but for saving the "
            "whole back-and-forth conversation instead of a single answer. Pick the "
            "folder and the file types you want, then press Save Chat on the Search "
            "tab. You get Markdown, plain text, or Word here. There is no JSON "
            "option, because a conversation is not stored as data the way a single "
            "answer is. One type always stays ticked, and Browse chooses the folder "
            "(the app asks the first time if you have not set one). These choices are "
            "kept separate from the Save results ones, so you can send answers and "
            "chats to different places."
        ),
        technical=(
            "Drives GuiConfig.chat_save_formats (default ['md']) and chat_dir, "
            "independent of the Save results pair. The checkboxes come from "
            "CHAT_FORMATS (md, txt, docx, no json). on_chat_format_changed persists "
            "the ticked set, keeps at least one, and rolls back on ConfigError. "
            "on_chat_browse_folder persists chat_dir via the OS folder picker with "
            "rollback. A legacy config with no chat_* keys inherits the results_dir "
            "and save_formats values once (the _mirror_chat_from_results validator), "
            "after which the two sets diverge freely."
        ),
    ),
    "settings.app_behaviour": InfoText(
        title="App behaviour",
        standard=(
            "Five preferences for how the app starts and how much help it shows. "
            "Restore last used dataset on startup: reopen the corpus you last used "
            "when the app launches, instead of starting with none. Keep loaded file "
            "when clearing chat: when you press Clear, leave any answer file you had "
            "opened on screen rather than clearing it too. The three help-icon "
            "toggles turn the (i) help tiers on or off across the whole app: standard "
            "(blue) is the everyday note, beginner (green) adds plain-language help "
            "for new users, and technical (orange) adds power-user detail such as "
            "environment variables and backend behaviour. Each tier is independent, "
            "and the sample icon beside each toggle shows what it looks like. All "
            "three off gives a clean, icon-free interface."
        ),
        beginner=(
            "A few switches for how the app behaves. The first, restore last used "
            "dataset on startup, means that when you open the app it goes straight "
            "back to the collection of documents (a corpus) you were last working "
            "with, so you do not have to pick it again. The second, keep loaded file "
            "when clearing chat, means that when you press Clear to tidy up, a "
            "document you had opened stays on screen instead of disappearing with "
            "everything else. The last three switches control these little help icons "
            "themselves. You are reading one right now. Standard (blue) is the normal "
            "help, beginner (green) is this friendlier kind written for people new to "
            "the tool, and technical (orange) is extra detail for advanced users. "
            "Turn on whichever mix you like; the small coloured icon next to each "
            "switch shows what it looks like. Turn all three off and the help icons "
            "disappear for a clean screen."
        ),
        technical=(
            "Each checkbox auto-saves one GuiConfig flag with rollback on "
            "ConfigError. restore_last_corpus (default True): on startup the GUI "
            "loads corpus_config_path, falling back to <cwd>/corpus.toml; when False "
            "it skips the explicit path, so you start with no corpus unless one sits "
            "in the working directory. keep_loaded_file_on_clear (default True): "
            "Clear leaves an opened answer file mounted. show_info_icons (default "
            "True), show_beginner_info (default False), and show_technical_info "
            "(default False) gate the three (i) tiers; their handlers also call "
            "app.set_info_icons_visible(tier, on), so every registered icon of that "
            "tier flips live without a restart. The tier widgets and their gating "
            "live in info_icon.py, and the help strings live in this info_text.py "
            "registry."
        ),
    ),
    "settings.diagnostics": InfoText(
        title="Diagnostics",
        standard=(
            "Tools for seeing what the app is doing under the hood. Show diagnostic "
            "info in chat: when on, each query adds one extra line to the chat just "
            "before it runs, showing the input mode, the retrieval mode, and the "
            "exact search query being sent. When off, that line is hidden and the "
            "chat shows only the normal 'answer ready' summary. Below the toggle, the "
            "System health board checks that the app can reach its databases and that "
            "your provider keys are set; it has its own (i) with the full detail."
        ),
        beginner=(
            "This section helps you look behind the scenes. The switch, show "
            "diagnostic info in chat, decides whether the app shows a little extra "
            "detail as it works. Leave it off for a clean conversation. Turn it on "
            "and, just before each answer, the chat adds one line telling you how it "
            "read your request, which kind of search it is running, and the exact "
            "wording it is searching for. That is handy when a result looks off and "
            "you want to see what was actually searched. Underneath is a System "
            "health panel that runs a few quick checks on the app's plumbing; it has "
            "its own help icon that explains it in full."
        ),
        technical=(
            "The checkbox drives GuiConfig.debug_mode (default False), auto-saved "
            "with rollback. When on, the app emits a single debug-gated line before "
            "retrieval, via app._diag(): 'retrieving (input_mode=..., mode=..., "
            "query=...)' carrying the input mode, the resolved retrieval_mode, and "
            "the query. That is the only debug-gated output today; every other chat "
            "line (including the 'answer ready, N chunk + M KG sources' count) is "
            "unconditional, and there is no per-node or per-hit title/score stream. "
            "The System health board below is a separate feature with its own (i): it "
            "renders health.system_status() as OK or FAIL chips. This section only "
            "groups the two controls under one heading."
        ),
    ),
    "settings.database_connection": InfoText(
        title="Database connection",
        standard=(
            "A read-only view of the databases the active corpus uses. It shows the "
            "active corpus name, its Neo4j knowledge-graph connection (URI and user), "
            "and its LanceDB search-store path. A corpus's connection is fixed when "
            "you create it (Library, Create New Dataset) and cannot be edited here; "
            "to use a different one, switch the active corpus with the 'Selected "
            "corpus' dropdown at the top right. Connection pool size and acquisition "
            "timeout are advanced Neo4j tuning values; change them by setting the "
            "NEO4J_MAX_CONNECTION_POOL_SIZE and NEO4J_CONNECTION_ACQUISITION_TIMEOUT "
            "environment variables and restarting the app."
        ),
        beginner=(
            "This just shows you which databases your current corpus (your searchable "
            "document collection) is wired to. Nothing here can be typed over, it is "
            "for looking only. The app keeps two stores: Neo4j, which holds the "
            "knowledge graph (how facts and documents link together), and LanceDB, "
            "which holds the searchable text. Each corpus is tied to its own stores "
            "when you first create it in the Library tab, and that wiring stays put; "
            "if you want different databases, make or switch to another corpus using "
            "the dropdown at the top right. The last two lines, pool size and "
            "acquisition timeout, are fine-tuning numbers for advanced setups; you "
            "can ignore them unless someone tells you otherwise."
        ),
        technical=(
            "Read-only mirror of the active corpus. Active corpus, Neo4j URI, Neo4j "
            "user, and LanceDB path come from GuiConfig (the mirror of the active "
            "CorpusEntry); the connection is set at Library, Create New Dataset and "
            "is immutable here (the old on-blur editors were removed at the read-only "
            "pivot). Switch corpora via the top-bar dropdown (app.select_corpus). "
            "Connection pool size (neo4j_max_connection_pool_size, default 100) and "
            "acquisition timeout (neo4j_connection_acquisition_timeout, default "
            "60.0s) are read live from get_settings() and passed to "
            "AsyncGraphDatabase.driver(); they fall back to the pydantic Field "
            "defaults with a '(default)' suffix if Settings cannot be built yet (for "
            "example before a corpus bridges NEO4J_PASSWORD). Both are read once at "
            "driver construction, so an override via the matching uppercase env var "
            "needs a restart."
        ),
    ),
    # ---- Search tab: LLMs sub-tab (llm_tab.py) ----
    "llms.installed_providers": InfoText(
        title="Installed providers",
        standard=(
            "A read-only list of the LLM providers you have installed. There is no "
            "single active provider: you pick a model per step in the section below, "
            "and it can come from any installed provider.\n\n"
            "Install or uninstall providers in the Installs tab, not here. This "
            "section only reflects what is set up; if nothing is installed yet, it "
            "points you there.\n\n"
            "This list is read when the tab opens. If you install a provider, reopen "
            "the tab (or restart the app) to see it appear here and in the model "
            "pickers below.\n\n"
            "The four LLM providers are Anthropic, OpenAI, Google and Ollama (local). "
            "Voyage is an embedder, so it is not listed here; it appears only in the "
            "rate-limit section below."
        ),
        beginner=(
            "This box just shows which AI model providers are already set up on this "
            "computer. You do not change anything here.\n\n"
            "To add a provider (Anthropic, OpenAI, Google, or the local Ollama), open "
            "the Installs tab and install it there. After installing, reopen this tab "
            "(or restart the app) and the new provider shows up here and in the "
            "pickers just below.\n\n"
            "If the box says none are installed, start in the Installs tab."
        ),
        technical=(
            "Read-only. Each provider's presence comes from its is_installed_fn in "
            "LLM_PROVIDER_REGISTRY, which probes importlib.util.find_spec (no heavy "
            "import) for the adapter package (langchain-anthropic, langchain-openai, "
            "langchain-google-genai, langchain-ollama).\n\n"
            "The list is filled once when the tab first builds in a session; it does "
            "not live-refresh after an install, so reopen the tab or restart to "
            "reflect a change (installing a provider requires a restart to take "
            "effect anyway). The same install state drives the per-step model options "
            "below (available_models lists only installed providers' models)."
        ),
    ),
    "llms.models": InfoText(
        title="Select LLM models and temperatures",
        standard=(
            "Pick which model runs at each step of answering a question, plus its "
            "temperature. Each step is a separate choice, so you can use a cheap "
            "model where speed matters and a stronger one where reasoning matters.\n\n"
            "The steps:\n"
            "- Chat router: the conversational layer. It reads the running "
            "conversation, decides when to search, and distils a clean, "
            "self-contained query (resolving 'it', 'those', earlier context) before "
            "retrieval. It runs only in Conversational input mode. Routing is light, "
            "so a cheap, fast model is usually enough.\n"
            "- Mode classifier: in Auto retrieval mode, reads your question and picks "
            "which store answers it. A small, repeated task; a cheap model suits it.\n"
            "- Query builder: rewrites your question into a clean query for the "
            "document (chunk) search. Also light.\n"
            "- Cypher builder: writes the graph query from your question when the "
            "knowledge graph is used. This needs stronger, compositional reasoning, "
            "so a more capable model is worth it.\n"
            "- Synthesizer: writes the final answer with citations from the retrieved "
            "evidence. It benefits from a stronger model; it runs once per question, "
            "so the cost stays small.\n\n"
            "For each step:\n"
            "- Model (dropdown): choose any model from any installed provider. You "
            "can also type a model name to use one that is not in the list.\n"
            "- Temperature (slider): 0.0 is focused and consistent; higher is more "
            "varied. These steps produce structured output, so low is usually best. "
            "The slider greys out for models that do not accept a temperature (the "
            "newest Anthropic models), because it would have no effect.\n\n"
            "These are the query-time models. The ingest-time extractor models "
            "(entities and triples) are set per corpus in the Library tab."
        ),
        beginner=(
            "Answering a question happens in a few steps, and each step can use a "
            "different AI model. This section sets the model and its 'temperature' "
            "for each step.\n\n"
            "The steps, in plain terms:\n"
            "- Chat router: understands the back-and-forth of the chat and turns what "
            "you meant into a clear search request. Used only in the Conversational "
            "input mode. A small, fast model is fine.\n"
            "- Mode classifier: when the retrieval mode is Auto, decides where to "
            "look (your documents, the knowledge graph, or both). A small model is "
            "fine.\n"
            "- Query builder: tidies your question into a good search query for your "
            "documents. A small model is fine.\n"
            "- Cypher builder: writes the special query used to search the knowledge "
            "graph. Trickier, so a stronger model helps.\n"
            "- Synthesizer: writes the final answer and its citations. A stronger "
            "model gives better answers; it only runs once per question.\n\n"
            "For each step:\n"
            "- Model: pick from the models you have installed. Cheaper models are "
            "faster and cost less; stronger models reason better.\n"
            "- Temperature: how varied the model is. Low (near 0) keeps it focused "
            "and repeatable, which is what these steps want; higher makes it more "
            "varied. If the slider is greyed out, the chosen model does not use this "
            "setting.\n\n"
            "A safe starting point is the defaults: light models for the router, "
            "classifier and query builder, and stronger ones for the cypher builder "
            "and synthesizer."
        ),
        technical=(
            "The four graph nodes read Settings.<node>_model and "
            "Settings.<node>_temperature (env MODE_CLASSIFIER_MODEL / _TEMPERATURE, "
            "QUERY_BUILDER_*, CYPHER_BUILDER_*, SYNTHESIZER_*), bridged by "
            "apply_llm_to_env and consumed via get_llm_ref wrapped in with_retry. "
            "Defaults come from PROVIDER_NODE_DEFAULTS (Haiku for the classifier and "
            "query builder, Sonnet for the cypher builder and synthesizer).\n\n"
            "Chat router is GUI-only: chat_router_model and chat_router_temperature "
            "live in GuiConfig, are not bridged to env, and are read by "
            "get_chat_router (via get_llm_ref) only when the input mode is "
            "conversational. It is not wrapped in with_retry, so the retry setting "
            "does not cover it.\n\n"
            "Each selection is stored as a provider:model reference (format_model_ref), "
            "so a step can target any installed provider. The dropdown is editable: a "
            "typed value is stored verbatim, and a bare model name with no provider "
            "prefix falls back to the global llm_provider for dispatch (and for the "
            "temperature check).\n\n"
            "Temperature is omitted for the newest Anthropic models (claude-opus-4-8, "
            "claude-opus-4-7, claude-sonnet-5, claude-fable-5, claude-mythos-5), which "
            "reject the sampling parameter with a 400. The slider greys via "
            "supports_temperature and the factory omits the parameter for the same "
            "set, so the grey-out matches actual behaviour. Extractor models (entity "
            "and triples, ingest-time) are per corpus in corpus.toml, set in the "
            "Library tab."
        ),
    ),
    "llms.rate_limits": InfoText(
        title="Per-provider rate limits + retries",
        standard=(
            "Two kinds of control: per-provider rate caps, and a shared retry "
            "count.\n\n"
            "Rate limit (requests/sec) per provider: caps how many requests per "
            "second the app sends to that provider. Leave a field blank for no limit. "
            "Set one when your provider tier limits you below the app's concurrent "
            "fan-out (for example, the many parallel calls during ingest or "
            "evaluation).\n"
            "- Anthropic, OpenAI, Google, Ollama: throttle that provider's LLM (chat) "
            "calls, meaning the models in the section above.\n"
            "- OpenAI and Google also produce embeddings, but these caps throttle "
            "only their chat calls, not their embedding calls.\n"
            "- Voyage: throttles Voyage embedding requests. Voyage is the default "
            "embedder, and this is a global cap applied wherever it embeds (mainly "
            "ingest).\n\n"
            "LLM max retries (1 to 10): how many attempts each LLM call gets when it "
            "fails transiently (a rate-limit response or a network blip), using "
            "exponential backoff. The default 3 is a sensible balance. This covers "
            "the query-time graph steps and the ingest-time extractors."
        ),
        beginner=(
            "These stop the app from calling a provider too quickly, and control how "
            "many times it retries a failed call.\n\n"
            "Rate limit (requests/sec): some accounts only allow so many requests per "
            "second. Leave these blank unless you actually see rate-limit errors; "
            "then enter a small number for that provider to slow the app to a safe "
            "pace. It matters most during ingest, when the app makes many calls at "
            "once.\n\n"
            "LLM max retries: if a call briefly fails (the provider is busy, or the "
            "network hiccups), the app waits a moment and tries again, up to this "
            "many times. Three is a good default; raise it a little if your "
            "connection is flaky."
        ),
        technical=(
            "Each rate field maps to Settings.<provider>_requests_per_second (env "
            "<PROVIDER>_REQUESTS_PER_SECOND). Blank bridges as an unset env var, so no "
            "limiter is wired (the default). A positive value builds one "
            "InMemoryRateLimiter token bucket per provider, shared across that "
            "provider's cached clients (llm_factory._rate_limiter_for).\n\n"
            "Anthropic, OpenAI, Google and Ollama caps apply to LLM (chat) calls "
            "only. OpenAI and Google embedding calls are not throttled by these caps: "
            "LangChain Embeddings clients take no rate-limiter argument. Voyage maps "
            "to Settings.voyage_requests_per_second (env VOYAGE_REQUESTS_PER_SECOND) "
            "and throttles the native Voyage client: embed_texts / embed_chunks "
            "acquire a token from the same InMemoryRateLimiter class before each "
            "native call.\n\n"
            "LLM max retries maps to Settings.llm_max_retries (env LLM_MAX_RETRIES) "
            "and drives with_retry(stop_after_attempt=N, wait_exponential_jitter=True) "
            "at the four graph nodes and the entity and triples extractors; the GUI "
            "chat router is not wrapped. Rate limits and retries here are saved "
            "settings applied on the next call, not per-request overrides."
        ),
    ),
    # ---- Search tab: Retrieval sub-tab (retrieval_tab.py) ----
    "retrieval.mode": InfoText(
        title="Mode",
        standard=(
            "This section has two separate controls.\n\n"
            "Retrieval mode (the dropdown) chooses which store answers your "
            "question:\n"
            "- Auto: the app reads your question and picks one of the five modes "
            "below for you.\n"
            "- LanceDB only: searches the text inside your documents (the chunks). "
            'Best for "what do the documents say about X".\n'
            "- Neo4j only: queries the knowledge graph (the network of authors, "
            "documents, citations and entities) instead of the text. Best for "
            '"who wrote X", "what cites Y", counts and relationships.\n'
            "- LanceDB then Neo4j: finds the most relevant chunks first, then "
            "looks up graph facts about those same documents and adds them.\n"
            "- Neo4j then LanceDB: uses the graph to narrow to a set of documents "
            "first (say, by author or year), then searches chunk text only inside "
            "that set.\n"
            "- Parallel fused: runs the chunk search and the graph query at the "
            "same time, and the answer step combines both.\n\n"
            "LanceDB search mode (the radio buttons) chooses how the chunk search "
            "itself works. It only matters when the chosen mode runs a chunk "
            "search:\n"
            "- Hybrid: combines keyword matching (BM25) with meaning-based vector "
            "search. The all-round default.\n"
            "- FTS: keyword matching only. Best when exact words matter (names, "
            "codes, precise phrases).\n"
            "- Vector: meaning-based only. Best when your wording differs from the "
            "documents but the concept is the same.\n\n"
            "Controls the current mode does not use are greyed out."
        ),
        beginner=(
            "Two questions decide how a search runs.\n\n"
            "First, where should the app look? That is the Retrieval mode "
            "dropdown:\n"
            "- Auto: let the app choose for you. Use this if you are unsure.\n"
            "- LanceDB only: read the actual words inside your documents. Use it "
            'for "what do the documents say about ...".\n'
            "- Neo4j only: look at the map of who wrote what and what links to "
            "what, rather than the text. Use it for "
            '"who wrote ...", "what cites ...", or counting things.\n'
            "- LanceDB then Neo4j: find the text first, then add facts about those "
            "documents (such as their authors or citations).\n"
            "- Neo4j then LanceDB: first pick documents by a fact (like an "
            "author), then read the text only inside those.\n"
            "- Parallel fused: do both at once and blend the result. Good for big, "
            "open-ended questions.\n\n"
            "Second, how should it search the text? That is the LanceDB search "
            "mode buttons (they only matter when the app reads document text):\n"
            "- Hybrid: matches both exact words and meaning. Leave it here if "
            "unsure.\n"
            "- FTS: exact words only. Good when a specific term or name must "
            "appear.\n"
            "- Vector: meaning only. Good when you describe an idea in your own "
            "words.\n\n"
            "Starting out, Auto with Hybrid is a safe pairing."
        ),
        technical=(
            "Agent-level mode maps to Settings.default_retrieval_mode (env "
            "DEFAULT_RETRIEVAL_MODE) and is also passed per-search as a state "
            "override that wins over the saved default. It selects the LangGraph "
            "topology:\n"
            "- auto: an LLM mode_classifier reads the query and picks one of the "
            "five concrete modes (fail-soft to lancedb_only on error).\n"
            "- lancedb_only: query_builder, then lancedb_retriever, then "
            "synthesizer.\n"
            "- neo4j_only: cypher_builder, then neo4j_retriever, then "
            "synthesizer.\n"
            "- lancedb_then_neo4j: the chunk hits are fed into cypher_builder so "
            "its Cypher can correlate by doc_id.\n"
            "- neo4j_then_lancedb: the doc_ids from the graph rows become a "
            '"doc_id IN (...)" filter on the chunk search (unfiltered fallback '
            "when none).\n"
            "- parallel_fused: both legs fan out and the synthesizer LLM merges "
            "the two evidence lists. There is no cross-store rank fusion.\n\n"
            "Within-store mode maps to Settings.lancedb_search_mode (env "
            "LANCEDB_SEARCH_MODE):\n"
            "- hybrid: BM25 and cosine-vector legs fused by LanceDB's RRF "
            "reranker.\n"
            "- fts: BM25 only (MMR does not apply).\n"
            "- vector: cosine kNN only.\n\n"
            "Direct Cypher input mode forces the store to neo4j_only, overriding "
            "this dropdown. Unused knobs grey out via apply_gray_out."
        ),
    ),
    "retrieval.result_size": InfoText(
        title="Result size",
        standard=(
            "top_k is how many document chunks a search returns: the final result "
            "count.\n\n"
            "The search gathers a larger pool of candidate chunks, ranks them, and "
            "keeps the best top_k. It caps document chunks only; graph rows have "
            "their own limit (see Knowledge graph). The allowed range is 1 to 50, "
            "and top_k must not exceed num_candidates (the form reverts the value "
            "if you break that).\n\n"
            'It has no effect in "Neo4j only" mode, which returns graph rows, not '
            "chunks."
        ),
        beginner=(
            "This is simply how many results come back. Around 5 is a good start: "
            "enough to answer most questions without burying you in text. Raise it "
            "to have the assistant weigh more sources, lower it for short, focused "
            "answers. The most you can set is 50."
        ),
        technical=(
            "Maps to Settings.top_k (env TOP_K); also sent per-search in the graph "
            "state, where it overrides the saved default. Constraints: 1 to 50, "
            "and top_k must be less than or equal to num_candidates (the Retrieval "
            "tab checks this itself and reverts an edit that breaks it). In hybrid "
            "and vector search the ranked pool is cut, or MMR-reranked, to top_k; "
            "FTS limits straight to top_k. It is not greyed in Neo4j-only mode but "
            "has no effect there (no chunk leg runs)."
        ),
    ),
    "retrieval.rrf": InfoText(
        title="Hybrid fusion (RRF)",
        standard=(
            "Two knobs that shape the chunk search before it returns your top_k "
            "results.\n\n"
            "- num_candidates: how many rows the search pulls into its working "
            "pool before trimming down to top_k. A bigger pool means better recall "
            "and better fusion, at a little more cost. It applies to Hybrid and "
            "Vector search, and must be at least top_k (the form reverts an edit "
            "that breaks this).\n"
            "- RRF rank constant k: used only by Hybrid. When Hybrid blends its "
            "keyword (BM25) and vector results, each hit scores 1/(k + rank). A "
            "lower k lets the very top hits dominate; a higher k evens out the "
            "difference between ranks."
        ),
        beginner=(
            'These fine-tune the "Hybrid" chunk search, and you can usually leave '
            "them alone.\n\n"
            "- num_candidates: how wide a net the search casts before picking the "
            "best few. Larger can find slightly better results but is a touch "
            "slower. The default (100) suits most corpora.\n"
            "- RRF rank constant: how strongly the very top matches are favoured "
            "when Hybrid blends keyword and meaning results. The default (60) is a "
            "sensible balance. Lower favours the top hits more; higher treats more "
            "results evenly."
        ),
        technical=(
            "num_candidates maps to Settings.num_candidates (env NUM_CANDIDATES, "
            "default 100); the RRF constant to Settings.rrf_rank_constant (env "
            "RRF_RANK_CONSTANT, default 60). In Hybrid the BM25 and cosine-vector "
            "legs each fetch num_candidates rows and LanceDB's RRF reranker fuses "
            "them with score 1/(k + rank); the fused pool is then cut to top_k, or "
            "MMR-reranked. Vector search also fetches num_candidates but ignores "
            "k. FTS uses neither and limits straight to top_k, so num_candidates "
            "has no effect in FTS (it stays editable there; the RRF constant greys "
            "out in both FTS and Vector). From the GUI these two are saved "
            "settings, applied on the next search, not per-search overrides."
        ),
    ),
    "retrieval.mmr": InfoText(
        title="Diversity (MMR)",
        standard=(
            "MMR (Maximal Marginal Relevance) re-ranks the chunk pool to cut "
            "near-duplicates, so the results cover more ground instead of "
            "repeating the same point.\n\n"
            "- Apply MMR diversity re-rank (checkbox): turns the re-rank on or "
            "off.\n"
            "- MMR lambda (slider): the balance between relevance and variety. "
            "Higher favours relevance (results closest to your query); lower "
            "favours diversity (results that differ from each other).\n\n"
            "MMR needs vectors, so it applies to Hybrid and Vector search, not FTS."
        ),
        beginner=(
            "Turn this on when your results feel repetitive.\n\n"
            "- Apply MMR diversity re-rank: when several top chunks say almost the "
            "same thing, tick this to spread the results across different points. "
            "Off by default.\n"
            "- MMR lambda: slide toward the right for the most on-topic results, "
            "or toward the left for more variety. The middle is a sensible "
            "balance.\n\n"
            "It only works with the Hybrid or Vector search modes (it needs "
            "meaning-based vectors), so it is unavailable for FTS."
        ),
        technical=(
            "use_mmr maps to Settings.default_use_mmr (env DEFAULT_USE_MMR, "
            "default off); mmr_lambda to Settings.mmr_lambda (env MMR_LAMBDA, 0.0 "
            "to 1.0, default 0.6). When on, the num_candidates pool is re-ranked "
            "Python-side down to top_k by score = lambda * sim(query, c) minus "
            "(1 - lambda) * max sim(c, already-picked), cosine on each row's "
            "stored embedding. lambda 1.0 is pure relevance, 0.0 is pure "
            "diversity. FTS ignores use_mmr (no query vector); a pool row missing "
            "its embedding falls back to plain relevance order. The checkbox greys "
            "out in FTS and Neo4j-only; the slider greys out whenever MMR is off. "
            "Both are saved settings applied on the next search, not per-search "
            "overrides."
        ),
    ),
    "retrieval.knowledge_graph": InfoText(
        title="Knowledge graph",
        standard=(
            "kg_max_rows is the most rows a single knowledge-graph query may "
            "return.\n\n"
            "It is a hard cap on graph results (authors, citations, entities and "
            "other structured rows). It is separate from top_k, which caps "
            "document chunks; graph queries often return more rows, for example "
            "the 50 citations of a paper.\n\n"
            'It has no effect in "LanceDB only" mode, which does not query the '
            "graph."
        ),
        beginner=(
            "This limits how many rows the graph side hands back at once. The "
            "default (50) is plenty for most questions. Raise it when you ask for "
            "long lists (say, every author across many papers) and want to see "
            'more; lower it to keep answers tight. It does nothing in "LanceDB '
            'only" mode, which never touches the graph.'
        ),
        technical=(
            "Maps to Settings.kg_max_rows (env KG_MAX_ROWS, default 50). The Neo4j "
            'retriever wraps the generated Cypher as "CALL { <cypher> } RETURN * '
            "LIMIT kg_max_rows\", so Neo4j enforces the cap even when the LLM's "
            "own query forgets a LIMIT or sets a larger one. Distinct from top_k. "
            'Greys out in "LanceDB only" (no graph leg runs), and is a saved '
            "setting applied on the next search."
        ),
    ),
    "retrieval.input_mode": InfoText(
        title="Input mode",
        standard=(
            "This section has two controls: how your typed input becomes a search, "
            "and whether the app writes an answer or just shows the raw results.\n\n"
            "Input mode (the radios) decides how your text is turned into a query:\n"
            "- Conversational: the chat assistant reads the conversation, decides "
            "when to search, and turns your intent into a query for you. The normal "
            "chat experience.\n"
            "- Refined query: no chat assistant, but the query builder still "
            "rewrites your text into a search-friendly query before searching.\n"
            "- Direct query: your exact text is the search query, with no "
            "rewriting.\n"
            "- Direct Cypher: your text is run as a read-only Cypher query against "
            "the knowledge graph. This forces the search to Neo4j only, whatever the "
            "Mode dropdown says.\n\n"
            "Direct retrieval (the checkbox) decides what comes back:\n"
            "- Off: the app writes a synthesized answer with citations (the normal "
            "result).\n"
            "- On: the app skips the answer-writing step and returns the raw "
            "retrieved chunks and graph rows so you can read the sources yourself. "
            "Works with every retrieval mode."
        ),
        beginner=(
            "Leave Input mode on Conversational and Direct retrieval off, and it "
            "works like a normal chat: you type a question in plain words, and the "
            "app figures out how and when to search and writes you an answer.\n\n"
            "The other input modes are shortcuts for advanced users:\n"
            "- Refined query and Direct query give you more control over the exact "
            "search words.\n"
            "- Direct Cypher is for people who want to write graph queries by "
            "hand.\n\n"
            "Turn Direct retrieval on only if you want to see the raw sources the "
            "app found, without a written answer. That is mainly for checking search "
            "quality."
        ),
        technical=(
            "Input mode is a GUI-only routing choice (GuiConfig.input_mode); it is "
            "not a backend setting. It maps at query time to graph knobs:\n"
            "- conversational: runs the chat router node, which gates retrieval "
            "(ready_to_retrieve) and distills a search_query, then the graph runs. "
            "The only mode that uses the router.\n"
            "- refined: skips the router; query_builder rewrites the text "
            "(skip_query_builder=False).\n"
            "- direct_query: skips the router and query_builder; the raw text is the "
            "query (skip_query_builder=True).\n"
            "- direct_cypher: the text is passed as user_cypher and run verbatim; "
            "store_forced_by_mode pins retrieval_mode to neo4j_only, and the "
            "read-only rails (is_cypher_read_only, LIMIT wrap, read session) reject "
            "writes.\n\n"
            "Direct retrieval maps to direct_retrieval (env DIRECT_RETRIEVAL plus a "
            "per-invocation state field, which wins from the GUI). When on, the "
            "synthesizer node is skipped: the answer is empty and the retrieved "
            "chunks and KG rows are returned as sources. It applies to every "
            "topology, since the synthesizer is the single convergence node."
        ),
    ),
    # ---- Log tab (log_tab.py) ----
    "log.overview": InfoText(
        title="Log",
        standard=(
            "The app's activity log, read in-app. File shows the full path of the current "
            "log file (kagent.log); the box below shows the last 500 lines of it, newest "
            "at the bottom, and the line under the buttons says how many of that file's "
            "lines are shown. The view is a snapshot: it does not update on its own, so "
            "press Refresh to re-read the file and pick up entries written since you "
            "opened the app. Open folder opens the log's folder in your file manager, "
            "where the older compressed log archives and a crashes folder also live. The "
            "text is read-only; you can select and copy it. To see more than the last 500 "
            "lines, open the file itself from that folder."
        ),
        beginner=(
            "This tab shows the app's diary: a running record of what it did, which helps "
            "when something goes wrong and you (or someone helping you) want to see what "
            "happened. The File line at the top is where that record is saved on your "
            "computer. The large box shows the most recent entries (the last 500 lines), "
            "with the newest at the bottom, where the view scrolls automatically. It does "
            "not refresh by itself, so press Refresh whenever you want to pick up the "
            "latest, since the app keeps adding to the log as it works. Press Open folder "
            "to jump to where the log is kept, in case you want to send the file to "
            "someone; that folder also holds older logs (compressed to save space) and a "
            "separate crashes folder for any serious errors. You cannot type here; it is "
            "for reading and copying only."
        ),
        technical=(
            "Reads the tail of the active rotating log. File is log_file_path(): the dir "
            "init_logging() resolved (KAGENT_LOG_DIR or settings.toml [logging].log_dir, "
            "else platformdirs.user_log_dir) plus kagent.log. The viewer shows the last "
            "500 lines (_TAIL_LINES) in file order, newest last, via a deque, read once "
            "when the tab is built at startup and again on each Refresh, never on tab "
            "re-select; the pane auto-scrolls to the bottom and the status reports "
            "shown-of-total for the current file only (the count resets on rotation). "
            "Open folder reveals log_file_path().parent (os.startfile / open / xdg-open). "
            "The file is written off the event loop (a QueueHandler feeds a background "
            "QueueListener) by a RotatingFileHandler that rotates at 20 MB and keeps 10 "
            "gzipped backups (kagent.log.N.gz); the active file stays plain text so "
            "tailing works. Home paths are redacted to ~ by RedactingFormatter. Level is "
            "INFO by default (LOG_LEVEL or settings.toml), with noisy libraries clamped "
            "to WARNING (transformers to ERROR). The .gz archives are not stitched into "
            "the view, and the crashes/ subdir (crash_<UTC>.log + fault.log) sits "
            "alongside; both are reached via Open folder."
        ),
    ),
}


def info(app: GuiApp, key: str) -> ft.Control:
    """Render the (i) help-icon row for a registry key.

    Looks up `INFO[key]` and delegates to `info_point`. A `KeyError` means
    the key isn't registered (typo / missing entry) — surfaced loudly so it
    is caught in dev, never silently blank.
    """
    return info_point(app, INFO[key])
