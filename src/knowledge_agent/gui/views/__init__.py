"""Per-mode views rendered by `display_panel.DisplayPanel`.

Each view file owns one `MODE_*` value's render — `_frame.py` carries
the shared header / empty-state helpers so every view stays visually
consistent.
"""
